<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Account Settings</title>
  <style>
    :root{
      --bg:#f2ede3;
      --card:#fbf7ef;
      --line:#d9c8ad;
      --text:#16324f;
      --muted:#6f8096;
      --accent:#8e6d3d;
      --accent-soft:#f7f0e5;
      --good:#0f766e;
      --danger:#b42318;
      --warn:#a16207;
      --shadow:0 12px 24px rgba(15,23,42,.06);
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0;background:var(--bg);font:14px/1.4 Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:var(--text)}
    button,input,select,textarea{font:inherit}
    .wrap{max-width:none;width:100%;margin:0;padding:0 20px 20px}
    .backroom-header{display:grid;grid-template-columns:minmax(300px,340px) minmax(420px,1fr) auto;gap:0;align-items:stretch;width:100%;background:linear-gradient(135deg,#4b3923 0%,#6c522e 55%,#8e6d3d 100%);border:1px solid rgba(255,255,255,.05);border-radius:0 0 24px 24px;box-shadow:0 22px 48px rgba(15,23,42,.16);padding:0;margin:0 0 16px;overflow:visible;position:relative}
    .backroom-header::after{content:"";position:absolute;left:24px;right:24px;bottom:-10px;height:22px;background:radial-gradient(ellipse at center, rgba(15,23,42,.14), rgba(15,23,42,0) 72%);pointer-events:none;filter:blur(10px)}
    .backroom-brand{display:flex;flex-direction:column;justify-content:center;gap:4px;min-height:82px;padding:14px 18px 14px 78px;position:relative;background:linear-gradient(135deg,#8e6d3d 0%,#a8844a 62%,#6c522e 100%);color:#fff}
    .backroom-brand::before{content:"D";position:absolute;left:20px;top:50%;transform:translateY(-50%);width:42px;height:42px;border-radius:14px;background:rgba(255,255,255,.16);display:grid;place-items:center;font-size:20px;font-weight:900;letter-spacing:-.03em;box-shadow:inset 0 1px 0 rgba(255,255,255,.22),0 8px 18px rgba(0,0,0,.12)}
    .backroom-brand::after{content:"";position:absolute;right:-28px;top:-24px;bottom:-24px;width:74px;background:linear-gradient(120deg,rgba(255,255,255,.18),rgba(255,255,255,0));transform:skewX(-18deg)}
    .backroom-brand b{display:block;font-size:20px;letter-spacing:-.02em;color:#fff}
    .backroom-brand span{display:block;color:rgba(255,255,255,.82);font-size:12px;margin-top:2px}
    .backroom-nav{display:flex;flex-wrap:nowrap;align-items:center;justify-content:center;gap:4px;padding:0 18px;background:linear-gradient(180deg,rgba(255,255,255,.03) 0%,rgba(255,255,255,0) 100%);min-width:0;overflow:auto;scrollbar-width:none}.backroom-nav::-webkit-scrollbar{display:none}
    .nav-btn{appearance:none;border:0;background:transparent;color:rgba(255,255,255,.82);padding:0 14px;height:82px;font-weight:800;font-size:14px;cursor:pointer;position:relative;transition:background .18s ease,color .18s ease,transform .18s ease;border-radius:16px;flex:0 0 auto;white-space:nowrap}
    .nav-btn::after{content:"";position:absolute;left:16px;right:16px;bottom:10px;height:3px;border-radius:999px;background:transparent;transition:background .18s ease,transform .18s ease}
    .nav-btn:hover{background:rgba(255,255,255,.06);color:#fff;transform:translateY(-1px)}
    .nav-btn.active{background:linear-gradient(180deg,rgba(255,255,255,.08) 0%,rgba(255,255,255,.03) 100%);color:#fff;box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}
    .nav-btn.active::after{background:#d7b27a}
    .nav-btn.locked{opacity:.48}
    .backroom-user{display:flex;justify-content:flex-end;gap:10px;align-items:center;flex-wrap:nowrap;padding:12px 16px;background:linear-gradient(180deg,rgba(79,58,34,.92) 0%,rgba(58,42,24,.98) 100%);border-left:1px solid rgba(255,255,255,.06);position:relative;min-width:430px;overflow:visible}
    .backroom-user::before{content:"";position:absolute;left:-1px;top:14px;bottom:14px;width:1px;background:rgba(255,255,255,.08)}
    .backroom-search{display:flex;align-items:center;min-width:200px;max-width:280px;flex:0 1 260px;height:44px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.08);box-shadow:inset 0 1px 0 rgba(255,255,255,.04),0 8px 18px rgba(0,0,0,.08)}
    .backroom-search input{flex:1;min-width:0;height:100%;border:0;background:transparent;color:#fff;padding:0 14px;font:700 13px/1.2 Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif}
    .backroom-search input::placeholder{color:rgba(255,255,255,.58)}
    .backroom-search input:focus{outline:none}
    .backroom-search button{appearance:none;border:0;border-left:1px solid rgba(255,255,255,.1);background:transparent;color:#fff;height:100%;padding:0 14px;font-weight:800;cursor:pointer}
    .backroom-search button:hover{background:rgba(255,255,255,.08)}
    .top-chip{padding:8px 10px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.08);font-size:12px;color:rgba(255,255,255,.72);flex:0 0 auto;max-width:84px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
    .top-chip strong{color:#fff}
    .header-action-btn{appearance:none;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.08);color:#fff;border-radius:12px;padding:10px 12px;font-weight:800;cursor:pointer;transition:background .18s ease,border-color .18s ease,transform .18s ease;flex:0 0 auto;white-space:nowrap}
    .header-action-btn:hover{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22);transform:translateY(-1px)}
    .quick-apps{position:relative}
    .quick-apps-btn{appearance:none;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.08);color:#fff;border-radius:14px;padding:10px 12px;font-weight:800;cursor:pointer;transition:background .18s ease,border-color .18s ease,transform .18s ease;display:inline-flex;align-items:center;gap:8px;white-space:nowrap}
    .quick-apps-btn:hover{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22);transform:translateY(-1px)}
    .quick-apps-btn::before{content:"☰";font-size:13px;line-height:1}
    .quick-apps-panel{position:absolute;top:calc(100% + 12px);right:0;left:auto;min-width:420px;display:none;padding:14px;background:#3f2e1e;border:1px solid rgba(255,255,255,.08);border-radius:20px;box-shadow:0 22px 44px rgba(15,23,42,.28);z-index:40}
    .quick-apps.open .quick-apps-panel{display:block}
    .quick-apps-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}
    .quick-app-card{appearance:none;border:1px solid rgba(255,255,255,.08);background:linear-gradient(180deg,#5b4328 0%,#46311d 100%);color:#fff;border-radius:16px;padding:14px;display:flex;flex-direction:column;align-items:flex-start;gap:8px;cursor:pointer;min-height:104px;transition:transform .18s ease,border-color .18s ease,background .18s ease}
    .quick-app-card:hover{transform:translateY(-2px);border-color:rgba(180,138,79,.48);background:linear-gradient(180deg,#71522f 0%,#4c3720 100%)}
    .quick-app-card b{font-size:14px}
    .quick-app-card span{font-size:12px;line-height:1.4;color:rgba(255,255,255,.7);text-align:left}
    .quick-app-icon{width:34px;height:34px;border-radius:12px;display:grid;place-items:center;background:rgba(255,255,255,.08);font-size:15px}
    .top-icon-group{display:flex;align-items:center;gap:8px;flex:0 0 auto}
    .top-icon-btn{appearance:none;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.08);color:#fff;border-radius:12px;padding:10px 11px;display:grid;place-items:center;cursor:pointer;transition:background .18s ease,border-color .18s ease,transform .18s ease}
    .top-icon-btn:hover{background:rgba(255,255,255,.14);border-color:rgba(255,255,255,.22);transform:translateY(-1px)}
    .top-icon-btn .dot{width:8px;height:8px;border-radius:999px;background:#22c55e;box-shadow:0 0 0 4px rgba(34,197,94,.18)}
    .hero{display:grid;grid-template-columns:1.6fr 1fr;gap:16px;margin-bottom:16px}
    .panel{background:linear-gradient(180deg,#fdfaf4 0%,#f7f1e6 100%);border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow)}
    .hero-main{padding:18px 20px}
    .hero-side{padding:18px 20px;display:flex;flex-direction:column;gap:10px}
    .eyebrow{font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);font-weight:700}
    h1{margin:6px 0 10px;font-size:28px;line-height:1.1}
    .sub{color:var(--muted);max-width:900px}
    .kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-top:16px}
    .kpi{background:#fff;border:1px solid #eadfce;border-radius:16px;padding:10px 12px;min-height:74px}
    .kpi b{display:block;font-size:18px;margin-bottom:2px;letter-spacing:-.02em}
    .kpi span{color:var(--muted);font-size:12px;line-height:1.35}
    .mini-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
    .mini{border:1px solid var(--line);border-radius:14px;padding:12px;background:#fbfdff}
    .mini .label{font-size:12px;color:var(--muted);margin-bottom:3px}
    .mini .value{font-weight:700}
    .tabs{margin:0 0 14px}
    .tabs-fallback{display:flex;flex-wrap:wrap;gap:8px;margin:0 0 14px}
    .tabs-fallback .tab{padding:8px 11px}
    .workspace-board{background:linear-gradient(180deg,#fdfaf4 0%,#f7f1e6 100%);border:1px solid var(--line);border-radius:24px;padding:14px;margin-bottom:14px;box-shadow:0 12px 28px rgba(15,23,42,.05)}
    .workspace-board .settings-nav-shell{background:transparent;border:0;box-shadow:none;padding:0 0 12px;border-bottom:1px solid #e7dbc8;border-radius:0}
    .workspace-board .tabs{margin:0}
    .workspace-board .tabs-fallback{margin:0 0 10px}
    .workspace-board .section .panel.section-card{background:transparent;border:0;box-shadow:none;padding:14px 4px 2px}
    .settings-nav-shell{display:grid;gap:10px;padding:12px 14px;background:linear-gradient(180deg,#ffffff 0%,#fbf8f3 100%);border:1px solid var(--line);border-radius:22px;box-shadow:0 14px 26px rgba(15,23,42,.06)}
    .settings-nav-topline{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;padding-bottom:4px;border-bottom:1px solid #eadfce}
    .settings-context{display:flex;align-items:center;gap:10px;flex-wrap:wrap}
    .settings-context-badge{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:999px;background:#6c522e;color:#fff;font-size:12px;font-weight:900;letter-spacing:.04em;text-transform:uppercase;box-shadow:0 8px 18px rgba(13,47,98,.16)}
    .settings-context-badge::before{content:"⚙";font-size:13px}
    .settings-context-copy{display:flex;flex-direction:column;gap:2px}
    .settings-context-copy strong{font-size:14px;letter-spacing:-.01em}
    .settings-context-copy span{color:var(--muted);font-size:12px}
    .settings-nav-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .top-options{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px}
    .top-option-group{border:1px solid #e7dbc8;border-radius:14px;background:linear-gradient(180deg,#fffdfa 0%,#f7f1e8 100%);padding:8px}
    .top-option-label{font-size:10px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:#8e6d3d;margin:0 0 6px 2px}
    .top-option-items{display:flex;flex-wrap:wrap;gap:6px}
    .top-option-tab{appearance:none;border:1px solid #e6dbc9;background:#fff;color:#6c522e;border-radius:999px;padding:7px 10px;font-size:11px;font-weight:800;cursor:pointer;white-space:nowrap}
    .top-option-tab.active{background:linear-gradient(180deg,#a8844a 0%,#8e6d3d 100%);border-color:#8e6d3d;color:#fff;box-shadow:0 6px 14px rgba(142,109,61,.16)}
    .group-rail{display:flex;gap:8px;overflow:auto;padding-bottom:2px;scrollbar-width:thin;align-items:center;background:linear-gradient(180deg,#fbf8f3 0%,#f7f1e8 100%);border:1px solid #eee3d2;border-radius:999px;padding:6px 8px}
    .group-pill{padding:8px 14px;border-radius:999px;border:1px solid transparent;background:transparent;color:#7b6a56;cursor:pointer;font-weight:800;font-size:12px;white-space:nowrap;transition:all .18s ease}
    .group-pill.active{background:#fff;border-color:#d9c3a2;color:var(--accent);box-shadow:0 6px 14px rgba(142,109,61,.10)}
    .section-row{display:flex;gap:10px;align-items:center;flex-wrap:wrap;justify-content:space-between}
    .section-rail{display:flex;gap:8px;overflow:auto;flex:1 1 720px;padding-bottom:2px;scrollbar-width:thin;min-height:42px}
    .tab{padding:9px 12px;border-radius:999px;border:1px solid #e6dbc9;background:#fff;color:var(--text);cursor:pointer;font-weight:700;font-size:12px;white-space:nowrap;transition:all .18s ease}
    .tab.active{background:linear-gradient(180deg,#a8844a 0%,#8e6d3d 100%);border-color:#8e6d3d;color:#fff;box-shadow:0 8px 16px rgba(142,109,61,.18)}
    .tab-picker{position:relative;flex:0 0 auto}
    .picker-trigger{padding:9px 13px;border-radius:999px;border:1px solid #e6dbc9;background:#fff;color:var(--text);cursor:pointer;font-weight:700;font-size:12px;white-space:nowrap;box-shadow:0 4px 10px rgba(15,23,42,.03)}
    .picker-menu{position:absolute;right:0;top:calc(100% + 8px);min-width:300px;max-width:min(92vw,380px);max-height:62vh;overflow:auto;display:none;background:#fff;border:1px solid var(--line);border-radius:16px;box-shadow:0 18px 36px rgba(15,23,42,.12);padding:10px;z-index:20}
    .picker-menu.open{display:block}
    .picker-section{display:grid;gap:8px;padding:8px 0}
    .picker-section + .picker-section{border-top:1px solid #eef2f7}
    .picker-label{font-size:10px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);padding:0 4px}
    .picker-items{display:flex;flex-wrap:wrap;gap:8px}
    .section{display:none}
    .section.active{display:block}
    .section-card{padding:18px 18px 16px}
    .section-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:14px}
    .section-head h2{margin:0;font-size:22px;letter-spacing:-.02em}
    .section-head p{margin:4px 0 0;color:var(--muted)}
    .section.active .section-card{border-top-left-radius:0;border-top-right-radius:0;border-top:0}
    .section.active .section-head > div:first-child{display:none}
    .actions{display:flex;gap:10px;flex-wrap:wrap}
    .btn{appearance:none;border:1px solid var(--line);background:#fffaf2;color:var(--text);border-radius:12px;padding:10px 14px;font-weight:700;cursor:pointer}
    .btn.primary{background:var(--accent);border-color:var(--accent);color:#fff}
    .btn.good{background:var(--good);border-color:var(--good);color:#fff}
    .btn.warn{background:#fff7ed;border-color:#fed7aa;color:var(--warn)}
    .btn.danger{background:#fff1f2;border-color:#fecdd3;color:var(--danger)}
    .btn:disabled{opacity:.55;cursor:not-allowed}
    .grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:14px}
    .field{display:flex;flex-direction:column;gap:6px}
    .field label{font-size:12px;color:var(--muted);font-weight:700;text-transform:uppercase;letter-spacing:.03em}
    .field input,.field select,.field textarea{width:100%;padding:11px 12px;border:1px solid var(--line);background:linear-gradient(180deg,#fffdfa 0%,#f8f2e8 100%);border-radius:14px;color:var(--text);box-shadow:inset 0 1px 0 rgba(255,255,255,.8),0 1px 2px rgba(15,23,42,.02)}
    .field input:focus,.field select:focus,.field textarea:focus{outline:none;border-color:#e2cda6;box-shadow:0 0 0 4px rgba(180,138,79,.10),inset 0 1px 0 rgba(255,255,255,.8)}
    .field textarea{min-height:92px;resize:vertical}
    .col-3{grid-column:span 3}.col-4{grid-column:span 4}.col-5{grid-column:span 5}.col-6{grid-column:span 6}.col-8{grid-column:span 8}.col-12{grid-column:span 12}
    .switch{display:flex;align-items:center;gap:10px;padding-top:28px}
    .switch input{width:18px;height:18px}
    .table-shell{display:flex;flex-direction:column;gap:10px}
    .table-toolbar{display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;padding:10px 12px;border:1px solid var(--line);border-radius:16px;background:linear-gradient(180deg,#ffffff 0%,#fbf8f3 100%)}
    .table-toolbar-left{display:flex;align-items:center;gap:8px;flex-wrap:wrap;min-width:0}
    .table-toolbar-right{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .table-toolbar-title{font-size:12px;font-weight:900;color:#0f172a;letter-spacing:.02em}
    .table-toolbar-sub{font-size:11px;color:#64748b;font-weight:700}
    .table-col-btn{appearance:none;border:1px solid rgba(15,23,42,.10);background:#fff;border-radius:12px;padding:8px 12px;font:900 12px/1 system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:#334155;cursor:pointer;box-shadow:0 8px 18px rgba(15,23,42,.04)}
    .table-col-btn:hover{border-color:rgba(168,132,74,.25);color:#8e6d3d;background:#f7f0e5}
    .table-col-btn.active{background:#f1e4cf;border-color:#d7b27a;color:#8e6d3d}
    .table-col-menu{position:relative}
    .table-col-panel{position:absolute;right:0;top:calc(100% + 8px);z-index:30;width:min(360px,calc(100vw - 40px));border:1px solid rgba(15,23,42,.12);border-radius:18px;background:#fff;box-shadow:0 18px 40px rgba(15,23,42,.14);display:none;overflow:hidden}
    .table-col-menu.open .table-col-panel{display:block}
    .table-col-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;padding:12px 12px 8px;border-bottom:1px solid #eef2f7;background:linear-gradient(180deg,#ffffff 0%,#fbf8f3 100%)}
    .table-col-head strong{display:block;font-size:12px;line-height:1.2;color:#0f172a}
    .table-col-head span{display:block;margin-top:4px;font-size:11px;line-height:1.35;color:#64748b}
    .table-col-search{padding:10px 12px;border-bottom:1px solid #eef2f7;background:#fff}
    .table-col-search input{width:100%;height:38px;border:1px solid rgba(15,23,42,.10);border-radius:12px;padding:0 12px;font:700 13px/1 system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:#334155;background:#fff}
    .table-col-search input:focus{outline:none;border-color:#d7b27a;box-shadow:0 0 0 4px rgba(180,138,79,.14)}
    .table-col-list{max-height:340px;overflow:auto;padding:8px;background:#fbfdff}
    .table-col-item{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:10px;padding:8px 10px;border:1px solid rgba(15,23,42,.08);border-radius:12px;background:#fff;margin-bottom:8px;cursor:grab;user-select:none}
    .table-col-item:last-child{margin-bottom:0}
    .table-col-item.dragging{opacity:.45}
    .table-col-item.drag-over{outline:2px dashed #d7b27a;outline-offset:-4px}
    .table-col-item.drag-over-after{outline:2px dashed #b48a4f;outline-offset:-4px;box-shadow:inset 0 -3px 0 #b48a4f}
    .table-col-check{display:flex;align-items:center;gap:8px;min-width:0}
    .table-col-check input{width:15px;height:15px;flex:0 0 auto}
    .table-col-label{display:flex;align-items:center;gap:8px;min-width:0;font-size:12px;font-weight:800;color:#334155}
    .table-col-label span:last-child{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
    .table-col-handle{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:999px;border:1px dashed rgba(15,23,42,.12);font-size:10px;color:#94a3b8;line-height:1;background:#fff;flex:0 0 auto}
    .table-col-order{display:inline-flex;align-items:center;justify-content:center;min-width:24px;height:24px;padding:0 7px;border-radius:999px;background:#f7f0e5;color:#8e6d3d;font-size:11px;font-weight:900}
    .table-col-empty{padding:12px;border:1px dashed rgba(15,23,42,.12);border-radius:12px;background:#fff;font-size:12px;font-weight:700;color:#64748b;text-align:center}
    .table-col-note{padding:10px 12px;border-top:1px solid #eef2f7;font-size:11px;color:#64748b;font-weight:700;background:#fff}
    .table-wrap{overflow:auto;border:1px solid var(--line);border-radius:16px;background:#fdfaf4}
    table{width:100%;border-collapse:collapse;min-width:900px}
    th,td{padding:12px 10px;border-bottom:1px solid #eef2f7;text-align:left;vertical-align:top}
    th{font-size:12px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;background:#efe4d2}
    .table-wrap table.is-enhanced th{position:relative;user-select:none;background:#fbf8f3;touch-action:none}
    .table-wrap table.is-enhanced th.drag-over{outline:2px dashed #d7b27a;outline-offset:-4px;box-shadow:inset 3px 0 0 #d7b27a}
    .table-wrap table.is-enhanced th.drag-over-after{outline:2px dashed #b48a4f;outline-offset:-4px;box-shadow:inset -3px 0 0 #b48a4f}
    .table-wrap table.is-enhanced th.dragging{opacity:.46}
    .th-inner{display:flex;align-items:center;justify-content:space-between;gap:10px;min-width:0}
    .th-label{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
    .th-tools{display:flex;align-items:center;gap:8px;flex:0 0 auto}
    .th-drag{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:999px;border:1px dashed rgba(15,23,42,.12);font-size:10px;color:#94a3b8;cursor:grab;line-height:1;background:#fff}
    .th-sort{display:inline-flex;align-items:center;justify-content:center;min-width:18px;height:18px;border-radius:999px;border:1px solid rgba(15,23,42,.08);font-size:10px;color:#94a3b8;line-height:1;background:#fff;padding:0 5px;cursor:pointer}
    .th-sort.active{color:var(--accent);border-color:#e8d4b6;background:#fbf6ee}
    .th-sort::before{content:'↕'}
    .th-sort[data-dir="asc"]::before{content:'↑'}
    .th-sort[data-dir="desc"]::before{content:'↓'}
    .th-resizer{position:absolute;top:0;right:-4px;width:8px;height:100%;cursor:col-resize;touch-action:none}
    .th-resizer::after{content:'';position:absolute;top:20%;bottom:20%;left:3px;width:2px;border-radius:999px;background:#d5deeb}
    .table-wrap table.is-enhanced th:last-child .th-resizer{display:none}
    body.is-table-resizing,body.is-table-resizing *{cursor:col-resize!important}
    tbody tr:hover{background:#fbfdff}
    .badge{display:inline-flex;align-items:center;gap:6px;padding:5px 10px;border-radius:999px;font-size:12px;font-weight:700;border:1px solid var(--line);background:#fff}
    .badge.good{color:var(--good);background:#ecfdf3;border-color:#abefc6}
    .badge.off{color:#667085;background:#f8fafc;border-color:#e2e8f0}
    .badge.private{color:#7c2d12;background:#fff7ed;border-color:#fed7aa}
    .toolbar{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:12px}
    .toolbar .left,.toolbar .right{display:flex;gap:10px;align-items:center;flex-wrap:wrap}
    .toolbar input,.toolbar select{padding:10px 12px;border:1px solid var(--line);border-radius:12px;background:#fff}
    .row-actions{display:flex;gap:8px;flex-wrap:wrap}
    .row-actions .link{background:none;border:0;color:var(--accent);padding:0;cursor:pointer;font-weight:700}
    .statusbar{position:sticky;bottom:14px;display:flex;justify-content:flex-end;margin-top:16px;pointer-events:none}
    .toast{pointer-events:auto;min-width:320px;max-width:520px;background:#0f172a;color:#fff;padding:12px 14px;border-radius:14px;box-shadow:0 18px 32px rgba(15,23,42,.18);display:none}
    .toast.show{display:block}
    .overlay{position:fixed;inset:0;background:rgba(15,23,42,.42);display:none;align-items:center;justify-content:center;padding:20px;z-index:50}
    .overlay.show{display:flex}
    .modal{width:min(860px,100%);max-height:92vh;overflow:auto;background:#fff;border-radius:22px;border:1px solid var(--line);box-shadow:0 30px 60px rgba(15,23,42,.18)}
    .modal-head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;padding:18px 20px;border-bottom:1px solid var(--line)}
    .modal-head h3{margin:0;font-size:22px}
    .modal-head p{margin:4px 0 0;color:var(--muted)}
    .modal-body{padding:18px 20px}
    .modal-foot{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:18px 20px;border-top:1px solid var(--line);position:sticky;bottom:0;background:#fff}
    .viewer-stack{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}
    .viewer-card{border:1px solid var(--line);border-radius:14px;background:#fbfdff;padding:12px}
    .viewer-card h4{margin:0 0 8px;font-size:14px}
    .codebox{margin:0;padding:12px;border-radius:12px;background:#0f172a;color:#e2e8f0;white-space:pre-wrap;word-break:break-word;max-height:320px;overflow:auto;font:12px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace}
    .kv-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
    .pill-list{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
    .pill{display:inline-flex;align-items:center;gap:6px;padding:7px 10px;border-radius:999px;border:1px solid var(--line);background:#fff;font-size:12px;font-weight:700}
    .pill.good{background:#ecfdf3;border-color:#bbf7d0;color:#166534}.pill.warn{background:#fff7ed;border-color:#fed7aa;color:#9a3412}.pill.off{background:#f8fafc;border-color:#e2e8f0;color:#475569}
    .empty{padding:20px;border:1px dashed var(--line);border-radius:16px;background:#fbfdff;color:var(--muted);text-align:center}
    .footnote{color:var(--muted);font-size:12px;margin-top:10px}
    .hero-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}
    .health-list{display:grid;gap:8px;margin-top:10px}
    .health-item{display:flex;justify-content:space-between;gap:12px;padding:10px 12px;border:1px solid var(--line);border-radius:12px;background:#fbfdff}
    .health-item b{display:block;font-size:13px;margin-bottom:2px}
    .health-item span{color:var(--muted);font-size:12px}
    .health-item.warn{border-color:#fed7aa;background:#fff7ed}
    .settings-stack{display:grid;gap:16px}
    .settings-group{border:1px solid var(--line);border-radius:18px;background:#fbfdff;padding:16px}
    .settings-group + .settings-group{margin-top:0}
    .settings-group-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;margin-bottom:14px}
    .settings-group-head h3{margin:0;font-size:16px}
    .settings-group-head p{margin:4px 0 0;color:var(--muted);font-size:13px;max-width:760px}
    .settings-group-badge{display:inline-flex;align-items:center;gap:6px;padding:6px 10px;border-radius:999px;border:1px solid var(--line);background:#fff;font-size:11px;font-weight:800;letter-spacing:.04em;text-transform:uppercase;color:var(--muted)}
    .settings-grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:14px}
    .field.compact textarea{min-height:72px}
    .group-note{margin-top:10px;color:var(--muted);font-size:12px}

    .settings-home-card{border:1px solid #eadfce;border-radius:18px;background:linear-gradient(180deg,#fbfdff 0%,#fbf8f3 100%);padding:12px 13px;box-shadow:0 6px 14px rgba(15,23,42,.03)}
    .settings-home-card h3{margin:0 0 4px;font-size:15px;letter-spacing:-.01em}
    .settings-home-card p{margin:0;color:var(--muted);font-size:12px;line-height:1.35}
    .settings-home-list{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:10px}
    .settings-home-item{border:1px solid #eadfce;border-radius:16px;background:#fff;padding:10px 11px}
    .settings-home-item b{display:block;font-size:13px;margin-bottom:2px}
    .settings-home-item span{display:block;color:var(--muted);font-size:12px;min-height:28px}
    .settings-home-item .actions{margin-top:8px}.settings-home-item .btn{padding:8px 11px;border-radius:10px}
    .summary-strip{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:10px}
    .summary-chip{border:1px solid #eadfce;border-radius:16px;background:#fff;padding:10px 12px;min-height:72px}
    .summary-chip .label{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;margin-bottom:4px}
    .summary-chip .value{font-size:14px;font-weight:800}
    .summary-chip .hint{font-size:11px;color:var(--muted);margin-top:2px}
    .quick-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}
    .quick-actions .btn{padding:9px 12px}
    .business-jumps{display:flex;flex-wrap:wrap;gap:8px;margin:0 0 16px}
    .jump-btn{border:1px solid var(--line);background:#fff;color:var(--text);padding:9px 12px;border-radius:999px;font-weight:700;cursor:pointer}
    .jump-btn:hover{border-color:#d8c2a0;color:var(--accent);background:var(--accent-soft)}
    .hero-side-card{border:1px solid #eadfce;border-radius:16px;background:#fbfdff;padding:12px}
    .hero-side-card h4{margin:0 0 8px;font-size:14px}
    .access-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
    .overview-grid{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(320px,.75fr);gap:14px;align-items:start}
    .overview-grid-secondary{display:grid;grid-template-columns:1fr;gap:14px;margin-top:14px}
    .overview-card{border:1px solid #d9c8ad;border-radius:22px;background:linear-gradient(180deg,#fdfaf4 0%,#f7f1e6 100%);padding:16px;box-shadow:0 10px 24px rgba(15,23,42,.04)}
    .overview-card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:8px}
    .overview-card h3{margin:0;font-size:18px;letter-spacing:-.02em}
    .overview-card p{margin:4px 0 0;color:var(--muted);font-size:12px;line-height:1.45}
    .overview-primary{background:linear-gradient(180deg,#fdfaf4 0%,#f7f1e6 100%)}
    .overview-status{display:grid;gap:12px}
    .overview-shortcuts{display:grid;gap:12px}
    .overview-status-panels{display:grid;gap:12px}
    .status-panel{border:1px solid #eadfce;border-radius:18px;background:#fff;padding:12px}
    .status-panel h4{margin:0 0 6px;font-size:15px}
    .status-panel p{margin:0;color:var(--muted);font-size:12px}
    .access-summary-shell{border:1px solid #eadfce;border-radius:18px;background:#fbfdff;padding:12px}
    .quick-focus-row{display:flex;flex-wrap:wrap;gap:8px;margin-top:4px}
    .micro-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:12px}
    .micro-card{border:1px solid #eadfce;border-radius:14px;background:#fff;padding:10px 11px}
    .micro-card .label{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin-bottom:4px}
    .micro-card .value{font-size:14px;font-weight:800}
    .nav-shell-helper{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:2px}
    .nav-shell-helper span{color:var(--muted);font-size:11px}

    .settings-command-deck{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin:0 0 16px}
    .settings-command-card{position:relative;overflow:hidden;border:1px solid #eadfce;border-radius:20px;background:linear-gradient(180deg,#ffffff 0%,#fbf8f3 100%);padding:14px 15px;box-shadow:0 10px 22px rgba(15,23,42,.04)}
    .settings-command-card::after{content:"";position:absolute;right:-18px;top:-18px;width:92px;height:92px;border-radius:999px;background:radial-gradient(circle,rgba(180,138,79,.14),rgba(180,138,79,0));pointer-events:none}
    .settings-command-kicker{font-size:11px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:#8e6d3d;margin-bottom:6px}
    .settings-command-title{font-size:17px;font-weight:950;letter-spacing:-.02em;color:#2f2418;margin:0 0 6px}
    .settings-command-copy{font-size:12px;line-height:1.5;color:var(--muted);margin:0 0 10px}
    .settings-command-pills{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:10px}
    .settings-command-pills .pill{background:#fffaf2;border-color:#e7d3b0;color:#6c522e;font-size:11px;padding:6px 9px}
    .settings-command-actions{display:flex;flex-wrap:wrap;gap:8px}
    .settings-command-actions .btn{padding:8px 11px;border-radius:11px;font-size:12px;font-weight:800}

    .business-shell{display:grid;gap:14px}
    .business-pane-shell{border:1px solid #eadfce;border-radius:20px;background:linear-gradient(180deg,#ffffff 0%,#fbfdff 100%);padding:12px;box-shadow:0 8px 18px rgba(15,23,42,.04)}
    .business-subtabs{display:flex;gap:8px;align-items:center;justify-content:space-between;flex-wrap:wrap;margin-bottom:10px}
    .business-area-rail{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px}
    .business-area-btn{padding:8px 12px;border-radius:999px;border:1px solid #e6dbc9;background:#fff;color:#6c522e;cursor:pointer;font-weight:800;font-size:12px}
    .business-area-btn.active{background:linear-gradient(180deg,#a8844a 0%,#8e6d3d 100%);border-color:#8e6d3d;color:#fff;box-shadow:0 8px 16px rgba(142,109,61,.14)}
    .business-subtabs-left{display:flex;gap:8px;overflow:auto;scrollbar-width:thin;flex:1 1 720px;padding-bottom:2px}
    .business-subtab{padding:10px 13px;border-radius:999px;border:1px solid #e6dbc9;background:#fff;color:#6c522e;cursor:pointer;font-weight:800;font-size:12px;white-space:nowrap;transition:all .18s ease;box-shadow:0 3px 8px rgba(15,23,42,.03)}
    .business-subtab.active{background:linear-gradient(180deg,#a8844a 0%,#8e6d3d 100%);border-color:#8e6d3d;color:#fff;box-shadow:0 10px 18px rgba(142,109,61,.16)}
    .business-subtab-picker{position:relative;flex:0 0 auto}
    .business-subtab-trigger{padding:10px 13px;border-radius:999px;border:1px solid #e6dbc9;background:#fff;color:var(--text);cursor:pointer;font-weight:700;font-size:12px;white-space:nowrap}
    .business-subtab-menu{position:absolute;right:0;top:calc(100% + 8px);min-width:320px;max-width:min(92vw,420px);max-height:65vh;overflow:auto;display:none;background:#fff;border:1px solid var(--line);border-radius:16px;box-shadow:0 18px 36px rgba(15,23,42,.12);padding:10px;z-index:35}
    .business-subtab-menu.open{display:block}
    .business-subtab-menu .picker-items{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}
    .business-subtab-note{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:8px 12px;border:1px solid #eee3d2;border-radius:14px;background:#fbf8f3;color:var(--muted);font-size:12px;margin-bottom:12px}
    #businessPaneTabs,#businessPaneNote{display:none}
    .section[data-section="business"] .section-head{justify-content:flex-end}
    .section[data-section="business"] .section-head > div:first-child{display:none}
    .section[data-section="business"] .section-head .actions{display:none}
    .business-pane{display:none}
    .business-pane.active{display:block}
    .embedded-manager{display:grid;gap:12px;margin-top:14px;padding-top:14px;border-top:1px solid #e4d5bf}
    .embedded-manager-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap}
    .embedded-manager-head h4{margin:0;font-size:15px}
    .embedded-manager-head p{margin:4px 0 0;color:var(--muted);font-size:12px;max-width:760px}
    .embedded-manager .toolbar{margin-bottom:0}
    .embedded-manager .toolbar input,.embedded-manager .toolbar select{background:#fff}
    .embedded-manager-note{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:10px 12px;border:1px solid #eadfce;border-radius:14px;background:#fbf8f3;color:var(--muted);font-size:12px}
    .embedded-manager-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
    .inline-callout{border:1px solid #eadfce;border-radius:16px;background:#fff;padding:12px 13px}
    .inline-callout h4{margin:0 0 6px;font-size:14px}
    .inline-callout p{margin:0;color:var(--muted);font-size:12px;line-height:1.45}
    .info-card{border:1px dashed #eadfce;border-radius:14px;background:#fbf8f3;padding:12px 13px;color:var(--muted);font-size:12px;line-height:1.45}
    .field-toolbar{display:flex;justify-content:flex-end;gap:8px;margin-top:6px}
    .field-toolbar .mini-btn{padding:7px 10px;border-radius:10px;border:1px solid #e6dbc9;background:#fff;color:var(--text);font-size:11px;font-weight:800;cursor:pointer}
    .field-toolbar .mini-btn:hover{background:#fbf6ee;border-color:#e2cda6;color:var(--accent)}
    .field textarea[data-editor='modal']{display:none}
    .rich-preview{min-height:84px;padding:11px 12px;border:1px solid var(--line);background:#fff;border-radius:12px;color:var(--text);cursor:pointer;white-space:pre-wrap;line-height:1.45}
    .rich-preview.is-empty{color:#8b9ab0}
    .rich-preview:hover{border-color:#e2cda6;box-shadow:0 0 0 3px rgba(142,109,61,.08)}
    .text-editor-tools{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:10px}
    .text-editor-tools .mini-btn{padding:8px 11px;border-radius:10px;border:1px solid #e6dbc9;background:#fff;color:var(--text);font-size:12px;font-weight:800;cursor:pointer}
    .text-editor-tools .mini-btn:hover{background:#fbf6ee;border-color:#e2cda6;color:var(--accent)}
    .text-editor-textarea{width:100%;min-height:320px;padding:12px;border:1px solid var(--line);border-radius:14px;resize:vertical;font:inherit;line-height:1.5}
    .text-editor-meta{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-top:10px;color:var(--muted);font-size:12px}
    
    .patternStrip{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:0 0 16px}
    .patternCard{position:relative;overflow:hidden;border:1px solid #eadfce;border-radius:18px;background:linear-gradient(180deg,#ffffff 0%,#fbf8f3 100%);padding:12px 13px;box-shadow:0 10px 22px rgba(15,23,42,.04);display:grid;gap:5px}
    .patternCard::after{content:"";position:absolute;right:-16px;top:-16px;width:74px;height:74px;border-radius:999px;background:radial-gradient(circle,rgba(180,138,79,.12),rgba(180,138,79,0));pointer-events:none}
    .patternKick{font-size:10px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;color:#8e6d3d}
    .patternValue{font-size:15px;font-weight:950;letter-spacing:-.02em;color:#2f2418}
    .patternMeta{font-size:12px;line-height:1.45;color:var(--muted)}
    @media (max-width:1100px){.patternStrip{grid-template-columns:repeat(2,minmax(0,1fr));}}
    @media (max-width:720px){.patternStrip{grid-template-columns:1fr;}}

@media (max-width:1100px){
      .hero{grid-template-columns:1fr}
      .overview-grid{grid-template-columns:1fr}
      .settings-home-list{grid-template-columns:repeat(2,minmax(0,1fr))}
      .kpis,.summary-strip{grid-template-columns:repeat(2,minmax(0,1fr))}
      .col-3,.col-4,.col-5,.col-6,.col-8,.col-12{grid-column:span 12}
    }
    @media (max-width:720px){
      .wrap{padding:12px}
      .kpis,.summary-strip{grid-template-columns:1fr}
      .settings-home-list{grid-template-columns:1fr}
      .mini-grid,.access-grid,.embedded-manager-grid{grid-template-columns:1fr}
      .section-row{align-items:stretch}
      .section-rail{flex-basis:100%}
      .tab-picker{width:100%}
      .picker-trigger{width:100%;justify-content:center}
      .quick-focus-row{flex-direction:column}
      h1{font-size:24px}
    }

    @media (max-width:1280px){.wrap{padding:0 14px 18px}.backroom-header{grid-template-columns:1fr;overflow:visible;border-radius:0 0 22px 22px}.backroom-brand,.backroom-nav,.backroom-user{min-height:auto}.backroom-nav{padding:4px 12px 10px;justify-content:flex-start}.nav-btn{height:58px}.backroom-user{justify-content:flex-start;flex-wrap:wrap;overflow:visible;min-width:0}.backroom-search{min-width:260px;max-width:none;flex:1 1 320px}.quick-apps-panel{left:0;right:auto;min-width:min(92vw,420px)}}
    @media (max-width:760px){.quick-apps-panel{min-width:min(94vw,360px)}.quick-apps-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.top-icon-group{display:none}}
  

    /* phase 16: settings system upgrade */
    html{scrollbar-color:rgba(142,109,61,.42) rgba(15,23,42,.06)}
    ::-webkit-scrollbar{width:12px;height:12px}
    ::-webkit-scrollbar-thumb{background:linear-gradient(180deg,rgba(180,138,79,.44),rgba(108,82,46,.44));border-radius:999px;border:3px solid rgba(255,255,255,.9)}
    ::-webkit-scrollbar-track{background:rgba(15,23,42,.04);border-radius:999px}
    .overview-card,.section-card,.quick-app-card,.business-pane,.panel,.viewer-card,.info-card,.overview-primary,.status-panel,.settings-command-card,.table-wrap,.embedded-manager,.access-summary-shell{transition:transform .16s ease, box-shadow .16s ease, border-color .16s ease, background .16s ease}
    .overview-card:hover,.section-card:hover,.quick-app-card:hover,.business-pane:hover,.viewer-card:hover,.info-card:hover,.status-panel:hover,.settings-command-card:hover{transform:translateY(-2px);box-shadow:0 18px 38px rgba(15,23,42,.10);border-color:rgba(180,138,79,.18)}
    .business-pane-shell,.table-wrap,.access-summary-shell,.embedded-manager{box-shadow:0 16px 34px rgba(15,23,42,.06)}
    .btn,.nav-btn,.mini-btn,.header-action-btn,.jump-btn,.business-subtab,.tab,.top-icon-btn,.quick-apps-btn,.table-col-btn{transition:transform .14s ease, box-shadow .14s ease, border-color .14s ease, background .14s ease}
    .btn:hover,.nav-btn:hover,.mini-btn:hover,.header-action-btn:hover,.jump-btn:hover,.business-subtab:hover,.tab:hover,.top-icon-btn:hover,.quick-apps-btn:hover,.table-col-btn:hover{transform:translateY(-1px);box-shadow:0 12px 24px rgba(15,23,42,.08)}
    .btn.primary,.quick-apps-btn.primary,.header-action-btn.primary{box-shadow:0 14px 28px rgba(108,82,46,.18)}
    .field input:focus,.field textarea:focus,.field select:focus,.backroom-search input:focus,.table-col-search input:focus,.text-editor-textarea:focus{outline:none;border-color:rgba(180,138,79,.34);box-shadow:0 0 0 4px rgba(180,138,79,.12),0 12px 28px rgba(108,82,46,.08)}
    .empty{padding:18px 16px;border:1px dashed rgba(180,138,79,.22);border-radius:16px;background:linear-gradient(180deg,#fffdfa 0%,#f7f1e6 100%);font-weight:800;color:#6c522e}
    .modal,.overlay{backdrop-filter:blur(4px)}
</style>
</head>
<body>
  <div class="wrap">
    <div class="backroom-header">
      <div class="backroom-brand">
        <b>DIAMOND Backroom</b>
        <span>Settings workspace · front, company & communications</span>
      </div>
      <div class="backroom-nav" id="backroomNav"></div>
      <div class="backroom-user" id="backroomUser"></div>
    </div>
    <div class="panel" id="accessGate" style="display:none;margin-bottom:16px;padding:18px 20px"></div>

    <div class="workspace-board">
      <div class="tabs" id="tabs"></div>
      <div class="tabs-fallback" id="tabsFallback">
        <button class="tab active" data-tab="settingsHome">Overview</button>
        <button class="tab" data-tab="business">Business control</button>
        <button class="tab" data-tab="fleetCategories">Fleet categories</button>
        <button class="tab" data-tab="fleetVehicles">Fleet vehicles</button>
        <button class="tab" data-tab="dynamicPricing">Dynamic pricing</button>
        <button class="tab" data-tab="insurance">Insurance plans</button>
        <button class="tab" data-tab="extras">Extra services</button>
        <button class="tab" data-tab="fees">Fee rules</button>
      </div>

    <section class="section active" data-section="settingsHome">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Overview</h2>
            <p>Συμπτυγμένο control overview για τη συνολική εικόνα της εταιρίας, το system status και τη γρήγορη μετάβαση στα βασικά sections.</p>
          </div>
          <div class="actions"></div>
        </div>

        <div class="settings-command-deck">
          <div class="settings-command-card">
            <div class="settings-command-kicker">Front &amp; catalog</div>
            <h3 class="settings-command-title">Brand, operations &amp; vehicle presentation</h3>
            <p class="settings-command-copy">Γρήγορη πρόσβαση στα σημεία που επηρεάζουν το front look, τη λογική κατηγοριών/μοντέλων και τα βασικά operational defaults.</p>
            <div class="settings-command-pills">
              <span class="pill">Front company</span>
              <span class="pill">Operations</span>
              <span class="pill">Vehicle catalog</span>
            </div>
            <div class="settings-command-actions">
              <button class="btn" data-home-go="business" data-focus-group="frontBrand">Front &amp; company</button>
              <button class="btn" data-home-go="business" data-focus-group="operations">Operations</button>
              <button class="btn" data-home-go="business" data-focus-group="vehicleCatalog">Vehicle catalog</button>
            </div>
          </div>
          <div class="settings-command-card">
            <div class="settings-command-kicker">Pricing &amp; legal</div>
            <h3 class="settings-command-title">Rules, coverages &amp; commercial policy</h3>
            <p class="settings-command-copy">Ενιαίο σημείο για δυναμική τιμολόγηση, ασφάλειες, extras, fees και όρους που επηρεάζουν προσφορά, checkout και contract.</p>
            <div class="settings-command-pills">
              <span class="pill">Dynamic pricing</span>
              <span class="pill">Insurance</span>
              <span class="pill">Fees &amp; extras</span>
            </div>
            <div class="settings-command-actions">
              <button class="btn" data-settings-section="dynamicPricing">Dynamic pricing</button>
              <button class="btn" data-settings-section="insurance">Insurance</button>
              <button class="btn" data-settings-section="fees">Fees</button>
            </div>
          </div>
          <div class="settings-command-card">
            <div class="settings-command-kicker">Team &amp; privacy</div>
            <h3 class="settings-command-title">Access control, staff roles &amp; audit visibility</h3>
            <p class="settings-command-copy">Σύνδεσε ρόλους, χρήστες, active sessions και privacy checks με το ίδιο admin language που χρησιμοποιείς σε Daily, Fleet και Bookings.</p>
            <div class="settings-command-pills">
              <span class="pill">Security</span>
              <span class="pill">Roles</span>
              <span class="pill">Users &amp; audit</span>
            </div>
            <div class="settings-command-actions">
              <button class="btn" data-settings-section="security">Security</button>
              <button class="btn" data-settings-section="roles">Roles</button>
              <button class="btn" data-settings-section="users">Users</button>
            </div>
          </div>
        </div>

        <div class="overview-grid">
          <div class="overview-card overview-primary">
            <div class="overview-card-head">
              <div>
                <div class="eyebrow">Company control center</div>
                <h3>Backroom settings &amp; front data control</h3>
                <p>Κεντρική διαχείριση εταιρίας, front catalog, τιμολόγησης, λειτουργίας κράτησης και automations από ένα σημείο.</p>
              </div>
            </div>
            <div class="summary-strip">
              <div class="summary-chip"><div class="label">Νόμισμα</div><div class="value" id="snapCurrency">—</div><div class="hint">Primary catalog currency</div></div>
              <div class="summary-chip"><div class="label">VAT</div><div class="value" id="snapVat">—</div><div class="hint">Global tax rate</div></div>
              <div class="summary-chip"><div class="label">Night window</div><div class="value" id="snapNight">—</div><div class="hint">Out-of-hours window</div></div>
              <div class="summary-chip"><div class="label">Deposit</div><div class="value" id="snapDeposit">—</div><div class="hint">Default guarantee amount</div></div>
            </div>
            <div class="micro-grid" id="kpis"></div>
            <div class="quick-actions">
              <button class="btn" id="refreshSnapshotBtn" data-allow-busy="1">Refresh catalog</button>
              <button class="btn warn" id="seedAllDefaultsBtn" data-allow-busy="1">Seed missing defaults</button>
              <button class="btn good" id="exportSnapshotBtn" data-allow-busy="1">Export JSON</button>
              <button class="btn" id="copySnapshotBtn" data-allow-busy="1">Copy JSON</button>
              <button class="btn" id="copySectionDigestBtn" data-allow-busy="1">Copy section digest</button>
              <button class="btn" id="queueSettingsSectionBtn" data-allow-busy="1">Queue section</button>
            </div>
          </div>

          <div class="overview-card overview-status">
            <div class="overview-card-head">
              <div>
                <div class="eyebrow">Status</div>
                <h3>System health &amp; access</h3>
                <p>Άμεση εικόνα για access, automations, privacy και γενική λειτουργική κατάσταση.</p>
              </div>
            </div>
            <div class="overview-status-panels">
              <div class="status-panel">
                <h4>System health</h4>
                <p>Βασικοί έλεγχοι που επηρεάζουν front, automations και access.</p>
                <div class="health-list" id="healthList"></div>
              </div>
              <div class="access-summary-shell" id="accessSummary">
                <h4>Access status</h4>
                <p>Γίνεται φόρτωση του current access snapshot...</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section" data-section="business">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Business settings</h2>
            <p>Βασικές ρυθμίσεις business και διαχείριση σταθμών/τοποθεσιών.</p>
          </div>
          <div class="actions">
            <button class="btn warn" id="resetBusinessDefaultsBtn" data-allow-busy="1">Reset defaults</button>
            <button class="btn primary" id="saveBusinessBtn">Αποθήκευση business settings</button>
          </div>
        </div>
        <div class="table-toolbar" style="margin-bottom:12px">
          <div class="table-toolbar-left">
            <span class="table-toolbar-title">Κεντρικός σταθμός ρυθμίσεων</span>
            <select id="businessStationSelect" style="min-width:260px"></select>
            <span class="table-toolbar-sub" id="businessStationMeta">Οι παρακάτω ρυθμίσεις φορτώνονται από τον επιλεγμένο σταθμό.</span>
          </div>
          <div class="table-toolbar-right">
            <button class="btn" id="openStationModalBtn" type="button">Νέος / Επεξεργασία σταθμού</button>
          </div>
        </div>
        <div class="business-shell">
          <div class="business-pane-shell">
            <div class="settings-stack">
              <div class="settings-group business-pane active" id="group-frontBrand" data-group-anchor="frontBrand" data-business-pane="frontBrand">
                <div class="settings-group-head">
                  <div>
                    <h3>1. Ταυτότητα εταιρίας &amp; front brand</h3>
                    <p>Στοιχεία εταιρίας, brand labels και κείμενα που επηρεάζουν άμεσα την προβολή του front site.</p>
                  </div>
                  <div class="settings-group-badge">Brand</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-5"><label>Τίτλος εταιρικών ρυθμίσεων</label><input id="bsTitle" placeholder="Diamond Rent A Car settings" /></div>
                  <div class="field col-3"><label>Νόμισμα</label><input id="bsCurrency" placeholder="EUR" /></div>
                  <div class="field col-2"><label>VAT %</label><input id="bsVatRate" type="number" step="0.01" /></div>
                  <div class="field col-2 switch"><input id="bsActive" type="checkbox" /><label for="bsActive">Ενεργό</label></div>

                  <div class="field col-4"><label>Εταιρική ονομασία</label><input id="bsCompanyName" placeholder="DIAMOND Rent A Car" /></div>
                  <div class="field col-4"><label>Πόλη / υποτίτλος brand</label><input id="bsCompanyCity" placeholder="Thessaloniki" /></div>
                  <div class="field col-4"><label>Tagline front</label><input id="bsCompanyTagline" placeholder="Ενοικίαση οχημάτων στη Θεσσαλονίκη με καθαρή διαδικασία..." /></div>

                  <div class="field col-4"><label>Company phone</label><input id="bsCompanyPhone" /></div>
                  <div class="field col-4"><label>Company email</label><input id="bsCompanyEmail" /></div>
                  <div class="field col-4"><label>Διεύθυνση / footer contact</label><input id="bsCompanyAddress" placeholder="Θεσσαλονίκη, Ελλάδα" /></div>

                  <div class="field col-12 compact"><label>Footer note</label><textarea id="bsFooterNote" data-editor="modal" placeholder="Επαγγελματική εμπειρία ενοικίασης οχήματος..."></textarea></div>
                </div>
              </div>

              <div class="settings-group business-pane" id="group-operations" data-group-anchor="operations" data-business-pane="operations">
                <div class="settings-group-head">
                  <div>
                    <h3>2. Operations</h3>
                    <p>Booking rules, ώρες λειτουργίας, καταθέσεις και το operational layer που συνδέει categories, fleet vehicles και daily χρήση στόλου.</p>
                  </div>
                  <div class="settings-group-badge">Operations</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-4"><label>Κείμενο ωρών λειτουργίας</label><input id="bsOperatingHoursLabel" placeholder="09:00 - 21:00" /></div>
                  <div class="field col-8"><label>Σημείωση εκτός ωραρίου</label><input id="bsAfterHoursNotice" placeholder="Για εξυπηρέτηση εκτός ωραρίου ισχύουν επιπλέον χρεώσεις." /></div>

                  <div class="field col-3"><label>Night start hour</label><input id="bsNightStartHour" type="number" min="0" max="23" step="1" /></div>
                  <div class="field col-3"><label>Night end hour</label><input id="bsNightEndHour" type="number" min="0" max="23" step="1" /></div>
                  <div class="field col-3"><label>Default deposit</label><input id="bsDefaultDeposit" type="number" step="0.01" /></div>
                  <div class="field col-3 switch"><input id="bsAllowOverbooking" type="checkbox" /><label for="bsAllowOverbooking">Allow overbooking</label></div>

                  <div class="field col-12"><label>Σημείωση</label><div class="info-card">Από εδώ ελέγχεις live booking defaults, ωράρια και τη βασική operational πολιτική του rental flow. Οι κατηγορίες και τα οχήματα στόλου διαχειρίζονται μαζί στις ενότητες Fleet categories και Fleet vehicles.</div></div>
                </div>
                <div class="embedded-manager-note">
                  <span>Vehicle structure & public catalog</span>
                  <strong>Οι κατηγορίες και τα πραγματικά οχήματα μεταφέρθηκαν στο Operations area του Settings για ενιαία διαχείριση στόλου.</strong>
                </div></div>

              <div class="settings-group business-pane" id="group-locations" data-group-anchor="locations" data-business-pane="locations">
                <div class="settings-group-head">
                  <div>
                    <h3>3. Stations / Locations</h3>
                  </div>
                  <div class="settings-group-badge">Stations &amp; Locations</div>
                </div>
                <div class="business-subtabs" id="stationsLocationsTabs"></div>
                <div class="business-subtab-note" id="stationsLocationsNote" style="display:none"></div>
                <div class="table-toolbar" style="margin-bottom:12px">
                  <div class="table-toolbar-left">
                    <input id="searchStationProfiles" placeholder="Αναζήτηση station key / label / city" style="min-width:280px" />
                  </div>
                  <div class="table-toolbar-right">
                    <span class="badge" id="stationProfilesCount">0 stations</span>
                  </div>
                </div>

                <div class="embedded-manager" id="stationsWorkspacePane" style="margin-bottom:12px">
                  <div class="embedded-manager-head">
                    <div></div>
                    <div class="actions">
                      <button class="btn primary" id="addStationProfileBtn" type="button">Νέος σταθμός</button>
                    </div>
                  </div>
                  <div id="stationProfilesTableWrap"></div>
                </div>

                <div class="embedded-manager" id="locationsWorkspacePane" style="display:none">
                  <div class="embedded-manager-head">
                    <div>
                      <h4>Pickup / Dropoff locations</h4>
                      <p>Διαχειρίζεσαι από εδώ τα σημεία παραλαβής και επιστροφής που εμφανίζονται στα front dropdowns.</p>
                    </div>
                    <div class="actions">
                      <button class="btn primary" data-add-kind="pickupLocation">Νέα τοποθεσία</button>
                    </div>
                  </div>
                  <div class="toolbar">
                    <div class="left" style="gap:10px">
                      <select id="locationStationScope" style="min-width:240px"></select>
                      <input id="searchPickupLocations" placeholder="Αναζήτηση station / location / address" />
                    </div>
                    <div class="right"><span class="badge" id="pickupLocationsCount">0 items</span></div>
                  </div>
                  <div id="pickupLocationsTableWrap"></div>
                </div>
              </div>

              <div class="settings-group business-pane" id="group-vehicleCatalog" data-group-anchor="vehicleCatalog" data-business-pane="vehicleCatalog">
                <div class="settings-group-head">
                  <div>
                    <h3>4. Vehicle catalog preview</h3>
                    <p>Ρυθμίσεις για τον τρόπο προβολής της 2ης σελίδας και οπτικό preview του τι θα βλέπει ο πελάτης ανάλογα με το επιλεγμένο mode και source.</p>
                  </div>
                  <div class="settings-group-badge">Catalog</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-4"><label>Vehicles page display</label><select id="bsVehiclesPageDisplayMode"><option value="categories">Κατηγορίες</option><option value="models">Μοντέλα</option></select></div>
                  <div class="field col-4"><label>Models source</label><select id="bsVehiclesPageModelsSource"><option value="fleet">Fleet (grouped models)</option><option value="vehicles">Vehicles collection</option></select></div>
                  <div class="field col-4"><label>Σημείωση</label><div class="info-card">Η πραγματική επεξεργασία vehicle categories γίνεται πλέον στο Operations. Εδώ κρατάς μόνο τον τρόπο προβολής και ένα ζωντανό preview της δημόσιας σελίδας.</div></div>
                </div>
                <div class="embedded-manager">
                  <div class="embedded-manager-grid">
                    <div class="inline-callout">
                      <h4 id="vehicleCatalogPreviewTitle">Preview · Κατηγορίες</h4>
                      <p id="vehicleCatalogPreviewCopy">Ο πελάτης βλέπει τις ενεργές κατηγορίες όπως έχουν οριστεί στο operations section.</p>
                    </div>
                    <div class="inline-callout">
                      <h4 id="vehicleCatalogPreviewSource">Source · Vehicles collection</h4>
                      <p id="vehicleCatalogPreviewSourceCopy">Το source καθορίζει από πού αντλείται το οπτικό listing όταν η σελίδα δείχνει μοντέλα.</p>
                    </div>
                  </div>
                  <div class="embedded-manager-note">
                    <span>Οπτικό preview front listing</span>
                    <strong id="vehicleCatalogPreviewMeta">Categories · Vehicles</strong>
                  </div>
                  <div class="table-wrap">
                    <div id="vehicleCatalogPreviewList" style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;padding:14px"></div>
                  </div>
                </div>
              </div>

              <div class="settings-group business-pane" id="group-automation" data-group-anchor="automation" data-business-pane="automation">
                <div class="settings-group-head">
                  <div>
                    <h3>5. Triggered emails &amp; automation</h3>
                    <p>Ενεργοποίηση / απενεργοποίηση των Wix triggered emails και ορισμός των ids για booking confirmation και cancellation.</p>
                  </div>
                  <div class="settings-group-badge">Automation</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-4 switch"><input id="bsEnableTriggeredEmails" type="checkbox" /><label for="bsEnableTriggeredEmails">Enable triggered emails</label></div>
                  <div class="field col-4"><label>Triggered email · confirmed</label><input id="bsBookingConfirmedTriggerId" placeholder="Wix trigger id για booking confirmation" /></div>
                  <div class="field col-4"><label>Triggered email · canceled</label><input id="bsBookingCanceledTriggerId" placeholder="Wix trigger id για booking cancellation" /></div>
                </div>
              </div>

              <div class="settings-group business-pane" id="group-rentalTerms" data-group-anchor="rentalTerms" data-business-pane="rentalTerms">
                <div class="settings-group-head">
                  <div>
                    <h3>6. Rental terms page</h3>
                    <p>Το περιεχόμενο της ξεχωριστής σελίδας rental terms / πολιτικών. Όλα τα κείμενα εδώ τροφοδοτούν τη public σελίδα όρων.</p>
                  </div>
                  <div class="settings-group-badge">Terms</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-6"><label>Σελίδα: Όροι ενοικίασης · τίτλος</label><input id="bsRentalTermsTitle" placeholder="Όροι ενοικίασης και πολιτικές" /></div>
                  <div class="field col-6"><label>Σελίδα: Όροι ενοικίασης · intro</label><textarea id="bsRentalTermsIntro" data-editor="modal" placeholder="Σύντομη εισαγωγή για τη σελίδα όρων ενοικίασης."></textarea></div>
                  <div class="field col-6"><label>Όροι ενοικίασης · βασικό κείμενο</label><textarea id="bsRentalTermsBody" data-editor="modal" placeholder="Γενικοί όροι ενοικίασης, επιβεβαίωση κράτησης, αντικατάσταση οχήματος κτλ."></textarea></div>
                  <div class="field col-6"><label>Προϋποθέσεις οδηγού / εγγύηση</label><textarea id="bsRentalRequirementsBody" data-editor="modal" placeholder="Άδεια οδήγησης, ηλικία, έτη κατοχής διπλώματος, εγγύηση, δικαιολογητικά."></textarea></div>
                  <div class="field col-6"><label>Πολιτικές χρήσης / ακυρώσεων / καυσίμων</label><textarea id="bsRentalPoliciesBody" data-editor="modal" placeholder="Καύσιμα, εκτός ωραρίου, ακυρώσεις, καθυστερήσεις, παράταση μίσθωσης, ζημιές."></textarea></div>
                  <div class="field col-6"><label>Προσωπικά δεδομένα / λοιποί όροι</label><textarea id="bsRentalPrivacyBody" data-editor="modal" placeholder="Στοιχεία επικοινωνίας, επεξεργασία δεδομένων, λοιπές ενημερώσεις."></textarea></div>
                </div>
              </div>

              <div class="settings-group business-pane" id="group-insuranceTexts" data-group-anchor="insuranceTexts" data-business-pane="insuranceTexts">
                <div class="settings-group-head">
                  <div>
                    <h3>7. Insurance texts &amp; legal content</h3>
                    <p>Τα κείμενα που τροφοδοτούν τις σελίδες και τα sections ασφαλειών, καλύψεων και σχετικών ενημερώσεων.</p>
                  </div>
                  <div class="settings-group-badge">Insurance</div>
                </div>
                <div class="settings-grid">
                  <div class="field col-6"><label>Σελίδα: Όροι και λεπτομέρειες ασφαλειών · τίτλος</label><input id="bsInsuranceTermsTitle" placeholder="Όροι και λεπτομέρειες ασφαλειών" /></div>
                  <div class="field col-6"><label>Σελίδα: Λεπτομέρειες ασφαλειών · τίτλος</label><input id="bsInsuranceDetailsTitle" placeholder="Λεπτομέρειες ασφαλιστικών επιλογών" /></div>
                  <div class="field col-6"><label>Insurance terms intro</label><textarea id="bsInsuranceTermsIntro" data-editor="modal" placeholder="Σύντομη εισαγωγή για τους όρους και τις καλύψεις."></textarea></div>
                  <div class="field col-6"><label>Insurance details intro</label><textarea id="bsInsuranceDetailsIntro" data-editor="modal" placeholder="Σύντομη εισαγωγή για τα διαθέσιμα πακέτα."></textarea></div>
                  <div class="field col-6"><label>Insurance terms content</label><textarea id="bsInsuranceTermsBody" data-editor="modal" placeholder="Αναλυτικοί όροι, βασικές εξαιρέσεις, διαδικασία επιβεβαίωσης."></textarea></div>
                  <div class="field col-6"><label>Insurance details content</label><textarea id="bsInsuranceDetailsBody" data-editor="modal" placeholder="Πώς διαβάζουμε τις καλύψεις και πότε επιβεβαιώνονται."></textarea></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>


<section class="section" data-section="fleetCategories">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Fleet categories</h2>
            <p>Κατηγορίες οχημάτων που τροφοδοτούν το public catalog, τα vehicle pages και τη σύνδεση των πραγματικών οχημάτων με τη δημόσια εμπειρία κράτησης.</p>
          </div>
          <div class="actions">
            <button class="btn primary" data-add-kind="vehicleCategory">Νέα κατηγορία</button>
          </div>
        </div>
        <div class="embedded-manager-grid" style="margin-bottom:12px">
          <div class="inline-callout">
            <h4>Public vehicle layer</h4>
            <p>Εδώ ρυθμίζεις τίτλο, περιγραφή, βασικά specs, εικόνα και τιμή της κάθε κατηγορίας που βλέπει ο πελάτης μπροστά.</p>
          </div>
          <div class="inline-callout">
            <h4>Operational mapping</h4>
            <p>Τα Fleet vehicles παίρνουν category code από εδώ. Άρα categories και vehicles παραμένουν μαζί στο Operations area για πιο σωστή διαχείριση στόλου.</p>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchVehicleCategories" placeholder="Αναζήτηση κατηγορίας / τίτλου" /></div>
          <div class="right"><span class="badge" id="vehicleCategoriesCount">0 items</span></div>
        </div>
        <div id="vehicleCategoriesTableWrap"></div>
      </div>
    </section>

<section class="section" data-section="fleetVehicles">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Fleet vehicles</h2>
            <p>Πραγματικά οχήματα στόλου για plate, model, fleet category και operational status. Συνδέονται με τις Fleet categories του Operations area.</p>
          </div>
          <div class="actions">
            <button class="btn primary" data-add-kind="fleetVehicle">Νέο όχημα στόλου</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchFleetVehicles" placeholder="Αναζήτηση πινακίδας / μοντέλου / category" /></div>
          <div class="right"><span class="badge" id="fleetVehiclesCount">0 items</span></div>
        </div>
        <div id="fleetVehiclesTableWrap"></div>
      </div>
    </section>


    <section class="section" data-section="dynamicPricing">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Dynamic pricing</h2>
            <p>Χτίσε seasons και rate rules ανά category. Υποστηρίζει πλήρως custom min/max ημέρες και προαιρετικά έτοιμα presets.</p>
          </div>
          <div class="actions">
            <button class="btn primary" data-add-kind="pricingSeason">Νέα season</button>
            <button class="btn good" data-add-kind="categoryRateRule">Νέος rate rule</button>
          </div>
        </div>
        <div class="viewer-stack">
          <div class="viewer-card">
            <div class="toolbar">
              <div class="left"><input id="searchPricingSeasons" placeholder="Αναζήτηση season" /></div>
              <div class="right"><span class="badge" id="pricingSeasonsCount">0 items</span></div>
            </div>
            <div id="pricingSeasonsTableWrap"></div>
          </div>
          <div class="viewer-card">
            <div class="toolbar">
              <div class="left">
                <input id="searchCategoryRateRules" placeholder="Αναζήτηση rate rule / category / season" />
                <select id="filterRateRuleCategory"><option value="">Όλες οι κατηγορίες</option></select>
                <select id="filterRateRuleSeason"><option value="">Όλες οι seasons</option></select>
              </div>
              <div class="right"><span class="badge" id="categoryRateRulesCount">0 items</span></div>
            </div>
            <div id="categoryRateRulesTableWrap"></div>
          </div>
        </div>
        <div class="panel" style="margin-top:14px;background:#fbfdff">
          <div class="section-head" style="padding-bottom:8px">
            <div>
              <h2 style="font-size:18px">Pricing simulator</h2>
              <p>Δες ποια season και ποιος rate rule θα εφαρμοστεί για συγκεκριμένη κατηγορία, ημέρες και ημερομηνία.</p>
            </div>
          </div>
          <div class="grid">
            <div class="field col-4"><label>Κατηγορία</label><select id="simCategoryCode"><option value="">Επίλεξε κατηγορία</option></select></div>
            <div class="field col-4"><label>Pickup date</label><input id="simPickupDate" type="date" /></div>
            <div class="field col-2"><label>Ημέρες</label><input id="simDays" type="number" min="1" step="1" value="3" /></div>
            <div class="field col-2"><label>Fallback price</label><input id="simBasePrice" type="number" step="0.01" value="0" /></div>
          </div>
          <div id="pricingSimulatorWrap"></div>
        </div>
      </div>
    </section>

    <section class="section" data-section="insurance">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Insurance plans</h2>
            <p>Ασφαλιστικά πλάνα που εμφανίζονται στο public flow και στο contract.</p>
          </div>
          <div class="actions">
            <button class="btn" data-seed-target="insurance" data-allow-busy="1">Seed defaults</button>
            <button class="btn warn" data-reset-target="insurance" data-allow-busy="1">Reset defaults</button>
            <button class="btn primary" data-add-kind="insurance">Νέο insurance plan</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchInsurance" placeholder="Αναζήτηση ασφάλειας" /></div>
          <div class="right"><span class="badge" id="insuranceCount">0 items</span></div>
        </div>
        <div id="insuranceTableWrap"></div>
      </div>
    </section>

    <section class="section" data-section="extras">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Extra services</h2>
            <p>Προαιρετικές παροχές όπως καθίσματα, GPS, δεύτερος οδηγός κ.λπ.</p>
          </div>
          <div class="actions">
            <button class="btn" data-seed-target="extras" data-allow-busy="1">Seed defaults</button>
            <button class="btn warn" data-reset-target="extras" data-allow-busy="1">Reset defaults</button>
            <button class="btn primary" data-add-kind="extra">Νέο extra service</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchExtras" placeholder="Αναζήτηση extra" /></div>
          <div class="right"><span class="badge" id="extrasCount">0 items</span></div>
        </div>
        <div id="extrasTableWrap"></div>
      </div>
    </section>

    <section class="section" data-section="fees">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Fee rules</h2>
            <p>Αυτόματες ή manual χρεώσεις: ηλικιακά fees, νυχτερινές χρεώσεις, διαχειριστικά κ.λπ.</p>
          </div>
          <div class="actions">
            <button class="btn" data-seed-target="fees" data-allow-busy="1">Seed defaults</button>
            <button class="btn warn" data-reset-target="fees" data-allow-busy="1">Reset defaults</button>
            <button class="btn primary" data-add-kind="fee">Νέο fee rule</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left">
            <input id="searchFees" placeholder="Αναζήτηση fee" />
            <select id="filterRuleType">
              <option value="">Όλα τα rule types</option>
              <option value="manual">manual</option>
              <option value="ageRange">ageRange</option>
              <option value="nightPickup">nightPickup</option>
              <option value="nightDropoff">nightDropoff</option>
              <option value="night">night</option>
            </select>
          </div>
          <div class="right"><span class="badge" id="feesCount">0 items</span></div>
        </div>
        <div id="feesTableWrap"></div>
      </div>
    </section>

    <section class="section" data-section="security">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Security &amp; sessions</h2>
            <p>Password resets, προσωρινά passwords, active sessions και force logout ανά χρήστη.</p>
          </div>
          <div class="actions">
            <button class="btn" id="refreshSecurityBtn" data-allow-busy="1">Refresh security</button>
          </div>
        </div>
        <div class="mini-grid" id="securityCards" style="margin-bottom:14px"></div>
        <div class="grid" style="margin-bottom:14px">
          <div class="field col-4"><label>Selected staff email</label><select id="securityUserSelect"></select></div>
          <div class="field col-4"><label>New password</label><input id="securityPassword" type="password" placeholder="Νέο password" /></div>
          <div class="field col-4"><label>Confirm password</label><input id="securityPasswordConfirm" type="password" placeholder="Επανάληψη password" /></div>
          <div class="field col-3 switch"><input id="securityMustChange" type="checkbox" checked /><label for="securityMustChange">Require change on next login</label></div>
          <div class="field col-9"><label>Result / temporary password</label><textarea id="securityResult" readonly>Επίλεξε χρήστη και όρισε ή κάνε reset στο password.</textarea></div>
        </div>
        <div class="actions" style="margin-bottom:14px">
          <button class="btn good" id="setPasswordBtn">Set password</button>
          <button class="btn warn" id="resetPasswordBtn">Generate temporary password</button>
          <button class="btn danger" id="revokeUserSessionsBtn">Revoke all sessions for selected user</button>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchSessions" placeholder="Αναζήτηση session / email / browser" /></div>
          <div class="right"><span class="badge" id="sessionsCount">0 sessions</span></div>
        </div>
        <div id="sessionsTableWrap"></div>
        <div class="footnote">Τα temporary passwords εμφανίζονται μόνο εδώ τη στιγμή που δημιουργούνται. Αν ο χρήστης χάσει το code, κάνε νέο reset.</div>
      </div>
    </section>

    <section class="section" data-section="roles">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Roles &amp; permissions</h2>
            <p>Πίνακας ρόλων τύπου Wheels με δικαιώματα View / Insert / Edit / Delete και Special permissions.</p>
          </div>
          <div class="actions">
            <button class="btn primary" data-add-kind="role">Νέος ρόλος</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchRoles" placeholder="Αναζήτηση ρόλου" /></div>
          <div class="right"><span class="badge" id="rolesCount">0 roles</span></div>
        </div>
        <div id="rolesTableWrap"></div>
        <div class="footnote">Tip: Οι ρόλοι αντιγράφονται γρήγορα με Duplicate, ώστε να δουλεύεις σαν το “Copy from” του Wheels.</div>
      </div>
    </section>

    <section class="section" data-section="users">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Users / Team access</h2>
            <p>Ξεχωριστοί λογαριασμοί ανά άτομο, με deactivate αντί για μόνιμο delete.</p>
          </div>
          <div class="actions">
            <button class="btn" id="openSecurityFromUsersBtn">Security console</button><button class="btn primary" data-add-kind="user">Νέος staff user</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left"><input id="searchUsers" placeholder="Αναζήτηση χρήστη" /></div>
          <div class="right"><span class="badge" id="usersCount">0 users</span></div>
        </div>
        <div id="usersTableWrap"></div>
        <div class="footnote">Σημείωση: για λόγους audit trail οι χρήστες απενεργοποιούνται αντί να διαγράφονται. Από το Invite / Re-invite εκδίδεις προσωρινό onboarding password χωρίς να πειράζεις χειροκίνητα collections.</div>
      </div>
    </section>

    <section class="section" data-section="audit">
      <div class="panel section-card">
        <div class="section-head">
          <div>
            <h2>Change history</h2>
            <p>Audit trail για users / roles / pricing changes, εμπνευσμένο από το change history του Wheels.</p>
          </div>
          <div class="actions">
            <button class="btn" id="refreshAuditBtn" data-allow-busy="1">Refresh audit</button>
          </div>
        </div>
        <div class="toolbar">
          <div class="left">
            <input id="searchAudit" placeholder="Αναζήτηση actor / action / summary" />
            <select id="filterAuditType">
              <option value="">Όλα τα entities</option>
              <option value="accessRecovery">Access recovery</option>
              <option value="staffUser">Users</option>
              <option value="staffRole">Roles</option>
              <option value="pricing">Pricing</option>
            </select>
          </div>
          <div class="right"><span class="badge" id="auditCount">0 entries</span></div>
        </div>
        <div id="auditTableWrap"></div>
        <div class="footnote">Προτεινόμενη διατήρηση: 1 έτος για bookings, rentals, users και roles history.</div>
      </div>
    </section>

    </div>
    <div class="statusbar"><div class="toast" id="toast"></div></div>
  </div>

  <div class="overlay" id="editorOverlay">
    <div class="modal">
      <div class="modal-head">
        <div>
          <h3 id="editorTitle">Edit item</h3>
          <p id="editorSub">Συμπλήρωσε τα πεδία και αποθήκευσε.</p>
        </div>
        <button class="btn" id="closeEditorBtn">Κλείσιμο</button>
      </div>
      <div class="modal-body">
        <div class="grid" id="editorGrid"></div>
      </div>
      <div class="modal-foot">
        <div class="actions">
          <button class="btn danger" id="deleteEditorBtn" style="display:none">Διαγραφή</button>
        </div>
        <div class="actions">
          <button class="btn" id="cancelEditorBtn">Άκυρο</button>
          <button class="btn good" id="saveEditorBtn">Αποθήκευση</button>
        </div>
      </div>
    </div>
  </div>

  <div class="overlay" id="stationProfileOverlay">
    <div class="modal" style="max-width:760px">
      <div class="modal-head">
        <div>
          <h3 id="stationProfileTitle">Σταθμός</h3>
          <p id="stationProfileSub">Δημιουργία ή επεξεργασία station profile.</p>
        </div>
        <button class="btn" id="closeStationProfileBtn">Κλείσιμο</button>
      </div>
      <div class="modal-body">
        <div class="grid">
          <div class="field col-4"><label>Station key</label><input id="stationProfileKey" placeholder="airport" /></div>
          <div class="field col-5"><label>Station label</label><input id="stationProfileLabel" placeholder="Αεροδρόμιο" /></div>
          <div class="field col-3"><label>Sort order</label><input id="stationProfileSortOrder" type="number" step="1" value="9999" /></div>
          <div class="field col-4"><label>City</label><input id="stationProfileCity" placeholder="Thessaloniki" /></div>
          <div class="field col-8"><label>Address</label><input id="stationProfileAddress" placeholder="Address / σημείο σταθμού" /></div>
          <div class="field col-12"><label>Description</label><textarea id="stationProfileDescription" placeholder="Προαιρετική περιγραφή σταθμού"></textarea></div>
          <div class="field col-3 switch"><input id="stationProfileActive" type="checkbox" checked /><label for="stationProfileActive">Active</label></div>
        </div>
      </div>
      <div class="modal-foot">
        <div class="actions">
          <button class="btn danger" id="deleteStationProfileModalBtn">Διαγραφή</button>
        </div>
        <div class="actions">
          <button class="btn" id="cancelStationProfileBtn">Άκυρο</button>
          <button class="btn good" id="saveStationProfileBtn">Αποθήκευση</button>
        </div>
      </div>
    </div>
  </div>

  <div class="overlay" id="inspectorOverlay">
    <div class="modal" style="max-width:1100px">
      <div class="modal-head">
        <div>
          <h3 id="inspectorTitle">Inspector</h3>
          <p id="inspectorSub">Λεπτομέρειες, permissions και audit payload.</p>
        </div>
        <button class="btn" id="closeInspectorBtn">Κλείσιμο</button>
      </div>
      <div class="modal-body" id="inspectorBody"></div>
      <div class="modal-foot">
        <div class="actions" id="inspectorActions"></div>
        <div class="actions">
          <button class="btn" id="inspectorDoneBtn">Κλείσιμο</button>
        </div>
      </div>
    </div>
  </div>


  <div class="overlay" id="textEditorOverlay">
    <div class="modal" style="max-width:980px">
      <div class="modal-head">
        <div>
          <h3 id="textEditorTitle">Text editor</h3>
          <p id="textEditorSub">Επεξεργασία μεγάλου κειμένου σε άνετο modal χωρίς να μακραίνει το settings form.</p>
        </div>
        <button class="btn" id="textEditorCloseBtn">Κλείσιμο</button>
      </div>
      <div class="modal-body">
        <div class="text-editor-tools">
          <button class="mini-btn" type="button" data-text-tool="paragraph">Νέα παράγραφος</button>
          <button class="mini-btn" type="button" data-text-tool="bullet">• Bullet</button>
          <button class="mini-btn" type="button" data-text-tool="dash">— Divider</button>
          <button class="mini-btn" type="button" data-text-tool="uppercase">ΚΕΦΑΛΙΔΑ</button>
        </div>
        <textarea class="text-editor-textarea" id="textEditorInput"></textarea>
        <div class="text-editor-meta"><span id="textEditorHint">Το περιεχόμενο αποθηκεύεται ως απλό κείμενο για να παραμένει ασφαλές για το front rendering.</span><span id="textEditorCount">0 χαρακτήρες</span></div>
      </div>
      <div class="modal-foot">
        <div></div>
        <div class="actions">
          <button class="btn" id="textEditorCancelBtn">Άκυρο</button>
          <button class="btn good" id="textEditorSaveBtn">Ενημέρωση πεδίου</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    const TABLE_LAYOUT_STORAGE_KEY = 'diamondPricingAdminTableLayoutsV5';
    const SETTINGS_WORKSPACE_KEY = 'diamond.settings.workspace.v1';
    const RESUME_WORKSPACE_KEY = 'diamond.backroom.resume.v1';
const WORKSPACE_HISTORY_KEY = 'diamond.backroom.history.v1';
    const PINNED_WORKSPACES_KEY = 'diamond.backroom.pins.v1';
    const DAILY_WORKSPACE_KEY = 'diamond.daily.workspace.v1';
    const FLEET_WORKSPACE_KEY = 'diamond.fleet.workspace.v1';
    const BOOKINGS_WORKSPACE_KEY = 'diamond.bookings.workspace.v1';
    const HANDOFF_QUEUE_KEY = 'diamond.backroom.handoffQueue.v1';
    const SAVED_VIEWS_KEY = 'diamond.backroom.savedViews.v1';
    try { localStorage.removeItem(SETTINGS_SNAPSHOT_KEY); localStorage.removeItem(SAVED_VIEWS_KEY); localStorage.removeItem(PINNED_WORKSPACES_KEY); } catch(_){}
    function loadTableLayouts(){
      try{ return JSON.parse(localStorage.getItem(TABLE_LAYOUT_STORAGE_KEY) || '{}') || {}; }
      catch(_){ return {}; }
    }
    function persistTableLayouts(){
      try{ localStorage.setItem(TABLE_LAYOUT_STORAGE_KEY, JSON.stringify(state.tableLayouts || {})); }
      catch(_){ }
    }
    function loadSettingsWorkspace(){
      try{ return JSON.parse(localStorage.getItem(SETTINGS_WORKSPACE_KEY) || 'null') || null; }
      catch(_){ return null; }
    }
    const savedSettingsWorkspace = loadSettingsWorkspace();

    const state = {
      snapshot: null,
      accessSnapshot: null,
      activeSection: savedSettingsWorkspace?.activeSection || 'settingsHome',
      editor: { open: false, kind: '', item: null },
      inspector: { open:false, kind:'', item:null },
      busy: false,
      filters: { vehicleCategories: '', fleetVehicles: '', pricingSeasons: '', categoryRateRules: '', rateRuleCategory: '', rateRuleSeason: '', stationProfiles: '', pickupLocations: '', insurance: '', extras: '', fees: '', ruleType: '', roles: '', users: '', sessions: '', audit: '', auditType: '' },
      securityEmail: '',
      securityResult: '',
      activeBusinessPane: savedSettingsWorkspace?.activeBusinessPane || 'frontBrand',
      activeBusinessArea: savedSettingsWorkspace?.activeBusinessArea || 'front',
      textEditor: { open:false, sourceId:'', title:'', hint:'' },
      tableLayouts: loadTableLayouts(),
      stationProfiles: [],
      locationStationScope: 'all',
      locationsWorkspaceTab: 'stations',
      activeBusinessStationKey: 'default',
      stationModal: { open:false, mode:'create', originalKey:'' }
    };

    const TAB_DEF = [
      { key:'settingsHome', label:'Overview', group:'Overview' },
      { key:'business', label:'Business control', group:'Front & company' },
      { key:'fleetCategories', label:'Fleet categories', group:'Operations' },
      { key:'fleetVehicles', label:'Fleet vehicles', group:'Operations' },
      { key:'dynamicPricing', label:'Dynamic pricing', group:'Pricing rules' },
      { key:'insurance', label:'Insurance plans', group:'Pricing rules' },
      { key:'extras', label:'Extra services', group:'Pricing rules' },
      { key:'fees', label:'Fee rules', group:'Pricing rules' },
      { key:'roles', label:'Roles', group:'Team & access' },
      { key:'users', label:'Users', group:'Team & access' },
      { key:'security', label:'Security', group:'Team & access' },
      { key:'audit', label:'Change history', group:'Team & access' }
    ];


    const BUSINESS_AREAS = [
      { key:'front', label:'Front' },
      { key:'company', label:'Company' },
      { key:'communications', label:'Communications' }
    ];

    const BUSINESS_PANES = [
      { key:'frontBrand', area:'front', label:'1. Ταυτότητα εταιρίας & front brand', shortLabel:'Front brand', helper:'Brand, company identity, footer and front-facing contact info.' },
      { key:'operations', area:'front', label:'2. Operations', shortLabel:'Operations', helper:'Booking rules, operating hours, deposits and out-of-hours behavior.' },
      { key:'vehicleCatalog', area:'front', label:'3. Vehicle catalog', shortLabel:'Vehicle catalog', helper:'Vehicles page mode and a visual preview of what the customer sees on the vehicles page.' },
      { key:'rentalTerms', area:'front', label:'4. Rental terms', shortLabel:'Rental terms', helper:'Public rental terms page content and large legal copy.' },
      { key:'insuranceTexts', area:'front', label:'5. Insurance & legal', shortLabel:'Insurance & legal', helper:'Insurance texts, intros and legal helper content.' },
      { key:'locations', area:'company', label:'1. Stations / Locations', shortLabel:'Stations / Locations', helper:'Pickup and dropoff locations, ordering, fees and activation state.' },
      { key:'automation', area:'communications', label:'1. Triggered emails', shortLabel:'Triggered emails', helper:'Automation enablement and trigger ids.' }
    ];
    const initialBusinessPane = BUSINESS_PANES.find((pane) => pane.key === state.activeBusinessPane);
    if(initialBusinessPane && state.activeBusinessArea !== initialBusinessPane.area){
      state.activeBusinessArea = initialBusinessPane.area;
    }

    const ROLE_MODULES = ['bookings','rentals','customers','pricing','fleet','reports','financials'];
    const ROLE_ACTIONS = ['view','insert','edit','delete'];
    const SPECIAL_PERMISSIONS = ['cancelBooking','cancelRental','voidInvoice','mergeCorporateCustomers','mergeIndividualRenters','mergeAgents','mergeSuppliers','manageStaff','manageBusinessSettings','overridePricing','approveManualOverride','viewChangeHistory'];
    const SPECIAL_PERMISSION_LABELS = {
      cancelBooking:'Cancel a booking',
      cancelRental:'Cancel a rental',
      voidInvoice:'Void invoice',
      mergeCorporateCustomers:'Merge corporate customers',
      mergeIndividualRenters:'Merge individual renters',
      mergeAgents:'Merge agents',
      mergeSuppliers:'Merge suppliers',
      manageStaff:'Manage staff',
      manageBusinessSettings:'Manage business settings',
      overridePricing:'Override pricing',
      approveManualOverride:'Approve manual override',
      viewChangeHistory:'View change history'
    };

    const DAY_BRACKET_PRESETS = [
      { key:'custom', label:'Custom range', minDays:'', maxDays:'' },
      { key:'1_3', label:'1-3 ημέρες', minDays:1, maxDays:3 },
      { key:'4_7', label:'4-7 ημέρες', minDays:4, maxDays:7 },
      { key:'8_14', label:'8-14 ημέρες', minDays:8, maxDays:14 },
      { key:'15_21', label:'15-21 ημέρες', minDays:15, maxDays:21 },
      { key:'22_30', label:'22-30 ημέρες', minDays:22, maxDays:30 },
      { key:'30_plus', label:'30+ ημέρες', minDays:30, maxDays:0 }
    ];

    const els = {
      backroomNav: document.getElementById('backroomNav'),
      backroomUser: document.getElementById('backroomUser'),
      tabs: document.getElementById('tabs'),
      tabsFallback: document.getElementById('tabsFallback'),
      sections: Array.from(document.querySelectorAll('.section')),
      accessGate: document.getElementById('accessGate'),
      accessSummary: document.getElementById('accessSummary'),
      settingsHomeRefreshBtn: document.getElementById('settingsHomeRefreshBtn'),
      kpis: document.getElementById('kpis'),
      snapCurrency: document.getElementById('snapCurrency'),
      snapVat: document.getElementById('snapVat'),
      snapNight: document.getElementById('snapNight'),
      snapDeposit: document.getElementById('snapDeposit'),
      saveBusinessBtn: document.getElementById('saveBusinessBtn'),
      businessStationSelect: document.getElementById('businessStationSelect'),
      businessStationMeta: document.getElementById('businessStationMeta'),
      openStationModalBtn: document.getElementById('openStationModalBtn'),
      businessPaneTabs: document.getElementById('businessPaneTabs'),
      businessPaneNote: document.getElementById('businessPaneNote'),
      stationsLocationsTabs: document.getElementById('stationsLocationsTabs'),
      stationsLocationsNote: document.getElementById('stationsLocationsNote'),
      stationsWorkspacePane: document.getElementById('stationsWorkspacePane'),
      locationsWorkspacePane: document.getElementById('locationsWorkspacePane'),
      textEditorOverlay: document.getElementById('textEditorOverlay'),
      textEditorTitle: document.getElementById('textEditorTitle'),
      textEditorSub: document.getElementById('textEditorSub'),
      textEditorInput: document.getElementById('textEditorInput'),
      textEditorHint: document.getElementById('textEditorHint'),
      textEditorCount: document.getElementById('textEditorCount'),
      toast: document.getElementById('toast'),
      stationProfilesWrap: document.getElementById('stationProfilesTableWrap'),
      vehicleCategoriesWrap: document.getElementById('vehicleCategoriesTableWrap'),
      fleetVehiclesWrap: document.getElementById('fleetVehiclesTableWrap'),
      pickupLocationsWrap: document.getElementById('pickupLocationsTableWrap'),
      pricingSeasonsWrap: document.getElementById('pricingSeasonsTableWrap'),
      categoryRateRulesWrap: document.getElementById('categoryRateRulesTableWrap'),
      pricingSimulatorWrap: document.getElementById('pricingSimulatorWrap'),
      insuranceWrap: document.getElementById('insuranceTableWrap'),
      extrasWrap: document.getElementById('extrasTableWrap'),
      feesWrap: document.getElementById('feesTableWrap'),
      securityCards: document.getElementById('securityCards'),
      securityUserSelect: document.getElementById('securityUserSelect'),
      securityPassword: document.getElementById('securityPassword'),
      securityPasswordConfirm: document.getElementById('securityPasswordConfirm'),
      securityMustChange: document.getElementById('securityMustChange'),
      securityResult: document.getElementById('securityResult'),
      refreshSecurityBtn: document.getElementById('refreshSecurityBtn'),
      revokeUserSessionsBtn: document.getElementById('revokeUserSessionsBtn'),
      setPasswordBtn: document.getElementById('setPasswordBtn'),
      resetPasswordBtn: document.getElementById('resetPasswordBtn'),
      sessionsWrap: document.getElementById('sessionsTableWrap'),
      rolesWrap: document.getElementById('rolesTableWrap'),
      usersWrap: document.getElementById('usersTableWrap'),
      auditWrap: document.getElementById('auditTableWrap'),
      searchAudit: document.getElementById('searchAudit'),
      filterAuditType: document.getElementById('filterAuditType'),
      vehicleCategoriesCount: document.getElementById('vehicleCategoriesCount'),
      fleetVehiclesCount: document.getElementById('fleetVehiclesCount'),
      pickupLocationsCount: document.getElementById('pickupLocationsCount'),
      pricingSeasonsCount: document.getElementById('pricingSeasonsCount'),
      categoryRateRulesCount: document.getElementById('categoryRateRulesCount'),
      insuranceCount: document.getElementById('insuranceCount'),
      extrasCount: document.getElementById('extrasCount'),
      feesCount: document.getElementById('feesCount'),
      sessionsCount: document.getElementById('sessionsCount'),
      rolesCount: document.getElementById('rolesCount'),
      usersCount: document.getElementById('usersCount'),
      auditCount: document.getElementById('auditCount'),
      searchVehicleCategories: document.getElementById('searchVehicleCategories'),
      searchFleetVehicles: document.getElementById('searchFleetVehicles'),
      searchStationProfiles: document.getElementById('searchStationProfiles'),
      searchPickupLocations: document.getElementById('searchPickupLocations'),
      locationStationScope: document.getElementById('locationStationScope'),
      stationProfilesCount: document.getElementById('stationProfilesCount'),
      addStationProfileBtn: document.getElementById('addStationProfileBtn'),
      stationProfileOverlay: document.getElementById('stationProfileOverlay'),
      stationProfileTitle: document.getElementById('stationProfileTitle'),
      stationProfileSub: document.getElementById('stationProfileSub'),
      stationProfileKey: document.getElementById('stationProfileKey'),
      stationProfileLabel: document.getElementById('stationProfileLabel'),
      stationProfileSortOrder: document.getElementById('stationProfileSortOrder'),
      stationProfileCity: document.getElementById('stationProfileCity'),
      stationProfileAddress: document.getElementById('stationProfileAddress'),
      stationProfileDescription: document.getElementById('stationProfileDescription'),
      stationProfileActive: document.getElementById('stationProfileActive'),
      closeStationProfileBtn: document.getElementById('closeStationProfileBtn'),
      cancelStationProfileBtn: document.getElementById('cancelStationProfileBtn'),
      saveStationProfileBtn: document.getElementById('saveStationProfileBtn'),
      deleteStationProfileModalBtn: document.getElementById('deleteStationProfileModalBtn'),
      searchPricingSeasons: document.getElementById('searchPricingSeasons'),
      searchCategoryRateRules: document.getElementById('searchCategoryRateRules'),
      filterRateRuleCategory: document.getElementById('filterRateRuleCategory'),
      filterRateRuleSeason: document.getElementById('filterRateRuleSeason'),
      simCategoryCode: document.getElementById('simCategoryCode'),
      simPickupDate: document.getElementById('simPickupDate'),
      simDays: document.getElementById('simDays'),
      simBasePrice: document.getElementById('simBasePrice'),
      searchInsurance: document.getElementById('searchInsurance'),
      searchExtras: document.getElementById('searchExtras'),
      searchFees: document.getElementById('searchFees'),
      searchSessions: document.getElementById('searchSessions'),
      searchRoles: document.getElementById('searchRoles'),
      searchUsers: document.getElementById('searchUsers'),
      openSecurityFromUsersBtn: document.getElementById('openSecurityFromUsersBtn'),
      filterRuleType: document.getElementById('filterRuleType'),
      refreshAuditBtn: document.getElementById('refreshAuditBtn'),
      editorOverlay: document.getElementById('editorOverlay'),
      editorTitle: document.getElementById('editorTitle'),
      editorSub: document.getElementById('editorSub'),
      editorGrid: document.getElementById('editorGrid'),
      deleteEditorBtn: document.getElementById('deleteEditorBtn'),
      closeEditorBtn: document.getElementById('closeEditorBtn'),
      cancelEditorBtn: document.getElementById('cancelEditorBtn'),
      saveEditorBtn: document.getElementById('saveEditorBtn'),
      healthList: document.getElementById('healthList'),
      refreshSnapshotBtn: document.getElementById('refreshSnapshotBtn'),
      exportSnapshotBtn: document.getElementById('exportSnapshotBtn'),
      copySnapshotBtn: document.getElementById('copySnapshotBtn'),
      pinSettingsViewBtn: document.getElementById('pinSettingsViewBtn'),
      saveSettingsViewBtn: document.getElementById('saveSettingsViewBtn'),
      clearWorkspaceViewsBtn: document.getElementById('clearWorkspaceViewsBtn'),
      clearWorkspacePinsBtn: document.getElementById('clearWorkspacePinsBtn'),
      seedAllDefaultsBtn: document.getElementById('seedAllDefaultsBtn'),
      resetBusinessDefaultsBtn: document.getElementById('resetBusinessDefaultsBtn'),
      inspectorOverlay: document.getElementById('inspectorOverlay'),
      inspectorTitle: document.getElementById('inspectorTitle'),
      inspectorSub: document.getElementById('inspectorSub'),
      inspectorBody: document.getElementById('inspectorBody'),
      inspectorActions: document.getElementById('inspectorActions'),
      closeInspectorBtn: document.getElementById('closeInspectorBtn'),
      inspectorDoneBtn: document.getElementById('inspectorDoneBtn')
    };

    function money(value, currency = 'EUR'){
      const num = Number(value || 0);
      return new Intl.NumberFormat('el-GR', { style:'currency', currency: currency || 'EUR' }).format(num);
    }
    function esc(value){ return String(value ?? '').replace(/[&<>"']/g, ch => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch])); }
    function clone(v){ return JSON.parse(JSON.stringify(v || null)); }
    function normalizeMessage(raw){ if(!raw) return null; if(typeof raw === 'string'){ try{ return JSON.parse(raw); }catch(_){ return null; } } return raw; }
    function post(type, extra){ try{ window.parent.postMessage(JSON.stringify({ type, ...extra }), '*'); }catch(_){ try{ window.parent.postMessage({ type, ...extra }, '*'); }catch(__){} } }
    function showToast(message, tone){ els.toast.textContent = message || ''; els.toast.style.background = tone === 'error' ? '#7f1d1d' : (tone === 'success' ? '#065f46' : '#0f172a'); els.toast.classList.add('show'); clearTimeout(showToast._t); showToast._t = setTimeout(() => els.toast.classList.remove('show'), 3600); }
    function requestResize(){ const h = Math.max(document.documentElement.scrollHeight, document.body.scrollHeight, 1200); post('resize', { height: h }); }

    function tableLayoutFor(key){
      if(!state.tableLayouts) state.tableLayouts = {};
      if(!state.tableLayouts[key]) state.tableLayouts[key] = { order: [], widths: {}, hidden: [], sort: { originalIndex:'', direction:'' } };
      if(!state.tableLayouts[key].sort) state.tableLayouts[key].sort = { originalIndex:'', direction:'' };
      return state.tableLayouts[key];
    }
    function normalizeColumnOrder(order, count){
      const max = Math.max(0, Number(count) || 0);
      const seen = new Set();
      const next = [];
      (Array.isArray(order) ? order : []).forEach((value) => {
        const num = Number(value);
        if(Number.isInteger(num) && num >= 0 && num < max && !seen.has(num)){
          seen.add(num);
          next.push(num);
        }
      });
      for(let i = 0; i < max; i += 1){ if(!seen.has(i)) next.push(i); }
      return next;
    }
    function normalizeHiddenColumns(hidden, count){
      const max = Math.max(0, Number(count) || 0);
      const seen = new Set();
      return (Array.isArray(hidden) ? hidden : []).map((value) => String(value)).filter((value) => {
        const num = Number(value);
        if(!Number.isInteger(num) || num < 0 || num >= max || seen.has(String(num))) return false;
        seen.add(String(num));
        return true;
      });
    }
    function normalizeSortState(sort, count){
      const max = Math.max(0, Number(count) || 0);
      const originalIndex = String(sort?.originalIndex ?? '').trim();
      const num = Number(originalIndex);
      const direction = sort?.direction === 'desc' ? 'desc' : (sort?.direction === 'asc' ? 'asc' : '');
      if(!Number.isInteger(num) || num < 0 || num >= max || !direction){
        return { originalIndex:'', direction:'' };
      }
      return { originalIndex:String(num), direction };
    }
    function parseSortValue(raw){
      const text = String(raw ?? '').trim();
      if(!text) return { type:'empty', value:'' };
      const dateMatch = text.match(/^(\d{1,2})[\/.\-](\d{1,2})[\/.\-](\d{2,4})(?:\s+(\d{1,2}):(\d{2}))?$/);
      if(dateMatch){
        let year = Number(dateMatch[3]);
        if(year < 100) year += 2000;
        const month = Number(dateMatch[2]) - 1;
        const day = Number(dateMatch[1]);
        const hour = Number(dateMatch[4] || 0);
        const minute = Number(dateMatch[5] || 0);
        const stamp = new Date(year, month, day, hour, minute).getTime();
        if(!Number.isNaN(stamp)) return { type:'number', value: stamp };
      }
      const isoStamp = Date.parse(text);
      if(!Number.isNaN(isoStamp) && /\d/.test(text) && /[-/:]/.test(text)) return { type:'number', value: isoStamp };
      const numericCandidate = text.replace(/[^\d,.-]/g, '').replace(/\.(?=\d{3}(?:\D|$))/g, '').replace(',', '.');
      if(numericCandidate && /^-?\d+(?:\.\d+)?$/.test(numericCandidate)){
        const num = Number(numericCandidate);
        if(!Number.isNaN(num)) return { type:'number', value:num };
      }
      return { type:'string', value:text.toLocaleLowerCase('el-GR') };
    }
    function compareSortValues(a, b){
      const va = parseSortValue(a);
      const vb = parseSortValue(b);
      if(va.type === 'empty' && vb.type === 'empty') return 0;
      if(va.type === 'empty') return 1;
      if(vb.type === 'empty') return -1;
      if(va.type === 'number' && vb.type === 'number') return va.value - vb.value;
      if(va.type === 'number') return -1;
      if(vb.type === 'number') return 1;
      return String(va.value).localeCompare(String(vb.value), 'el', { sensitivity:'base', numeric:true });
    }
    function getSortCellValue(row, originalIndex){
      const cell = Array.from(row.cells).find((item) => String(item.dataset.originalIndex || '') === String(originalIndex));
      if(!cell) return '';
      return cell.getAttribute('data-sort-value') || cell.dataset.sortValue || cell.textContent || '';
    }
    function applyTableSort(table, layout){
      const tbody = table.tBodies?.[0];
      const sort = normalizeSortState(layout?.sort, table.tHead?.rows?.[0]?.cells?.length || 0);
      layout.sort = sort;
      if(!tbody || !sort.originalIndex || !sort.direction) return;
      const rows = Array.from(tbody.rows).map((row, idx) => ({ row, idx, value: getSortCellValue(row, sort.originalIndex) }));
      rows.sort((a, b) => {
        const cmp = compareSortValues(a.value, b.value);
        if(cmp !== 0) return sort.direction === 'desc' ? -cmp : cmp;
        return a.idx - b.idx;
      });
      const frag = document.createDocumentFragment();
      rows.forEach((item) => frag.appendChild(item.row));
      tbody.appendChild(frag);
    }

    function ensureOriginalColumnIdentity(table){
      Array.from(table.rows).forEach((row) => {
        Array.from(row.children).forEach((cell, index) => {
          if(!cell.dataset.originalIndex){
            cell.dataset.originalIndex = String(index);
          }
        });
      });
    }
    function setColumnWidth(table, visualIndex, width){
      const px = Math.max(96, Math.round(width || 0));
      Array.from(table.rows).forEach((row) => {
        const cell = row.children[visualIndex];
        if(cell){ cell.style.width = px + 'px'; cell.style.minWidth = px + 'px'; }
      });
    }
    function applyColumnOrder(table, order){
      ensureOriginalColumnIdentity(table);
      Array.from(table.rows).forEach((row) => {
        const cells = Array.from(row.children);
        const byOriginal = new Map(cells.map((cell) => [String(cell.dataset.originalIndex || ''), cell]));
        const frag = document.createDocumentFragment();
        order.forEach((idx) => {
          const cell = byOriginal.get(String(idx));
          if(cell) frag.appendChild(cell);
        });
        row.appendChild(frag);
      });
    }
    function moveColumnOriginal(layoutOrder, dragOriginal, targetOriginal){
      const nextOrder = Array.from(layoutOrder, (value) => Number(value));
      const dragIndex = nextOrder.indexOf(Number(dragOriginal));
      const targetIndex = nextOrder.indexOf(Number(targetOriginal));
      if(dragIndex < 0 || targetIndex < 0 || dragIndex === targetIndex) return nextOrder;
      const moved = nextOrder.splice(dragIndex, 1)[0];
      let insertIndex = targetIndex;
      if(dragIndex < targetIndex) insertIndex -= 1;
      insertIndex = Math.max(0, Math.min(insertIndex, nextOrder.length));
      nextOrder.splice(insertIndex, 0, moved);
      return nextOrder;
    }
    function setColumnVisibility(table, layout){
      const hidden = new Set(Array.isArray(layout.hidden) ? layout.hidden.map((v) => String(v)) : []);
      Array.from(table.rows).forEach((row) => {
        Array.from(row.children).forEach((cell, visualIndex) => {
          const originalIndex = String(layout.order?.[visualIndex] ?? visualIndex);
          cell.style.display = hidden.has(originalIndex) ? 'none' : '';
        });
      });
    }
    function ensureTableToolbar(container, tableKey, layout, labels){
      let shell = container.querySelector(':scope > .table-shell');
      const tableWrap = container.querySelector(':scope > .table-wrap');
      if(tableWrap && !shell){
        shell = document.createElement('div');
        shell.className = 'table-shell';
        container.insertBefore(shell, tableWrap);
        shell.appendChild(tableWrap);
      }
      if(!shell) return null;
      let toolbar = shell.querySelector(':scope > .table-toolbar');
      if(!toolbar){
        toolbar = document.createElement('div');
        toolbar.className = 'table-toolbar';
        toolbar.innerHTML = `<div class="table-toolbar-left"><div><div class="table-toolbar-title">Προβολή πίνακα</div><div class="table-toolbar-sub">Ρύθμισε στήλες, πλάτη και σειρά εμφάνισης.</div></div></div><div class="table-toolbar-right"><div class="table-col-menu"><button type="button" class="table-col-btn" data-col-toggle="1">Columns</button><div class="table-col-panel"><div class="table-col-head"><div><strong>Columns</strong><span>Επίλεξε ποιες στήλες φαίνονται και άλλαξε τη σειρά τους από εδώ ή από τις κεφαλίδες του πίνακα.</span></div></div><div class="table-col-search"><input type="search" placeholder="Search columns…" aria-label="Search columns"></div><div class="table-col-list"></div><div class="table-col-note">Drag & drop για σειρά · checkbox για εμφάνιση/απόκρυψη · resize από το δεξί άκρο της κεφαλίδας.</div></div></div></div>`;
        shell.insertBefore(toolbar, shell.firstChild);
      }
      const menu = toolbar.querySelector('.table-col-menu');
      const toggleBtn = toolbar.querySelector('[data-col-toggle]');
      const list = toolbar.querySelector('.table-col-list');
      const searchInput = toolbar.querySelector('.table-col-search input');
      if(toggleBtn && !toggleBtn.dataset.bound){
        toggleBtn.dataset.bound = '1';
        toggleBtn.addEventListener('click', (event) => {
          event.preventDefault();
          menu.classList.toggle('open');
          toggleBtn.classList.toggle('active', menu.classList.contains('open'));
          if(menu.classList.contains('open') && searchInput){
            requestAnimationFrame(() => searchInput.focus());
          }
        });
        document.addEventListener('click', (event) => {
          if(!menu.contains(event.target)){
            menu.classList.remove('open');
            toggleBtn.classList.remove('active');
          }
        });
      }
      if(!searchInput?.dataset.bound){
        searchInput.dataset.bound = '1';
        searchInput.addEventListener('input', () => renderChooser());
      }
      function renderChooser(){
        if(!list) return;
        const hidden = new Set(Array.isArray(layout.hidden) ? layout.hidden.map((v) => String(v)) : []);
        const query = String(searchInput?.value || '').trim().toLowerCase();
        const rows = layout.order.map((originalIndex, visualIndex) => ({
          originalIndex: String(originalIndex),
          visualIndex,
          label: labels[String(originalIndex)] || `Column ${Number(originalIndex) + 1}`
        })).filter((row) => !query || row.label.toLowerCase().includes(query));
        if(!rows.length){
          list.innerHTML = '<div class="table-col-empty">Δεν βρέθηκαν στήλες για αυτό το search.</div>';
          return;
        }
        list.innerHTML = rows.map((row) => {
          const checked = hidden.has(row.originalIndex) ? '' : 'checked';
          return `<div class="table-col-item" draggable="true" data-original-index="${row.originalIndex}"><label class="table-col-check"><input type="checkbox" data-col-visibility="${row.originalIndex}" ${checked}><span class="table-col-label"><span class="table-col-handle">⋮⋮</span><span>${esc(row.label)}</span></span></label><span class="table-col-order">${row.visualIndex + 1}</span></div>`;
        }).join('');
        list.querySelectorAll('[data-col-visibility]').forEach((input) => {
          input.onchange = () => {
            const originalIndex = String(input.getAttribute('data-col-visibility') || '');
            let hiddenList = Array.isArray(layout.hidden) ? layout.hidden.map((v) => String(v)) : [];
            if(input.checked){
              hiddenList = hiddenList.filter((v) => v !== originalIndex);
            }else{
              const visibleCount = layout.order.filter((idx) => !hiddenList.includes(String(idx)) && String(idx) !== originalIndex).length;
              if(visibleCount < 1){ input.checked = true; return; }
              if(!hiddenList.includes(originalIndex)) hiddenList.push(originalIndex);
            }
            layout.hidden = hiddenList;
            persistTableLayouts();
            enhanceTable(container, tableKey);
          };
        });
        let chooserDrag = null;
        const clearChooserState = () => list.querySelectorAll('.table-col-item').forEach((row) => row.classList.remove('dragging','drag-over','drag-over-after'));
        const commitChooserDrop = (targetOriginal) => {
          if(!chooserDrag || !chooserDrag.active) return;
          const dragOriginal = String(chooserDrag.originalIndex || '');
          const target = String(targetOriginal || chooserDrag.targetOriginal || '');
          if(!dragOriginal || !target || dragOriginal === target) return;
          const nextOrder = moveColumnOriginal(layout.order, dragOriginal, target);
          const changed = nextOrder.some((value, index) => value !== Number(layout.order[index]));
          if(!changed) return;
          layout.order = normalizeColumnOrder(nextOrder, layout.order.length);
          persistTableLayouts();
          enhanceTable(container, tableKey);
        };
        list.querySelectorAll('.table-col-item').forEach((item) => {
          const handle = item.querySelector('.table-col-handle') || item;
          handle.style.cursor = 'grab';
          handle.onpointerdown = (event) => {
            if(event.button !== 0) return;
            if(String(searchInput?.value || '').trim()){
              showToast('Καθάρισε το search για reorder στηλών.', 'error');
              return;
            }
            event.preventDefault();
            event.stopPropagation();
            chooserDrag = {
              originalIndex: String(item.dataset.originalIndex || ''),
              item,
              startX: event.clientX,
              startY: event.clientY,
              active: false,
              targetOriginal: ''
            };
            const onMove = (moveEvent) => {
              if(!chooserDrag) return;
              const dist = Math.hypot(moveEvent.clientX - chooserDrag.startX, moveEvent.clientY - chooserDrag.startY);
              if(!chooserDrag.active){
                if(dist < 6) return;
                chooserDrag.active = true;
              }
              clearChooserState();
              chooserDrag.item.classList.add('dragging');
              const hit = document.elementFromPoint(moveEvent.clientX, moveEvent.clientY)?.closest('.table-col-item');
              if(hit && hit !== chooserDrag.item && list.contains(hit)){
                hit.classList.add('drag-over');
                chooserDrag.targetOriginal = String(hit.dataset.originalIndex || '');
                moveEvent.preventDefault();
              } else {
                chooserDrag.targetOriginal = '';
              }
            };
            const onUp = () => {
              document.removeEventListener('pointermove', onMove);
              document.removeEventListener('pointerup', onUp);
              if(!chooserDrag) return;
              const target = chooserDrag.targetOriginal;
              commitChooserDrop(target);
              clearChooserState();
              chooserDrag = null;
            };
            document.addEventListener('pointermove', onMove);
            document.addEventListener('pointerup', onUp, { once:true });
          };
        });
      }
      renderChooser();
      return toolbar;
    }
    function enhanceTable(container, tableKey){
      const table = container?.querySelector('table');
      if(!table || !table.tHead || !table.tHead.rows[0]) return;
      table.classList.add('is-enhanced');
      ensureOriginalColumnIdentity(table);
      const originalCount = table.tHead.rows[0].cells.length;
      const layout = tableLayoutFor(tableKey);
      layout.order = normalizeColumnOrder(layout.order, originalCount);
      layout.hidden = normalizeHiddenColumns(layout.hidden, originalCount);
      layout.sort = normalizeSortState(layout.sort, originalCount);
      applyColumnOrder(table, layout.order);
      const headCells = Array.from(table.tHead.rows[0].cells);
      const labels = {};
      headCells.forEach((th, visualIndex) => {
        const originalIndex = layout.order[visualIndex];
        const existingLabel = th.dataset.colLabel || th.querySelector('.th-label')?.textContent?.trim() || th.textContent.trim() || `Column ${visualIndex + 1}`;
        labels[String(originalIndex)] = existingLabel;
        th.dataset.tableKey = tableKey;
        th.dataset.originalIndex = String(originalIndex);
        th.dataset.visualIndex = String(visualIndex);
        th.dataset.colLabel = existingLabel;
        if(!th.querySelector('.th-inner')){
          th.textContent = '';
          const inner = document.createElement('div'); inner.className = 'th-inner';
          const label = document.createElement('span'); label.className = 'th-label'; label.textContent = existingLabel;
          const tools = document.createElement('span'); tools.className = 'th-tools';
          const sort = document.createElement('button'); sort.type = 'button'; sort.className = 'th-sort'; sort.title = 'Sort'; sort.setAttribute('aria-label', 'Sort column');
          const drag = document.createElement('span'); drag.className = 'th-drag'; drag.textContent = '⋮⋮'; drag.title = 'Drag & drop για μετακίνηση';
          tools.append(sort, drag); inner.append(label, tools); th.appendChild(inner);
          const resizer = document.createElement('span'); resizer.className = 'th-resizer'; th.appendChild(resizer);
        }else{
          const label = th.querySelector('.th-label');
          if(label) label.textContent = existingLabel;
        }
        th.draggable = false;
        const savedWidth = layout.widths?.[originalIndex];
        if(savedWidth) setColumnWidth(table, visualIndex, savedWidth);
        const sortBtn = th.querySelector('.th-sort');
        if(sortBtn){
          const active = layout.sort?.originalIndex === String(originalIndex) && !!layout.sort?.direction;
          sortBtn.dataset.dir = active ? layout.sort.direction : '';
          sortBtn.classList.toggle('active', active);
          sortBtn.title = active ? `Sort: ${layout.sort.direction === 'asc' ? 'ascending' : 'descending'}` : 'Sort';
        }
      });
      ensureTableToolbar(container, tableKey, layout, labels);
      applyTableSort(table, layout);
      setColumnVisibility(table, layout);

      let pointerReorder = null;
      let resizeState = null;
      const clearDragClasses = () => headCells.forEach((cell) => cell.classList.remove('dragging','drag-over','drag-over-after'));
      const getVisibleHeadCells = () => Array.from(table.tHead.rows[0].cells).filter((cell) => cell.style.display !== 'none');
      const getHeaderDropPosition = (clientX, clientY, srcCell) => {
        const hovered = document.elementFromPoint(clientX, clientY)?.closest('th');
        const visibleCells = getVisibleHeadCells();
        if(hovered && visibleCells.includes(hovered) && hovered !== srcCell){
          return { cell: hovered, targetOriginal: String(hovered.dataset.originalIndex || '') };
        }
        return null;
      };
      const finishReorder = (dropInfo) => {
        if(!pointerReorder) return;
        clearDragClasses();
        const dragOriginal = String(pointerReorder.originalIndex || '');
        const targetOriginal = String(dropInfo?.targetOriginal || '');
        if(dragOriginal && targetOriginal && dragOriginal !== targetOriginal){
          const nextOrder = moveColumnOriginal(layout.order, dragOriginal, targetOriginal);
          const changed = nextOrder.some((value, index) => value !== Number(layout.order[index]));
          if(changed){
            layout.order = normalizeColumnOrder(nextOrder, layout.order.length);
            persistTableLayouts();
            enhanceTable(container, tableKey);
          }
        }
        pointerReorder = null;
      };
      headCells.forEach((th, visualIndex) => {
        const handle = th.querySelector('.th-resizer');
        if(handle){
          handle.onpointerdown = (event) => {
            event.preventDefault(); event.stopPropagation();
            const startX = event.clientX;
            const startWidth = th.getBoundingClientRect().width;
            const originalIndex = th.dataset.originalIndex;
            resizeState = { visualIndex, startX, startWidth, originalIndex };
            document.body.classList.add('is-table-resizing');
            const onMove = (moveEvent) => {
              if(!resizeState) return;
              const newWidth = resizeState.startWidth + (moveEvent.clientX - resizeState.startX);
              setColumnWidth(table, visualIndex, newWidth);
              layout.widths[originalIndex] = Math.max(96, Math.round(newWidth));
            };
            const onUp = () => {
              document.removeEventListener('pointermove', onMove);
              document.removeEventListener('pointerup', onUp);
              resizeState = null;
              document.body.classList.remove('is-table-resizing');
              persistTableLayouts();
              requestResize();
            };
            document.addEventListener('pointermove', onMove);
            document.addEventListener('pointerup', onUp, { once:true });
          };
        }
        th.onpointerdown = (event) => {
          if(event.button !== 0) return;
          if(event.target.closest('.th-sort')) return;
          if(event.target.closest('.th-resizer') || event.target.closest('.table-col-panel')) return;
          if(!event.target.closest('.th-drag')) return;
          event.preventDefault();
          pointerReorder = { visualIndex, originalIndex:String(th.dataset.originalIndex || visualIndex), startX:event.clientX, startY:event.clientY, active:false, dropInfo:null };
          const onMove = (moveEvent) => {
            if(!pointerReorder || resizeState) return;
            const dist = Math.hypot(moveEvent.clientX - pointerReorder.startX, moveEvent.clientY - pointerReorder.startY);
            if(!pointerReorder.active){
              if(dist < 6) return;
              pointerReorder.active = true;
            }
            clearDragClasses();
            moveEvent.preventDefault();
            const src = headCells[pointerReorder.visualIndex];
            if(src) src.classList.add('dragging');
            const dropInfo = getHeaderDropPosition(moveEvent.clientX, moveEvent.clientY, src);
            pointerReorder.dropInfo = dropInfo;
            if(dropInfo?.cell && dropInfo.cell !== src) dropInfo.cell.classList.add('drag-over');
          };
          const onUp = () => {
            document.removeEventListener('pointermove', onMove);
            document.removeEventListener('pointerup', onUp);
            if(!pointerReorder || resizeState) return;
            finishReorder(pointerReorder.dropInfo);
          };
          document.addEventListener('pointermove', onMove);
          document.addEventListener('pointerup', onUp, { once:true });
        };
        th.onclick = (event) => {
          if(event.target.closest('.th-drag') || event.target.closest('.th-resizer') || event.target.closest('.table-col-panel')) return;
          event.preventDefault();
          const originalIndex = String(th.dataset.originalIndex || '');
          const current = layout.sort?.originalIndex === originalIndex ? layout.sort.direction : '';
          const next = current === 'asc' ? 'desc' : (current === 'desc' ? '' : 'asc');
          layout.sort = next ? { originalIndex, direction: next } : { originalIndex:'', direction:'' };
          persistTableLayouts();
          enhanceTable(container, tableKey);
        };
      });
    }
    function normalizedVehicleCategoryTitle(item){
      const categoryCode = String(item?.category || '').trim();
      let label = String(item?.displayTitle || item?.title || item?.categoryDisplayTitle || item?.category || '—').trim();
      const doubledPrefix = categoryCode ? `${categoryCode} - ${categoryCode} - ` : '';
      if(doubledPrefix && label.startsWith(doubledPrefix)){
        label = `${categoryCode} - ${label.slice(doubledPrefix.length)}`;
      }
      return label || '—';
    }
    function normalizedFleetCategoryLabel(item){
      const categoryCode = String(item?.category || '').trim();
      const linkedCategory = vehicleCategoryByCode(categoryCode);
      if(linkedCategory) return normalizedVehicleCategoryTitle(linkedCategory);
      let label = String(item?.categoryDisplayTitle || item?.category || '—').trim();
      const doubledPrefix = categoryCode ? `${categoryCode} - ${categoryCode} - ` : '';
      if(doubledPrefix && label.startsWith(doubledPrefix)){
        label = `${categoryCode} - ${label.slice(doubledPrefix.length)}`;
      }
      return label || '—';
    }
    function setBusy(flag){ state.busy = !!flag; Array.from(document.querySelectorAll('button')).forEach(btn => { if(btn.dataset.allowBusy === '1') return; btn.disabled = !!flag; }); }

    function isAdmin(){ return !!(currentStaff().isAdmin || currentStaff().roleKey === 'admin'); }
    function navAllowed(route){
      if(route === 'pricing') return canManageBusinessSettings();
      if(route === 'home') return true;
      const map = { daily:'rentalsView', fleet:'fleetView', bookings:'bookingsView' };
      const key = map[route];
      if(!key) return true;
      return !!((currentStaff().permissions || {})[key] || isAdmin());
    }

    function openSectionByKey(key, opts){
      const options = opts || {};
      const tabs = visibleTabs();
      if(!tabs.some(tab => tab.key === key)) key = tabs[0]?.key || 'settingsHome';
      state.activeSection = key;
      if(key === 'business' && options.businessPane){
        const selectedPane = BUSINESS_PANES.find((pane) => pane.key === options.businessPane);
        if(selectedPane){
          state.activeBusinessPane = selectedPane.key;
          state.activeBusinessArea = selectedPane.area;
        }
      }
      persistSettingsWorkspace();
      renderTabs();
      renderSections();
      requestResize();
    }

    function runToolbarSearch(raw){
      const q = String(raw || '').trim().toLowerCase();
      if(!q){
        showToast('Γράψε όνομα ενότητας ή ρύθμισης για γρήγορη μετάβαση.', 'info');
        return;
      }
      const pane = BUSINESS_PANES.find(item => `${item.label} ${item.shortLabel} ${item.helper || ''}`.toLowerCase().includes(q));
      if(pane){
        openSectionByKey('business', { businessPane: pane.key });
        showToast(`Άνοιξε: ${pane.shortLabel}`, 'success');
        return;
      }
      const tab = visibleTabs().find(item => `${item.label} ${item.group || ''}`.toLowerCase().includes(q));
      if(tab){
        openSectionByKey(tab.key);
        showToast(`Άνοιξε: ${tab.label}`, 'success');
        return;
      }
      showToast('Δεν βρέθηκε σχετική ενότητα στο settings toolbar.', 'error');
    }

    function renderBackroomHeader(){
      const current = currentStaff();
      const nav = [
        { key:'home', label:'Myroom' },
        { key:'daily', label:'Daily' },
        { key:'fleet', label:'Fleet' },
        { key:'bookings', label:'Bookings' },
        { key:'pricing', label:'Settings' }
      ];
      if(els.backroomNav){
        els.backroomNav.innerHTML = nav.map(item => `<button class="nav-btn ${item.key==='pricing'?'active':''} ${navAllowed(item.key)?'':'locked'}" data-nav-route="${item.key}" ${navAllowed(item.key)?'':'title="No access"'}>${item.label}</button>`).join('');
        els.backroomNav.querySelectorAll('[data-nav-route]').forEach(btn => btn.addEventListener('click', () => {
          const route = btn.getAttribute('data-nav-route');
          if(!navAllowed(route)){ showToast('Ο ρόλος σου δεν έχει πρόσβαση σε αυτή την ενότητα.', 'error'); return; }
          post('navigate', { route });
        }));
      }
      if(els.backroomUser){
        els.backroomUser.innerHTML = `
          <div class="backroom-search">
            <input id="topSearchInput" type="search" placeholder="Search settings, sections..." aria-label="Search settings" />
            <button id="topSearchBtn" type="button" data-allow-busy="1">Go</button>
          </div>
          <div class="quick-apps" id="quickAppsWrap">
            <button class="quick-apps-btn" id="quickAppsBtn" type="button">Apps</button>
            <div class="quick-apps-panel" id="quickAppsPanel">
              <div class="quick-apps-grid">
                <button class="quick-app-card" type="button" data-nav-route="home"><span class="quick-app-icon">⌂</span><b>Myroom</b><span>Κεντρική επιχειρησιακή εικόνα, daily actions και βασικά shortcuts.</span></button>
                <button class="quick-app-card" type="button" data-nav-route="daily"><span class="quick-app-icon">☼</span><b>Daily</b><span>Ημερήσια λειτουργία, movement view και operational timeline.</span></button>
                <button class="quick-app-card" type="button" data-nav-route="fleet"><span class="quick-app-icon">▣</span><b>Fleet</b><span>Διαθεσιμότητα, στόλος και fleet chart με focus στα οχήματα.</span></button>
                <button class="quick-app-card" type="button" data-nav-route="bookings"><span class="quick-app-icon">✓</span><b>Bookings</b><span>Κρατήσεις, follow-up και επιβεβαίωση ροών πελατών.</span></button>
                <button class="quick-app-card" type="button" data-nav-route="pricing"><span class="quick-app-icon">⚙</span><b>Settings</b><span>Front data, pricing rules, access και συνολική ρύθμιση εταιρίας.</span></button>
                <button class="quick-app-card" type="button" data-jump-settings="business"><span class="quick-app-icon">↗</span><b>Jump to business</b><span>Άμεση μετάβαση στο business control center του settings.</span></button>
              </div>
            </div>
          </div>
          <div class="top-icon-group">
            <button class="top-icon-btn" id="topStatusBtn" type="button" title="System status"><span class="dot"></span></button>
            <button class="top-icon-btn" id="topHelpBtn" type="button" title="Quick help">?</button>
          </div>
          <span class="top-chip"><strong>${esc(current.name || current.email || 'Staff')}</strong></span>
          <span class="top-chip">${esc(current.roleLabel || 'Role')}</span>
          <button class="header-action-btn" id="topReloadBtn" data-allow-busy="1">Reload</button>
          <button class="header-action-btn" id="topLogoutBtn" data-allow-busy="1">Logout</button>`;
        document.getElementById('topReloadBtn')?.addEventListener('click', () => { setBusy(true); post('pricingAdminReady', {}); });
        document.getElementById('topLogoutBtn')?.addEventListener('click', () => post('menuAction', { action:'logout' }));
        document.getElementById('topHelpBtn')?.addEventListener('click', () => showToast('Χρησιμοποίησε το search ή το quick apps panel για άμεση μετάβαση στις βασικές ενότητες.', 'info'));
        document.getElementById('topStatusBtn')?.addEventListener('click', () => openSectionByKey('settingsHome'));
        const searchInput = document.getElementById('topSearchInput');
        const searchBtn = document.getElementById('topSearchBtn');
        searchBtn?.addEventListener('click', () => runToolbarSearch(searchInput?.value || ''));
        searchInput?.addEventListener('keydown', (event) => {
          if(event.key === 'Enter'){
            event.preventDefault();
            runToolbarSearch(searchInput.value || '');
          }
        });
        const quickWrap = document.getElementById('quickAppsWrap');
        const quickBtn = document.getElementById('quickAppsBtn');
        const quickPanel = document.getElementById('quickAppsPanel');
        const closeQuick = (evt) => {
          if(!quickWrap?.contains(evt.target)) quickWrap?.classList.remove('open');
        };
        quickBtn?.addEventListener('click', (event) => {
          event.preventDefault();
          event.stopPropagation();
          const open = !quickWrap.classList.contains('open');
          quickWrap.classList.toggle('open', open);
          if(open){
            setTimeout(() => document.addEventListener('click', closeQuick, true), 0);
          } else {
            document.removeEventListener('click', closeQuick, true);
          }
        });
        quickPanel?.querySelectorAll('[data-nav-route]').forEach(btn => btn.addEventListener('click', () => {
          const route = btn.getAttribute('data-nav-route');
          quickWrap?.classList.remove('open');
          document.removeEventListener('click', closeQuick, true);
          if(!navAllowed(route)){ showToast('Ο ρόλος σου δεν έχει πρόσβαση σε αυτή την ενότητα.', 'error'); return; }
          post('navigate', { route });
        }));
        quickPanel?.querySelector('[data-jump-settings="business"]')?.addEventListener('click', () => {
          quickWrap?.classList.remove('open');
          document.removeEventListener('click', closeQuick, true);
          openSectionByKey('business', { businessPane: state.activeBusinessPane || 'frontCompany' });
          showToast('Άνοιξε το business control center.', 'success');
        });
      }
    }

    function visibleTabs(){
      const snap = accessSnapshot();
      if(snap.accessState !== 'ready'){
        return TAB_DEF.filter(tab => !['security','roles','users','audit'].includes(tab.key));
      }
      const current = snap.currentStaff || {};
      return TAB_DEF.filter((tab) => {
        if(['security','roles','users'].includes(tab.key)) return !!current.canManageStaff;
        if(tab.key === 'audit') return !!current.canViewAudit;
        return true;
      });
    }
    function renderTabs(){
      try{
        let tabs = visibleTabs();
        if(!tabs.length){
          tabs = TAB_DEF.filter(tab => ['settingsHome','business','fleetCategories','fleetVehicles','dynamicPricing','insurance','extras','fees'].includes(tab.key));
        }
        if(!tabs.some(tab => tab.key === state.activeSection)) state.activeSection = tabs[0]?.key || 'settingsHome';
        persistSettingsWorkspace();
        const grouped = tabs.reduce((acc, tab) => {
          const key = tab.group || 'General';
          if(!acc[key]) acc[key] = [];
          acc[key].push(tab);
          return acc;
        }, {});
        const groups = Object.keys(grouped);
        const activeGroup = (tabs.find(tab => tab.key === state.activeSection)?.group) || groups[0] || 'General';
        const activeLabelText = (tabs.find(tab => tab.key === state.activeSection)?.label) || 'Επιλογή ενότητας';
        const activeBusinessArea = state.activeBusinessArea || 'front';
        const areaPanes = BUSINESS_PANES.filter((pane) => pane.area === activeBusinessArea);
        const topGroupDefs = [
          { key:'overview', label:'Overview', section:'settingsHome' },
          { key:'front', label:'Front', section:'business', businessArea:'front' },
          { key:'company', label:'Company', section:'business', businessArea:'company' },
          { key:'communications', label:'Communications', section:'business', businessArea:'communications' },
          { key:'operations', label:'Operations', section:'fleetCategories' },
          { key:'pricing', label:'Pricing rules', section:'dynamicPricing' },
          { key:'team', label:'Team & access', section:(tabs.find(tab => ['roles','users','security','audit'].includes(tab.key)) || tabs.find(tab => tab.group === 'Team & access') || tabs[0] || {}).key || 'settingsHome' }
        ];
        const topGroupButtons = topGroupDefs
          .map((entry) => {
            const isVisible = tabs.some(tab => tab.key === entry.section) || entry.section === 'business';
            if(!isVisible) return '';
            const isActive = entry.section === 'business'
              ? (state.activeSection === 'business' && activeBusinessArea === entry.businessArea)
              : (activeGroup === entry.label || state.activeSection === entry.section);
            return `<button class="group-pill ${isActive ? 'active' : ''}" data-top-group="${entry.section}" ${entry.businessArea ? `data-top-area="${entry.businessArea}"` : ''}>${entry.label}</button>`;
          })
          .join('');
        els.tabs.innerHTML = `
        <div class="settings-nav-shell">
          <div class="settings-nav-topline">
            <div class="settings-context">
              <span class="settings-context-badge">Settings</span>
              <div class="settings-context-copy">
                <strong>${activeLabelText}</strong>
              </div>
            </div>
            <div class="settings-nav-actions">
              ${state.activeSection === 'business' ? '<button class="btn warn" id="navResetBusinessBtn" data-allow-busy="1">Reset defaults</button><button class="btn primary" id="navSaveBusinessBtn" data-allow-busy="1">Αποθήκευση business settings</button>' : ''}
              <button class="btn" id="settingsHomeRefreshBtn" data-allow-busy="1">Ανανέωση</button>
              <div class="tab-picker">
                <button class="picker-trigger" id="tabPickerTrigger">All sections ▾</button>
                <div class="picker-menu" id="tabPickerMenu">${groups.map(group => `<div class="picker-section"><div class="picker-label">${group}</div><div class="picker-items">${(grouped[group]||[]).map(tab => `<button class="tab ${state.activeSection === tab.key ? 'active' : ''}" data-tab="${tab.key}">${tab.label}</button>`).join('')}</div></div>`).join('')}</div>
              </div>
            </div>
          </div>
          <div class="group-rail">${topGroupButtons}</div>
          ${state.activeSection === 'business' ? `<div class="section-row"><div class="section-rail">${areaPanes.map((pane) => `<button class="tab ${pane.key===state.activeBusinessPane?'active':''}" data-top-business-pane="${pane.key}">${pane.label}</button>`).join('')}</div></div>` : ''}
        </div>`;
        const activate = (key) => { openSectionByKey(key); };
        els.tabs.querySelectorAll('[data-tab]').forEach(btn => btn.addEventListener('click', () => activate(btn.dataset.tab)));
        els.tabs.querySelectorAll('[data-top-group]').forEach(btn => btn.addEventListener('click', () => {
          const section = btn.getAttribute('data-top-group') || 'settingsHome';
          const area = btn.getAttribute('data-top-area') || '';
          if(section === 'business'){
            if(area) setActiveBusinessArea(area);
            openSectionByKey('business', { businessPane: state.activeBusinessPane || BUSINESS_PANES.find((pane) => pane.area === (area || activeBusinessArea))?.key || 'frontBrand' });
          } else {
            activate(section);
          }
          renderTabs();
          renderSections();
        }));
        els.tabs.querySelectorAll('[data-top-business-pane]').forEach(btn => btn.addEventListener('click', () => {
          const key = btn.getAttribute('data-top-business-pane') || '';
          if(key) setActiveBusinessPane(key);
          renderTabs();
        }));
        const navResetBusinessBtn = document.getElementById('navResetBusinessBtn');
        if(navResetBusinessBtn){
          navResetBusinessBtn.addEventListener('click', () => { document.getElementById('resetBusinessDefaultsBtn')?.click(); });
        }
        const navSaveBusinessBtn = document.getElementById('navSaveBusinessBtn');
        if(navSaveBusinessBtn){
          navSaveBusinessBtn.addEventListener('click', () => { document.getElementById('saveBusinessBtn')?.click(); });
        }
        const pickerTrigger = document.getElementById('tabPickerTrigger');
        const pickerMenu = document.getElementById('tabPickerMenu');
        if(state._tabPickerDocHandler){
          document.removeEventListener('click', state._tabPickerDocHandler, true);
          state._tabPickerDocHandler = null;
        }
        if(pickerTrigger && pickerMenu){
          pickerTrigger.addEventListener('click', (event) => {
            event.preventDefault();
            event.stopPropagation();
            const willOpen = !pickerMenu.classList.contains('open');
            pickerMenu.classList.toggle('open', willOpen);
            if(willOpen && state._tabPickerDocHandler){
              document.addEventListener('click', state._tabPickerDocHandler, true);
            }
          });
          pickerMenu.querySelectorAll('[data-tab]').forEach(btn => btn.addEventListener('click', () => { pickerMenu.classList.remove('open'); }));
          state._tabPickerDocHandler = (event) => {
            if(!pickerMenu.contains(event.target) && !pickerTrigger.contains(event.target)){
              pickerMenu.classList.remove('open');
              document.removeEventListener('click', state._tabPickerDocHandler, true);
            }
          };
        }
        if(els.tabsFallback){
          const hasPrimaryNav = !!els.tabs.querySelector('.settings-nav-shell');
          els.tabsFallback.style.display = hasPrimaryNav ? 'none' : 'flex';
          els.tabsFallback.querySelectorAll('[data-tab]').forEach(btn => {
            btn.classList.toggle('active', btn.getAttribute('data-tab') === state.activeSection);
            btn.onclick = () => activate(btn.getAttribute('data-tab') || 'settingsHome');
          });
        }
      }catch(err){
        console.error('[pricingAdmin] renderTabs failed', err);
        if(els.tabs) els.tabs.innerHTML = '';
        if(els.tabsFallback){
          els.tabsFallback.style.display = 'flex';
          els.tabsFallback.querySelectorAll('[data-tab]').forEach(btn => {
            btn.classList.toggle('active', btn.getAttribute('data-tab') === state.activeSection);
            btn.onclick = () => openSectionByKey(btn.getAttribute('data-tab') || 'settingsHome');
          });
        }
      }
    }
    function renderSections(){ els.sections.forEach(sec => sec.classList.toggle('active', sec.dataset.section === state.activeSection)); if(state.activeSection === 'settingsHome') renderSettingsHome(); if(state.activeSection === 'business') renderBusinessPaneNav(); enhanceBusinessTextEditors(); bindBusinessJumps(); }



    function currentBusinessPane(){
      const area = state.activeBusinessArea || 'front';
      const scoped = BUSINESS_PANES.filter((pane) => pane.area === area);
      return scoped.find((pane) => pane.key === state.activeBusinessPane) || scoped[0] || BUSINESS_PANES[0];
    }
    function setActiveBusinessArea(area){
      if(!BUSINESS_AREAS.some((entry) => entry.key === area)) area = 'front';
      state.activeBusinessArea = area;
      const scoped = BUSINESS_PANES.filter((pane) => pane.area === area);
      if(!scoped.some((pane) => pane.key === state.activeBusinessPane)){
        state.activeBusinessPane = scoped[0]?.key || BUSINESS_PANES[0]?.key || 'frontBrand';
      }
      persistSettingsWorkspace();
      if(state.activeSection === 'business') renderBusinessPaneNav();
    }
    function setActiveBusinessPane(key){
      const hit = BUSINESS_PANES.find((pane) => pane.key === key);
      if(!hit){
        const scoped = BUSINESS_PANES.filter((pane) => pane.area === (state.activeBusinessArea || 'front'));
        key = scoped[0]?.key || BUSINESS_PANES[0].key;
      }
      state.activeBusinessPane = key;
      const current = BUSINESS_PANES.find((pane) => pane.key === key);
      if(current) state.activeBusinessArea = current.area;
      persistSettingsWorkspace();
      if(state.activeSection === 'business') renderBusinessPaneNav();
    }
    function renderBusinessPaneNav(){
      if(state.activeSection !== 'business') return;
      const activeArea = state.activeBusinessArea || 'front';
      const areaPanes = BUSINESS_PANES.filter((pane) => pane.area === activeArea);
      const active = areaPanes.find((pane) => pane.key === state.activeBusinessPane) || areaPanes[0] || BUSINESS_PANES[0];
      if(els.businessPaneTabs) els.businessPaneTabs.style.display = 'none';
      if(els.businessPaneNote) els.businessPaneNote.style.display = 'none';
      document.querySelectorAll('[data-business-pane]').forEach((pane) => pane.classList.toggle('active', pane.getAttribute('data-business-pane') === active.key));
      requestResize();
    }
    function openTextEditor(sourceId, title, hint){
      const source = document.getElementById(sourceId);
      if(!source || !els.textEditorOverlay || !els.textEditorInput) return;
      state.textEditor = { open:true, sourceId, title:title || 'Text editor', hint:hint || '' };
      if(els.textEditorTitle) els.textEditorTitle.textContent = title || 'Text editor';
      if(els.textEditorSub) els.textEditorSub.textContent = hint || 'Επεξεργασία μεγάλου κειμένου σε άνετο modal.';
      if(els.textEditorHint) els.textEditorHint.textContent = 'Το περιεχόμενο αποθηκεύεται ως απλό κείμενο για να παραμένει ασφαλές για το front rendering.';
      els.textEditorInput.value = source.value || '';
      updateTextEditorCount();
      els.textEditorOverlay.classList.add('show');
      requestAnimationFrame(() => { els.textEditorInput.focus(); });
    }
    function closeTextEditor(save){
      const sourceId = state.textEditor?.sourceId;
      if(save && sourceId){
        const source = document.getElementById(sourceId);
        if(source){
          source.value = els.textEditorInput.value;
          source.dispatchEvent(new Event('input', { bubbles:true }));
          syncRichPreview(source);
        }
      }
      state.textEditor = { open:false, sourceId:'', title:'', hint:'' };
      els.textEditorOverlay?.classList.remove('show');
    }
    function updateTextEditorCount(){ if(els.textEditorCount && els.textEditorInput) els.textEditorCount.textContent = `${(els.textEditorInput.value || '').length} χαρακτήρες`; }
    function insertIntoTextEditor(textToInsert){
      const input = els.textEditorInput;
      if(!input) return;
      const start = input.selectionStart || 0;
      const end = input.selectionEnd || 0;
      const value = input.value || '';
      input.value = value.slice(0, start) + textToInsert + value.slice(end);
      const pos = start + textToInsert.length;
      input.focus();
      input.setSelectionRange(pos, pos);
      updateTextEditorCount();
    }
    function syncRichPreview(source){
      const preview = document.querySelector(`[data-preview-for="${source.id}"]`);
      if(!preview) return;
      const value = String(source.value || '').trim();
      preview.textContent = value || (source.getAttribute('placeholder') || 'Άνοιγμα editor');
      preview.classList.toggle('is-empty', !value);
    }
    function enhanceBusinessTextEditors(){
      document.querySelectorAll('textarea[data-editor="modal"]').forEach((area) => {
        if(area.dataset.enhanced === '1') return;
        area.dataset.enhanced = '1';
        const field = area.closest('.field');
        if(!field) return;
        const label = field.querySelector('label');
        const hint = label ? label.textContent.trim() : 'Μεγάλο κείμενο';
        const preview = document.createElement('div');
        preview.className = 'rich-preview is-empty';
        preview.setAttribute('data-preview-for', area.id);
        preview.tabIndex = 0;
        preview.addEventListener('click', () => openTextEditor(area.id, hint, hint));
        preview.addEventListener('keydown', (event) => { if(event.key === 'Enter' || event.key === ' '){ event.preventDefault(); openTextEditor(area.id, hint, hint); } });
        const toolbar = document.createElement('div');
        toolbar.className = 'field-toolbar';
        toolbar.innerHTML = `<button class="mini-btn" type="button" data-open-rich="${area.id}">Άνοιγμα editor</button>`;
        field.appendChild(preview);
        field.appendChild(toolbar);
        toolbar.querySelector('[data-open-rich]').addEventListener('click', () => openTextEditor(area.id, hint, hint));
        syncRichPreview(area);
        area.addEventListener('input', () => syncRichPreview(area));
      });
      if(!state._textEditorBound){
        state._textEditorBound = true;
        document.getElementById('textEditorCloseBtn')?.addEventListener('click', () => closeTextEditor(false));
        document.getElementById('textEditorCancelBtn')?.addEventListener('click', () => closeTextEditor(false));
        document.getElementById('textEditorSaveBtn')?.addEventListener('click', () => closeTextEditor(true));
        els.textEditorOverlay?.addEventListener('click', (event) => { if(event.target === els.textEditorOverlay) closeTextEditor(false); });
        els.textEditorInput?.addEventListener('input', updateTextEditorCount);
        document.querySelectorAll('[data-text-tool]').forEach((btn) => {
          btn.addEventListener('click', () => {
            const kind = btn.getAttribute('data-text-tool');
            if(kind === 'paragraph') return insertIntoTextEditor('\n\n');
            if(kind === 'bullet') return insertIntoTextEditor('\n• ');
            if(kind === 'dash') return insertIntoTextEditor('\n— ');
            if(kind === 'uppercase') return insertIntoTextEditor('\nΚΕΦΑΛΙΔΑ\n');
          });
        });
      }
    }

    function focusBusinessGroup(groupKey){
      if(!groupKey) return;
      if(state.activeSection !== 'business'){
        state.activeSection = 'business';
        renderTabs();
        renderSections();
      }
      setActiveBusinessPane(groupKey);
      const target = document.querySelector(`[data-group-anchor="${groupKey}"]`);
      if(target){
        target.scrollIntoView({ behavior:'smooth', block:'start' });
      }
    }
    function bindBusinessJumps(){
      document.querySelectorAll('[data-focus-group]').forEach(btn => {
        if(btn._boundFocusGroup) return;
        btn._boundFocusGroup = true;
        btn.addEventListener('click', () => {
          const groupKey = btn.getAttribute('data-focus-group') || '';
          const homeGo = btn.getAttribute('data-home-go') || '';
          if(homeGo){
            state.activeSection = homeGo;
            renderTabs();
            renderSections();
            requestAnimationFrame(() => focusBusinessGroup(groupKey));
          } else {
            focusBusinessGroup(groupKey);
          }
        });
      });
    }
    function renderSettingsHome(){
      const tabs = visibleTabs().filter(tab => tab.key !== 'settingsHome');
      const links = document.getElementById('settingsHomeLinks');
      if(links){
        links.innerHTML = tabs.map(tab => `<div class="settings-home-item"><b>${tab.label}</b><span>${esc(tab.group || 'Settings')}</span><div class="actions"><button class="btn" data-home-section="${tab.key}">Άνοιγμα</button></div></div>`).join('');
        links.querySelectorAll('[data-home-section]').forEach(btn => btn.addEventListener('click', () => { state.activeSection = btn.getAttribute('data-home-section') || 'business'; renderTabs(); renderSections(); requestResize(); }));
      }
      const refreshBtn = document.getElementById('settingsHomeRefreshBtn');
      if(refreshBtn && !refreshBtn._boundSettingsHomeRefresh){
        refreshBtn._boundSettingsHomeRefresh = true;
        refreshBtn.addEventListener('click', () => { setBusy(true); post('pricingAdminReady', {}); });
      }
      bindBusinessJumps();
      document.querySelectorAll('[data-settings-section]').forEach(btn => {
        if(btn._boundSettingsSection) return;
        btn._boundSettingsSection = true;
        btn.addEventListener('click', () => {
          state.activeSection = btn.getAttribute('data-settings-section') || 'business';
          renderTabs();
          renderSections();
          requestResize();
        });
      });
    }

    function businessSettings(){ return state.snapshot?.businessSettings || {}; }
    function pruneLegacyStationToolbar(){
      const legacySelect = document.getElementById('businessStationSelect');
      const legacyMeta = document.getElementById('businessStationMeta');
      const root = legacySelect?.closest('.table-toolbar') || legacyMeta?.closest('.table-toolbar');
      if (root) root.remove();
    }
    function stationSettingsMap(){
      const map = businessSettings()?.stationSettings;
      return (map && typeof map === 'object' && !Array.isArray(map)) ? map : {};
    }
    function setStationSettingsMap(next){
      const currentSettings = businessSettings();
      const normalized = (next && typeof next === 'object' && !Array.isArray(next)) ? next : {};
      state.snapshot.businessSettings = { ...(currentSettings || {}), stationSettings: normalized };
    }
    function renameStationSettingsKey(fromKey, toKey){
      const from = String(fromKey || '').trim().toLowerCase();
      const to = String(toKey || '').trim().toLowerCase();
      if (!from || !to || from === to) return;
      const map = { ...(stationSettingsMap() || {}) };
      if (map[from] && !map[to]) map[to] = { ...map[from] };
      delete map[from];
      setStationSettingsMap(map);
    }
    function removeStationSettingsKey(key){
      const target = String(key || '').trim().toLowerCase();
      if (!target) return;
      const map = { ...(stationSettingsMap() || {}) };
      delete map[target];
      setStationSettingsMap(map);
    }
    function normalizeStationProfileRow(row = {}){
      const key = String(row.key || row.stationKey || '').trim().toLowerCase().replace(/[^a-z0-9_]+/g, '_').replace(/^_+|_+$/g, '') || 'default';
      const label = String(row.label || row.stationLabel || row.station || '').trim() || 'Κεντρικός σταθμός';
      const description = String(row.description || '').trim();
      const city = String(row.city || row.stationCity || '').trim();
      const address = String(row.address || row.stationAddress || '').trim();
      const sortOrder = Number.isFinite(Number(row.sortOrder)) ? Number(row.sortOrder) : 9999;
      const active = row.active !== false;
      return { key, label, description, city, address, sortOrder, active };
    }
    function stationProfilesFromBusiness(){
      const source = businessSettings()?.stationProfiles;
      const rows = Array.isArray(source) ? source : [];
      return rows.map(normalizeStationProfileRow).filter((row, idx, arr) => arr.findIndex((it)=>it.key===row.key) === idx);
    }
    function stationProfilesFromLocations(){
      const rows = pickupLocationRows().map((item) => {
        const key = String(item.stationKey || item.stationCode || item.station || '').trim().toLowerCase().replace(/[^a-z0-9_]+/g, '_').replace(/^_+|_+$/g, '');
        const label = String(item.stationLabel || item.station || '').trim();
        return key ? { key, label: label || key, description:'' } : null;
      }).filter(Boolean);
      return rows
        .filter((row, idx, arr) => arr.findIndex((it)=>it.key===row.key) === idx);
    }
    function effectiveStationProfiles(){
      const fromBusiness = Array.isArray(state.stationProfiles) && state.stationProfiles.length ? state.stationProfiles : stationProfilesFromBusiness();
      const merged = [...fromBusiness];
      stationProfilesFromLocations().forEach((row) => {
        if (!merged.some((it) => it.key === row.key)) merged.push(row);
      });
      if (!merged.length) merged.push({ key:'default', label:'Κεντρικός σταθμός', description:'' });
      return merged;
    }
    function migratePickupLocationsStation(fromKey, toKey, toLabel = ''){
      const from = String(fromKey || '').trim().toLowerCase();
      const to = String(toKey || '').trim().toLowerCase();
      if (!from || !to || from === to) return;
      const label = String(toLabel || '').trim();
      const rows = pickupLocationRows();
      rows.forEach((row) => {
        const rowKey = String(row.stationKey || row.station || 'default').trim().toLowerCase();
        if (rowKey !== from) return;
        row.stationKey = to;
        if (label) row.stationLabel = label;
      });
    }
    function stationScopedBusinessSettings(){
      const key = String(state.activeBusinessStationKey || 'default');
      const perStation = stationSettingsMap();
      const scoped = perStation && perStation[key] && typeof perStation[key] === 'object' ? perStation[key] : {};
      return { ...businessSettings(), ...scoped };
    }
    function vehicleCategoryRows(){ return Array.isArray(state.snapshot?.vehicleCategories) ? state.snapshot.vehicleCategories : []; }
    function fleetVehicleRows(){ return Array.isArray(state.snapshot?.fleetVehicles) ? state.snapshot.fleetVehicles : []; }
    function pickupLocationRows(){ return Array.isArray(state.snapshot?.pickupLocations) ? state.snapshot.pickupLocations : []; }
    function insuranceRows(){ return Array.isArray(state.snapshot?.insurancePlans) ? state.snapshot.insurancePlans : []; }
    function extraRows(){ return Array.isArray(state.snapshot?.extraServices) ? state.snapshot.extraServices : []; }
    function feeRows(){ return Array.isArray(state.snapshot?.feeRules) ? state.snapshot.feeRules : []; }
    function pricingSeasonsRows(){ return Array.isArray(state.snapshot?.pricingSeasons) ? state.snapshot.pricingSeasons : []; }
    function categoryRateRuleRows(){ return Array.isArray(state.snapshot?.categoryRateRules) ? state.snapshot.categoryRateRules : []; }
    function dayBracketLabel(minDays, maxDays){ return Number(maxDays || 0) > 0 ? `${Number(minDays || 1)}-${Number(maxDays || 0)}` : `${Number(minDays || 1)}+`; }
    function presetKeyForRange(minDays, maxDays){ const found = DAY_BRACKET_PRESETS.find((row) => String(row.key) !== 'custom' && Number(row.minDays) === Number(minDays || 0) && Number(row.maxDays) === Number(maxDays || 0)); return found ? found.key : 'custom'; }
    function vehicleCategoryByCode(code = ''){ const raw = String(code || '').trim().toUpperCase(); return vehicleCategoryRows().find((row) => String(row.category || '').trim().toUpperCase() === raw) || null; }
    function pricingSeasonByKey(key = ''){ const raw = String(key || '').trim().toLowerCase(); return pricingSeasonsRows().find((row) => String(row.key || '').trim().toLowerCase() === raw) || null; }
    function categoryLabelFromRule(rule){ const byId = rule?.vehicleCategoryId ? vehicleCategoryRows().find((row) => String(row._id || '') === String(rule.vehicleCategoryId || '')) : null; if (byId) return byId.displayTitle || byId.title || byId.category || '—'; const byCode = vehicleCategoryByCode(rule?.categoryCode); return byCode?.displayTitle || byCode?.title || byCode?.category || String(rule?.categoryCode || '—'); }
    function seasonLabelFromRule(rule){ const season = pricingSeasonByKey(rule?.seasonKey); return season?.label || season?.title || (rule?.seasonKey ? String(rule.seasonKey) : 'All year'); }
    function normalizeDateOnly(value){ const raw = String(value || '').trim(); if(!raw) return null; const dt = new Date(raw); if(Number.isNaN(dt.getTime())) return null; dt.setHours(0,0,0,0); return dt; }
    function seasonMatchesDateClient(date, season){ if(!(date instanceof Date) || Number.isNaN(date.getTime()) || !season) return false; const start = normalizeDateOnly(season.startDate); const end = normalizeDateOnly(season.endDate); if(!start || !end) return false; const check = new Date(date); check.setHours(0,0,0,0); if(season.repeatYearly){ const md = (d) => ({ m: d.getUTCMonth()+1, d: d.getUTCDate() }); const s = md(start), e = md(end), c = md(check); const sv = s.m*100 + s.d, ev = e.m*100 + e.d, cv = c.m*100 + c.d; return sv <= ev ? (cv >= sv && cv <= ev) : (cv >= sv || cv <= ev); } return check >= start && check <= end; }
    function resolveSimulatorPrice(){
      const code = String(els.simCategoryCode?.value || '').trim().toUpperCase();
      const days = Math.max(1, Number(els.simDays?.value || 1));
      const pickup = normalizeDateOnly(els.simPickupDate?.value);
      const vehicle = vehicleCategoryByCode(code);
      const fallbackPrice = Number(els.simBasePrice?.value || vehicle?.price || 0);
      const seasons = pricingSeasonsRows().filter((row) => row && row.active !== false);
      const matches = seasons.filter((row) => pickup && seasonMatchesDateClient(pickup, row));
      matches.sort((a,b) => Number(b.priority || 0) - Number(a.priority || 0) || Number(a.sortOrder || 9999) - Number(b.sortOrder || 9999));
      const season = matches[0] || null;
      const rules = categoryRateRuleRows().filter((row) => {
        if(!row || row.active === false) return false;
        const rowCode = String(row.categoryCode || '').trim().toUpperCase();
        const rowId = String(row.vehicleCategoryId || '').trim();
        if(code && rowCode && rowCode !== code) return false;
        if(code && !rowCode && rowId && String(vehicle?._id || '') && rowId !== String(vehicle?._id || '')) return false;
        const min = Math.max(1, Number(row.minDays || 1));
        const max = Math.max(0, Number(row.maxDays || 0));
        if(days < min) return false;
        if(max > 0 && days > max) return false;
        const seasonKey = String(row.seasonKey || '').trim().toLowerCase();
        if(season?.key) return !seasonKey || seasonKey === String(season.key || '').trim().toLowerCase();
        return !seasonKey;
      });
      rules.sort((a,b) => {
        const seasonA = Number(String(a?.seasonKey || '').trim().toLowerCase() === String(season?.key || '').trim().toLowerCase());
        const seasonB = Number(String(b?.seasonKey || '').trim().toLowerCase() === String(season?.key || '').trim().toLowerCase());
        if(seasonA !== seasonB) return seasonB - seasonA;
        const minDiff = Number(b?.minDays || 1) - Number(a?.minDays || 1);
        if(minDiff !== 0) return minDiff;
        const rangeA = Number(a?.maxDays || 0) || 999999;
        const rangeB = Number(b?.maxDays || 0) || 999999;
        const widthA = rangeA - Number(a?.minDays || 1);
        const widthB = rangeB - Number(b?.minDays || 1);
        if(widthA !== widthB) return widthA - widthB;
        return Number(a?.sortOrder || 9999) - Number(b?.sortOrder || 9999);
      });
      const rule = rules[0] || null;
      const pricePerDay = Number(rule?.pricePerDay || fallbackPrice || 0);
      return { vehicle, season, rule, pricePerDay, total: pricePerDay * days, fallbackPrice, days };
    }
    function accessSnapshot(){ return state.accessSnapshot || { accessState:'authRequired', currentStaff:null, roles:[], users:[], auditRows:[], sessionRows:[], bootstrapHelp:null, meta:{} }; }
    function roleRows(){ return Array.isArray(accessSnapshot().roles) ? accessSnapshot().roles : []; }
    function staffUsers(){ return Array.isArray(accessSnapshot().users) ? accessSnapshot().users : []; }
    function auditRows(){ return Array.isArray(accessSnapshot().auditRows) ? accessSnapshot().auditRows : []; }
    function sessionRows(){ return Array.isArray(accessSnapshot().sessionRows) ? accessSnapshot().sessionRows : []; }
    function findDuplicateKeys(rows){
      const counts = {};
      (rows || []).forEach((item) => { const key = String(item?.key || '').trim(); if (!key) return; counts[key] = (counts[key] || 0) + 1; });
      return Object.keys(counts).filter((key) => counts[key] > 1);
    }
    function exportSnapshot(){
      const blob = new Blob([JSON.stringify(state.snapshot || {}, null, 2)], { type:'application/json' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      const stamp = new Date().toISOString().replace(/[:.]/g, '-');
      a.download = `pricing-catalog-${stamp}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      setTimeout(() => URL.revokeObjectURL(url), 1000);
    }
    async function copySnapshot(){
      const json = JSON.stringify(state.snapshot || {}, null, 2);
      try {
        if (navigator.clipboard?.writeText) {
          await navigator.clipboard.writeText(json);
          showToast('Το JSON αντιγράφηκε στο clipboard.', 'success');
          return;
        }
      } catch(_) {}
      const area = document.createElement('textarea');
      area.value = json;
      document.body.appendChild(area);
      area.select();
      try { document.execCommand('copy'); showToast('Το JSON αντιγράφηκε στο clipboard.', 'success'); } catch(_) { showToast('Δεν έγινε copy. Χρησιμοποίησε Export JSON.', 'error'); }
      area.remove();
    }
    function duplicateItem(kind, item){
      if (!item) return;
      const cloneItem = clone(item) || {};
      delete cloneItem._id;
      if(kind === 'vehicleCategory'){
        cloneItem.title = `${String(cloneItem.title || cloneItem.displayTitle || 'Category').trim()} Copy`;
        cloneItem.category = '';
      } else if (kind === 'fleetVehicle') {
        cloneItem.plate = '';
        cloneItem.model = `${String(cloneItem.model || 'Vehicle').trim()} Copy`;
      } else if (kind === 'pricingSeason') {
        cloneItem.key = `${String(cloneItem.key || 'season').trim()}_copy`;
        cloneItem.label = `${String(cloneItem.label || 'Season').trim()} Copy`;
        cloneItem.title = cloneItem.label;
      } else if (kind === 'categoryRateRule') {
        cloneItem.key = `${String(cloneItem.key || 'rate_rule').trim()}_copy`;
        cloneItem.label = `${String(cloneItem.label || 'Rate rule').trim()} Copy`;
        cloneItem.title = cloneItem.label;
      } else {
        cloneItem.key = `${String(cloneItem.key || 'item').trim()}_copy`;
        cloneItem.label = `${String(cloneItem.label || cloneItem.title || 'Item').trim()} Copy`;
        cloneItem.title = cloneItem.label;
      }
      openEditor(kind, cloneItem);
    }
    function currentStaff(){ return accessSnapshot().currentStaff || {}; }
    function pushWorkspaceHistory(entry){
      try{
        if(!entry || !entry.route) return;
        const raw = JSON.parse(localStorage.getItem(WORKSPACE_HISTORY_KEY) || '[]');
        const list = Array.isArray(raw) ? raw.filter((item) => item && typeof item === 'object' && item.route !== entry.route) : [];
        list.unshift(entry);
        localStorage.setItem(WORKSPACE_HISTORY_KEY, JSON.stringify(list.slice(0, 6)));
      } catch(_){}
    }
    function upsertPinnedWorkspace(entry){
      try{
        if(!entry || !entry.route) return;
        const raw = JSON.parse(localStorage.getItem(PINNED_WORKSPACES_KEY) || '[]');
        const list = Array.isArray(raw) ? raw.filter((item) => item && typeof item === 'object' && item.route !== entry.route) : [];
        list.unshift(entry);
        localStorage.setItem(PINNED_WORKSPACES_KEY, JSON.stringify(list.slice(0, 6)));
      } catch(_){}
    }
    function upsertHandoffQueue(entry){ try { if (!entry || !entry.id) return; const raw = JSON.parse(localStorage.getItem(HANDOFF_QUEUE_KEY) || '[]'); const list = Array.isArray(raw) ? raw.filter((it)=> it && typeof it === 'object' && it.id !== entry.id) : []; list.unshift(entry); localStorage.setItem(HANDOFF_QUEUE_KEY, JSON.stringify(list.slice(0, 8))); } catch(_){} }
    function upsertSavedView(entry){ try { if (!entry || !entry.id) return; const raw = JSON.parse(localStorage.getItem(SAVED_VIEWS_KEY) || '[]'); const list = Array.isArray(raw) ? raw.filter((it)=> it && typeof it === 'object' && it.id !== entry.id) : []; list.unshift(entry); localStorage.setItem(SAVED_VIEWS_KEY, JSON.stringify(list.slice(0, 12))); } catch(_){} }
    function queueCurrentSettingsSection(){ const info = buildCurrentSectionDigestData(); upsertHandoffQueue({ id:`settings:${state.activeSection || 'settingsHome'}:${state.activeBusinessPane || 'frontBrand'}`, route:'settings', routeLabel:'Settings', title:info.title, subtitle:info.subtitle, summaryLine:info.summaryLine, metrics:(info.metrics || []).slice(0, 3), workspaceKey:SETTINGS_WORKSPACE_KEY, workspace:{ activeSection: state.activeSection || 'settingsHome', activeBusinessPane: state.activeBusinessPane || 'frontBrand', activeBusinessArea: state.activeBusinessArea || 'front' }, updatedAt:new Date().toISOString() }); showToast('Το current Settings section μπήκε στο handoff queue της Myroom.', 'success'); }
    function saveCurrentSettingsView(){
      const info = buildCurrentSectionDigestData();
      const defaultName = `${info.title || 'Settings'} • ${info.subtitle || 'Admin context'}`;
      const title = String(window.prompt('Όνομα για το saved view του Settings:', defaultName) || '').trim();
      if (!title) { showToast('Η αποθήκευση του saved view ακυρώθηκε.', 'error'); return; }
      upsertSavedView({ id:`saved:settings:${Date.now()}`, route:'settings', routeLabel:'Settings', title, subtitle:info.subtitle, summaryLine:info.summaryLine, metrics:(info.metrics || []).slice(0, 3), workspaceKey:SETTINGS_WORKSPACE_KEY, workspace:{ activeSection: state.activeSection || 'settingsHome', activeBusinessPane: state.activeBusinessPane || 'frontBrand', activeBusinessArea: state.activeBusinessArea || 'front' }, updatedAt:new Date().toISOString() });
      showToast('Το current Settings view αποθηκεύτηκε στη Myroom.', 'success');
    }

    function pinCurrentSettingsView(){
      const tabs = visibleTabs();
      const active = tabs.find(tab => tab.key === state.activeSection) || tabs[0] || { label:'Overview' };
      const businessPane = BUSINESS_PANES.find((pane) => pane.key === state.activeBusinessPane) || BUSINESS_PANES[0] || { shortLabel:'Business' };
      const subtitle = state.activeSection === 'business' ? `${active.label} • ${businessPane.shortLabel || businessPane.label || 'Front & company'}` : `${active.label}`;
      upsertPinnedWorkspace({ route:'settings', routeLabel:'Settings', title:'Pinned account settings', subtitle, updatedAt:new Date().toISOString() });
      showToast('Το current Settings section έγινε pin στη Myroom.', 'success');
    }

    function clearSavedWorkspaceViews(){
      [RESUME_WORKSPACE_KEY, WORKSPACE_HISTORY_KEY, SETTINGS_WORKSPACE_KEY, DAILY_WORKSPACE_KEY, FLEET_WORKSPACE_KEY, BOOKINGS_WORKSPACE_KEY, SETTINGS_SNAPSHOT_KEY, SAVED_VIEWS_KEY].forEach((key)=>{ try { localStorage.removeItem(key); } catch(_){} });
      showToast('Καθαρίστηκαν τα saved views, τα presets, το local resume history και τα settings snapshots σε αυτόν τον browser.', 'success');
    }
    function clearPinnedWorkspaceViews(){
      try { localStorage.removeItem(PINNED_WORKSPACES_KEY); } catch(_){}
      showToast('Καθαρίστηκαν τα pinned workspaces σε αυτόν τον browser.', 'success');
    }
    function buildCurrentSectionDigestData(){
      const tabs = visibleTabs();
      const active = tabs.find(tab => tab.key === state.activeSection) || tabs[0] || { label:'Overview', key:'settingsHome' };
      const businessPane = BUSINESS_PANES.find((pane) => pane.key === state.activeBusinessPane) || BUSINESS_PANES[0] || { shortLabel:'Business', label:'Business' };
      const subtitle = state.activeSection === 'business' ? `${active.label} • ${businessPane.shortLabel || businessPane.label || 'Front & company'}` : `${active.label}`;
      let summaryLine = 'Admin control center';
      let metrics = [`Visible tab: ${active.label}`];
      if(state.activeSection === 'settingsHome'){
        metrics = [
          `Vehicle categories: ${vehicleCategoryRows().length}`,
          `Fleet vehicles: ${fleetVehicleRows().length}`,
          `Locations: ${pickupLocationRows().length}`,
          `Dynamic seasons: ${pricingSeasonsRows().length}`,
          `Rate rules: ${categoryRateRuleRows().length}`
        ];
        summaryLine = `Catalog ${vehicleCategoryRows().length} / Vehicles ${fleetVehicleRows().length} / Roles ${Array.isArray(state.accessSnapshot?.roles) ? state.accessSnapshot.roles.length : 0}`;
      } else if(state.activeSection === 'business'){
        metrics = [
          `Pane: ${businessPane.shortLabel || businessPane.label || 'Front & company'}`,
          `Locations: ${pickupLocationRows().length}`,
          `Vehicle catalog rows: ${fleetVehicleRows().length}`,
          `Insurance texts ready: ${insuranceRows().length}`
        ];
        summaryLine = `${businessPane.shortLabel || businessPane.label || 'Front & company'} • locations ${pickupLocationRows().length}`;
      } else if(state.activeSection === 'fleetCategories'){
        metrics = [`Categories: ${vehicleCategoryRows().length}`, `Display mode: ${businessSettings().vehiclesDisplayMode || 'categories'}`];
        summaryLine = `Fleet categories • ${vehicleCategoryRows().length} rows`;
      } else if(state.activeSection === 'fleetVehicles'){
        metrics = [`Vehicles: ${fleetVehicleRows().length}`, `Vehicle page source: ${businessSettings().vehiclesDisplayMode === 'models' ? 'Fleet models' : 'Vehicles categories'}`];
        summaryLine = `Fleet vehicles • ${fleetVehicleRows().length} rows`;
      } else if(state.activeSection === 'dynamicPricing'){
        metrics = [`Seasons: ${pricingSeasonsRows().length}`, `Rules: ${categoryRateRuleRows().length}`];
        summaryLine = `Dynamic pricing • seasons ${pricingSeasonsRows().length} • rules ${categoryRateRuleRows().length}`;
      } else if(state.activeSection === 'insurance'){
        metrics = [`Plans: ${insuranceRows().length}`];
        summaryLine = `Insurance plans • ${insuranceRows().length} rows`;
      } else if(state.activeSection === 'extras'){
        metrics = [`Extras: ${extraRows().length}`];
        summaryLine = `Extra services • ${extraRows().length} rows`;
      } else if(state.activeSection === 'fees'){
        metrics = [`Fees: ${feeRows().length}`];
        summaryLine = `Fee rules • ${feeRows().length} rows`;
      } else if(state.activeSection === 'roles'){
        metrics = [`Roles: ${Array.isArray(state.accessSnapshot?.roles) ? state.accessSnapshot.roles.length : 0}`];
        summaryLine = `Roles & permissions • ${Array.isArray(state.accessSnapshot?.roles) ? state.accessSnapshot.roles.length : 0} roles`;
      } else if(state.activeSection === 'users'){
        metrics = [`Users: ${Array.isArray(state.accessSnapshot?.users) ? state.accessSnapshot.users.length : 0}`];
        summaryLine = `Users • ${Array.isArray(state.accessSnapshot?.users) ? state.accessSnapshot.users.length : 0} staff users`;
      } else if(state.activeSection === 'security'){
        metrics = [`Sessions: ${Array.isArray(state.accessSnapshot?.sessionRows) ? state.accessSnapshot.sessionRows.length : 0}`];
        summaryLine = `Security console • ${Array.isArray(state.accessSnapshot?.sessionRows) ? state.accessSnapshot.sessionRows.length : 0} session rows`;
      } else if(state.activeSection === 'audit'){
        metrics = [`Audit rows: ${Array.isArray(state.accessSnapshot?.auditRows) ? state.accessSnapshot.auditRows.length : 0}`];
        summaryLine = `Change history • ${Array.isArray(state.accessSnapshot?.auditRows) ? state.accessSnapshot.auditRows.length : 0} audit rows`;
      }
      return { route:'settings', routeLabel:'Settings', title:'Account settings', subtitle, summaryLine, metrics, updatedAt:new Date().toISOString() };
    }
    function writeSettingsSnapshot(){ return; }
    function buildCurrentSectionDigest(){ const info = buildCurrentSectionDigestData(); return ['Settings section digest', `${info.title} • ${info.subtitle}`, info.summaryLine, ...info.metrics].filter(Boolean).join('\n'); }
    function persistSettingsWorkspace(){
      try{ localStorage.setItem(SETTINGS_WORKSPACE_KEY, JSON.stringify({ activeSection: state.activeSection || 'settingsHome', activeBusinessPane: state.activeBusinessPane || 'frontBrand', activeBusinessArea: state.activeBusinessArea || 'front' })); } catch(_){}
      try{
        const info = buildCurrentSectionDigestData();
        const entry = { route:'settings', routeLabel:'Settings', title:'Account settings', subtitle:info.subtitle, updatedAt:new Date().toISOString() };
        localStorage.setItem(RESUME_WORKSPACE_KEY, JSON.stringify(entry));
        pushWorkspaceHistory(entry);
      } catch(_){}
      writeSettingsSnapshot();
    }
    function specialDescriptions(){
      const rows = Array.isArray(accessSnapshot().meta?.specialPermissionDescriptions) ? accessSnapshot().meta.specialPermissionDescriptions : [];
      const map = new Map(rows.map((item) => [String(item.key || ''), String(item.description || '')]));
      SPECIAL_PERMISSIONS.forEach((key) => { if(!map.has(key)) map.set(key, SPECIAL_PERMISSION_LABELS[key] || ''); });
      return map;
    }
    function currentSpecials(){ return Array.isArray(currentStaff().specialPermissions) ? currentStaff().specialPermissions : []; }
    function canManageStaff(){ return !!(currentStaff().canManageStaff || isAdmin()); }
    function canViewAudit(){ return !!(currentStaff().canViewAudit || isAdmin()); }
    function canManageBusinessSettings(){ return !!(currentStaff().canManageBusinessSettings || isAdmin()); }
    function canOverridePricing(){ return !!(currentStaff().canOverridePricing || isAdmin()); }
    function canApproveManualOverride(){ return !!(currentStaff().canApproveManualOverride || isAdmin()); }
    function tryParseJson(value){
      const raw = String(value || '').trim();
      if(!raw) return null;
      try { return JSON.parse(raw); } catch(_) { return raw; }
    }
    function prettyJson(value){
      const parsed = typeof value === 'string' ? tryParseJson(value) : value;
      if(parsed === null || parsed === undefined || parsed === '') return '—';
      if(typeof parsed === 'string') return parsed;
      try { return JSON.stringify(parsed, null, 2); } catch(_) { return String(parsed); }
    }
    function renderSpecialPills(keys = []){
      if(!keys.length) return '<span style="color:var(--muted)">No special permissions</span>';
      const descMap = specialDescriptions();
      return `<div class="pill-list">${keys.map((key) => `<span class="pill good" title="${esc(descMap.get(key) || '')}">${esc(SPECIAL_PERMISSION_LABELS[key] || key)}</span>`).join('')}</div>`;
    }
    function openInspector(kind, item){
      state.inspector = { open:true, kind, item: clone(item) || null };
      let title='Inspector';
      let sub='';
      let body='';
      let actions='';
      if(kind === 'fleetVehicle'){
        const categoryOptions = vehicleCategoryRows().map(row => `<option value="${esc(row.category || '')}" ${String(item.category || '') === String(row.category || '') ? 'selected' : ''}>${esc(row.displayTitle || row.title || row.category || '')}</option>`).join('');
        return `
          <div class="field col-4"><label>Πινακίδα</label><input data-field="plate" value="${esc(item.plate || '')}" placeholder="ΝΗΧ1234" /></div>
          <div class="field col-5"><label>Μοντέλο</label><input data-field="model" value="${esc(item.model || '')}" placeholder="Hyundai i10" /></div>
          <div class="field col-3"><label>Category</label>${categoryOptions ? `<select data-field="category">${categoryOptions}</select>` : `<input data-field="category" value="${esc(item.category || '')}" placeholder="A" />`}</div>
          <div class="field col-4"><label>Status</label><select data-field="status"><option value="available" ${String(item.status || '') === 'available' ? 'selected' : ''}>available</option><option value="rented" ${String(item.status || '') === 'rented' ? 'selected' : ''}>rented</option><option value="service" ${String(item.status || '') === 'service' ? 'selected' : ''}>service</option><option value="inactive" ${String(item.status || '') === 'inactive' ? 'selected' : ''}>inactive</option></select></div>
          <div class="field col-8"><label>Notes</label><textarea data-field="notes">${esc(item.notes || '')}</textarea></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-12"><label>Preview label</label><input value="${esc((((item.plate || '').trim() ? ((item.plate || '').trim() + ((item.model || '').trim() ? ` - ${(item.model || '').trim()}` : '')) : ((item.model || '').trim() || ''))) + (((item.category || '').trim()) ? ` (${(item.category || '').trim()})` : ''))}" readonly /></div>`;
      }
      if(kind === 'role'){
        const role = item || {};
        const rows = ROLE_MODULES.map((moduleKey) => `<tr><td><strong>${esc(moduleKey)}</strong></td>${ROLE_ACTIONS.map((action) => `<td style="text-align:center">${role?.permissions?.[moduleKey]?.[action] ? '✅' : '—'}</td>`).join('')}</tr>`).join('');
        title = `Role matrix · ${role.label || role.key || 'role'}`;
        sub = 'Αναλυτική προβολή permissions και special permissions.';
        body = `<div class="viewer-stack"><div class="viewer-card"><h4>Role details</h4><div class="kv-grid"><div><strong>Key</strong><div>${esc(role.key || '—')}</div></div><div><strong>Status</strong><div>${role.active !== false ? 'Active' : 'Inactive'}</div></div><div><strong>System role</strong><div>${role.systemRole ? 'Yes' : 'No'}</div></div><div><strong>Sort order</strong><div>${esc(role.sortOrder || '—')}</div></div></div><div style="margin-top:10px;color:var(--muted)">${esc(role.description || '')}</div><h4 style="margin-top:14px">Special permissions</h4>${renderSpecialPills(role.specialPermissionsList || [])}</div><div class="viewer-card"><h4>Permissions window</h4><div class="table-wrap"><table><thead><tr><th>Module</th><th>View</th><th>Insert</th><th>Edit</th><th>Delete</th></tr></thead><tbody>${rows}</tbody></table></div></div></div>`;
      } else if(kind === 'user'){
        const user = item || {};
        title = `Staff user · ${user.fullName || user.email || 'user'}`;
        sub = 'Σύνοψη λογαριασμού, credentials και active sessions.';
        body = `<div class="viewer-stack"><div class="viewer-card"><h4>Profile</h4><div class="kv-grid"><div><strong>Email</strong><div>${esc(user.email || '—')}</div></div><div><strong>Role</strong><div>${esc(user.roleKey || '—')}</div></div><div><strong>Status</strong><div>${user.active !== false ? 'Active' : 'Inactive'}</div></div><div><strong>Credential</strong><div>${user.hasCredential ? (user.mustChangePassword ? 'Temp / must change' : 'Ready') : 'Not set'}</div></div><div><strong>Updated password</strong><div>${user.passwordSetAt ? esc(new Date(user.passwordSetAt).toLocaleString('el-GR')) : '—'}</div></div><div><strong>Active sessions</strong><div>${Number(user.activeSessionCount || 0)}</div></div></div><div style="margin-top:10px;color:var(--muted)">${esc(user.notes || '')}</div></div><div class="viewer-card"><h4>Quick actions</h4><div class="pill-list"><span class="pill ${user.hasCredential ? 'good':'off'}">${user.hasCredential ? 'Credential ready' : 'No credential'}</span><span class="pill ${user.active !== false ? 'good' : 'off'}">${user.active !== false ? 'Active user' : 'Inactive user'}</span><span class="pill ${(user.activeSessionCount||0) ? 'warn':'off'}">${Number(user.activeSessionCount || 0)} active sessions</span></div><div style="margin-top:12px;color:var(--muted)">Για invite / reset / revoke session χρησιμοποίησε τα actions στον πίνακα χρηστών ή το Security tab.</div></div></div>`;
      } else {
        const audit = item || {};
        const parsedOld = prettyJson(audit.oldValue);
        const parsedNew = prettyJson(audit.newValue);
        const canIssueRecovery = String(audit.entityType || '') === 'accessRecovery' && String(audit.action || '') === 'requestRecovery' && String(audit.entityId || '').includes('@') && canManageStaff();
        title = `Audit entry · ${audit.action || 'entry'}`;
        sub = 'Λεπτομέρειες change history, actor και payload diff.';
        body = `<div class="viewer-stack"><div class="viewer-card"><h4>Summary</h4><div class="kv-grid"><div><strong>Date</strong><div>${audit.createdAt ? esc(new Date(audit.createdAt).toLocaleString('el-GR')) : '—'}</div></div><div><strong>Actor</strong><div>${esc(audit.actorName || audit.actorEmail || '—')}</div></div><div><strong>Actor email</strong><div>${esc(audit.actorEmail || '—')}</div></div><div><strong>Entity</strong><div>${esc(audit.entityType || '—')}</div></div><div><strong>Entity id</strong><div>${esc(audit.entityId || '—')}</div></div><div><strong>Action</strong><div>${esc(audit.action || '—')}</div></div></div><div style="margin-top:12px"><strong>Summary</strong><div>${esc(audit.summary || '—')}</div></div></div><div class="viewer-card"><h4>Payload compare</h4><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px"><div><div style="font-weight:700;margin-bottom:6px">Old value</div><pre class="codebox">${esc(parsedOld)}</pre></div><div><div style="font-weight:700;margin-bottom:6px">New value</div><pre class="codebox">${esc(parsedNew)}</pre></div></div></div></div>`;
        if(canIssueRecovery){
          actions = `<button class="btn warn" id="inspectorIssueResetBtn">Issue reset</button>`;
        }
      }
      els.inspectorTitle.textContent = title;
      els.inspectorSub.textContent = sub;
      els.inspectorBody.innerHTML = body;
      els.inspectorActions.innerHTML = actions;
      if(document.getElementById('inspectorIssueResetBtn')){
        document.getElementById('inspectorIssueResetBtn').addEventListener('click', () => {
          const email = String(item?.entityId || '').trim();
          if(!email) return;
          state.securityEmail = email.toLowerCase();
          closeInspector();
          setBusy(true);
          post('resetStaffPassword', { payload: { email } });
        });
      }
      els.inspectorOverlay.classList.add('show');
      requestResize();
    }
    function closeInspector(){ state.inspector = { open:false, kind:'', item:null }; els.inspectorOverlay.classList.remove('show'); }

    function renderAccessGate(){
      const snap = accessSnapshot();
      const current = snap.currentStaff || null;
      if(els.accessGate){ els.accessGate.style.display = 'none'; els.accessGate.innerHTML = ''; }
      if(snap.accessState === 'ready'){
        if(els.accessSummary){
          els.accessSummary.innerHTML = `<h4>Access status</h4><div class="access-grid"><div class="mini"><div class="label">Current user</div><div class="value">${esc(current?.name || current?.email || '—')}</div></div><div class="mini"><div class="label">Custom role</div><div class="value">${esc(current?.roleLabel || '—')}${current?.isAdmin ? ' · FULL ACCESS' : ''}</div></div><div class="mini"><div class="label">Special permissions</div><div class="value">${esc((current?.specialPermissions || []).length)}</div></div><div class="mini"><div class="label">Session expires</div><div class="value">${esc(current?.sessionExpiresAt ? new Date(current.sessionExpiresAt).toLocaleString('el-GR') : '—')}</div></div></div><div class="pill-list" style="margin-top:10px"><span class="pill ${canManageBusinessSettings() ? 'good' : 'off'}">Business settings</span><span class="pill ${canManageStaff() ? 'good' : 'off'}">Manage staff</span><span class="pill ${canOverridePricing() ? 'good' : 'off'}">Override pricing</span><span class="pill ${canApproveManualOverride() ? 'good' : 'off'}">Approve overrides</span><span class="pill ${canViewAudit() ? 'good' : 'off'}">Audit / change history</span></div>${renderSpecialPills(current?.specialPermissions || [])}<div class="footnote">${esc(snap.meta?.note || '')}</div>`;
        }
        els.sections.forEach(sec => sec.style.display = '');
        return;
      }
      if(els.accessSummary){
        els.accessSummary.innerHTML = `<h4>${snap.accessState === 'authRequired' ? 'Backroom login required' : 'Access denied'}</h4><div class="sub" style="font-size:13px">${snap.accessState === 'authRequired' ? 'Για να μπεις στα Business Settings & Preferences πρέπει πρώτα να κάνεις custom login από το Myroom.' : 'Ο χρήστης δεν υπάρχει ως ενεργός staff user ή ο ρόλος του δεν έχει δικαίωμα Pricing / Manage Staff.'}</div><div class="hero-actions" style="margin-top:12px"><button class="btn" id="gateRefreshBtn" data-allow-busy="1">Refresh</button></div>`;
      }
      els.sections.forEach(sec => sec.style.display = '');
      const btn = document.getElementById('gateRefreshBtn');
      if(btn) btn.addEventListener('click', () => { setBusy(true); post('pricingAdminReady', {}); });
    }

    function renderHealth(){
      const insurance = insuranceRows();
      const extras = extraRows();
      const fees = feeRows();
      const dynamicSeasons = Array.isArray(state.snapshot?.pricingSeasons) ? state.snapshot.pricingSeasons : [];
      const dynamicRateRules = Array.isArray(state.snapshot?.categoryRateRules) ? state.snapshot.categoryRateRules : [];
      const duplicateCount = findDuplicateKeys(insurance).length + findDuplicateKeys(extras).length + findDuplicateKeys(fees).length;
      const hiddenPublic = [...insurance, ...extras, ...fees].filter((item) => item.active === false && item.publicVisible !== false).length;
      const privateItems = [...insurance, ...extras, ...fees].filter((item) => item.publicVisible === false).length;
      const pendingRecovery = Number(accessSnapshot().meta?.pendingRecoveryCount || 0);
      const settings = businessSettings();
      const emailsEnabled = !!settings.enableTriggeredEmails;
      const hasMissingEmailTriggers = emailsEnabled && (!String(settings.bookingConfirmedTriggerId || '').trim() || !String(settings.bookingCanceledTriggerId || '').trim());
      const rows = [
        { title:'Duplicate keys', value: duplicateCount, note:'Πρέπει να είναι 0 για καθαρό catalog.', warn: duplicateCount > 0 },
        { title:'Inactive αλλά public', value: hiddenPublic, note:'Κλείσε publicVisible όταν απενεργοποιείς item.', warn: hiddenPublic > 0 },
        { title:'Private admin-only items', value: privateItems, note:'Χρήσιμο για fees που δεν θες στο public booking flow.', warn: false },
        { title:'Dynamic pricing seasons', value: dynamicSeasons.length, note:'Backend έτοιμο για seasonal pricing windows.', warn: false },
        { title:'Category rate rules', value: dynamicRateRules.length, note:'Κανόνες ημερών/κατηγορίας για dynamic pricing.', warn: false },
        { title:'Overbooking mode', value: settings.allowOverbooking ? 'ON' : 'OFF', note:'Καλό είναι να μένει OFF εκτός αν το θες συνειδητά.', warn: !!settings.allowOverbooking },
        { title:'Triggered emails', value: !emailsEnabled ? 'Disabled' : (hasMissingEmailTriggers ? 'Missing IDs' : 'Ready'), note:'Άφησέ το Disabled αν δεν χρησιμοποιείς Wix Triggered Emails ακόμη.', warn: hasMissingEmailTriggers },
        { title:'Vehicles page mode', value: settings.vehiclesPageDisplayMode === 'models' ? `Μοντέλα · ${settings.vehiclesPageModelsSource === 'vehicles' ? 'Vehicles' : 'Fleet'}` : 'Κατηγορίες', note:'Ελέγχει αν η σελίδα Vehicles δείχνει κατηγορίες ή grouped μοντέλα.', warn: false },
        { title:'Pending recovery requests', value: pendingRecovery, note:'Αιτήματα πρόσβασης από το Myroom login panel.', warn: pendingRecovery > 0 }
      ];
      els.healthList.innerHTML = rows.map((row) => `<div class="health-item ${row.warn ? 'warn' : ''}"><div><b>${esc(row.title)}</b><span>${esc(row.note)}</span></div><strong>${esc(row.value)}</strong></div>`).join('');
    }

    function renderKpis(){
      const settings = businessSettings();
      const currency = settings.currency || 'EUR';
      const categories = vehicleCategoryRows();
      const insurance = insuranceRows();
      const extras = extraRows();
      const activeCategories = categories.filter(item => item.active !== false).length;
      const activeInsurance = insurance.filter(item => item.active !== false).length;
      const activeExtras = extras.filter(item => item.active !== false).length;
      els.kpis.innerHTML = [
        { big: activeCategories, small:'Ενεργές κατηγορίες οχημάτων' },
        { big: activeInsurance, small:'Ενεργά insurance plans' },
        { big: activeExtras, small:'Ενεργά extras' },
        { big: money(settings.defaultDeposit || 0, currency), small:'Default deposit' }
      ].map(k => `<div class="kpi"><b>${esc(k.big)}</b><span>${esc(k.small)}</span></div>`).join('');
      els.snapCurrency.textContent = settings.currency || 'EUR';
      els.snapVat.textContent = `${Number(settings.vatRate || 0)}%`;
      els.snapNight.textContent = `${settings.nightStartHour ?? '—'}:00 → ${settings.nightEndHour ?? '—'}:00`;
      els.snapDeposit.textContent = money(settings.defaultDeposit || 0, currency);
    }


    function renderVehicleCatalogPreview(){
      const display = String(document.getElementById('bsVehiclesPageDisplayMode')?.value || businessSettings().vehiclesPageDisplayMode || 'categories');
      const source = String(document.getElementById('bsVehiclesPageModelsSource')?.value || businessSettings().vehiclesPageModelsSource || 'fleet');
      const titleEl = document.getElementById('vehicleCatalogPreviewTitle');
      const copyEl = document.getElementById('vehicleCatalogPreviewCopy');
      const sourceEl = document.getElementById('vehicleCatalogPreviewSource');
      const sourceCopyEl = document.getElementById('vehicleCatalogPreviewSourceCopy');
      const metaEl = document.getElementById('vehicleCatalogPreviewMeta');
      const listEl = document.getElementById('vehicleCatalogPreviewList');
      if(!titleEl || !copyEl || !sourceEl || !sourceCopyEl || !metaEl || !listEl) return;
      const categories = vehicleCategoryRows().filter(item => item.active !== false);
      const fleet = fleetVehicleRows().filter(item => item.active !== false);
      titleEl.textContent = display === 'models' ? 'Preview · Μοντέλα' : 'Preview · Κατηγορίες';
      copyEl.textContent = display === 'models'
        ? 'Ο πελάτης βλέπει ένα listing με πραγματικά μοντέλα οχημάτων, ανάλογα με το source που έχει επιλεγεί.'
        : 'Ο πελάτης βλέπει ενεργές κατηγορίες με title, εικόνα και pricing βάση του operations catalog.';
      sourceEl.textContent = `Source · ${source === 'fleet' ? 'Fleet' : 'Vehicles collection'}`;
      sourceCopyEl.textContent = source === 'fleet'
        ? 'Το listing τραβά grouped μοντέλα από τον ενεργό στόλο για πιο operational preview.'
        : 'Το listing τραβά front-facing εγγραφές από τη δομή Vehicles όταν επιλέγεις model mode.';
      metaEl.textContent = `${display === 'models' ? 'Models' : 'Categories'} · ${source === 'fleet' ? 'Fleet' : 'Vehicles'}`;
      let cards = [];
      if(display === 'models'){
        if(source === 'fleet'){
          const seen = new Set();
          cards = fleet.map(item => String(item.model || item.title || item.vehicleModel || '').trim()).filter(Boolean).filter(name => {
            const key = name.toLowerCase();
            if(seen.has(key)) return false;
            seen.add(key);
            return true;
          }).slice(0,6);
        } else {
          cards = categories.map(item => String(item.title || item.name || item.category || '').trim()).filter(Boolean).slice(0,6);
        }
      } else {
        cards = categories.map(item => String(item.title || item.name || item.category || '').trim()).filter(Boolean).slice(0,6);
      }
      if(!cards.length){
        listEl.innerHTML = '<div class="info-card" style="grid-column:1/-1">Δεν υπάρχουν ακόμα αρκετά ενεργά records για preview. Πρόσθεσε categories ή vehicles/fleet entries και ξαναδοκίμασε.</div>';
        return;
      }
      listEl.innerHTML = cards.map((name, idx) => `
        <div class="inline-callout">
          <h4>${display === 'models' ? 'Model' : 'Category'} ${idx + 1}</h4>
          <p><strong>${esc(name)}</strong><br>${display === 'models' ? 'Προβολή τύπου μοντέλου στη Vehicles page.' : 'Προβολή κατηγορίας στη δημόσια λίστα οχημάτων.'}</p>
        </div>
      `).join('');
    }

    function setBusinessForm(){
      const s = stationScopedBusinessSettings();
      state.stationProfiles = stationProfilesFromBusiness();
      document.getElementById('bsTitle').value = s.title || '';
      document.getElementById('bsCurrency').value = s.currency || 'EUR';
      document.getElementById('bsVatRate').value = Number(s.vatRate || 0);
      document.getElementById('bsNightStartHour').value = Number(s.nightStartHour || 0);
      document.getElementById('bsNightEndHour').value = Number(s.nightEndHour || 0);
      document.getElementById('bsDefaultDeposit').value = Number(s.defaultDeposit || 0);
      document.getElementById('bsCompanyName').value = s.companyName || '';
      document.getElementById('bsCompanyCity').value = s.companyCity || '';
      document.getElementById('bsCompanyTagline').value = s.companyTagline || '';
      document.getElementById('bsCompanyAddress').value = s.companyAddress || '';
      document.getElementById('bsFooterNote').value = s.footerNote || '';
      document.getElementById('bsCompanyPhone').value = s.companyPhone || '';
      document.getElementById('bsCompanyEmail').value = s.companyEmail || '';
      document.getElementById('bsOperatingHoursLabel').value = s.operatingHoursLabel || '';
      document.getElementById('bsAfterHoursNotice').value = s.afterHoursNotice || '';
      document.getElementById('bsAllowOverbooking').checked = !!s.allowOverbooking;
      document.getElementById('bsEnableTriggeredEmails').checked = !!s.enableTriggeredEmails;
      document.getElementById('bsVehiclesPageDisplayMode').value = s.vehiclesPageDisplayMode || 'categories';
      document.getElementById('bsVehiclesPageModelsSource').value = s.vehiclesPageModelsSource || 'fleet';
      document.getElementById('bsBookingConfirmedTriggerId').value = s.bookingConfirmedTriggerId || '';
      document.getElementById('bsBookingCanceledTriggerId').value = s.bookingCanceledTriggerId || '';
      document.getElementById('bsInsuranceTermsTitle').value = s.insuranceTermsTitle || '';
      document.getElementById('bsInsuranceTermsIntro').value = s.insuranceTermsIntro || '';
      document.getElementById('bsInsuranceTermsBody').value = s.insuranceTermsBody || '';
      document.getElementById('bsInsuranceDetailsTitle').value = s.insuranceDetailsTitle || '';
      document.getElementById('bsInsuranceDetailsIntro').value = s.insuranceDetailsIntro || '';
      document.getElementById('bsInsuranceDetailsBody').value = s.insuranceDetailsBody || '';
      document.getElementById('bsRentalTermsTitle').value = s.rentalTermsTitle || '';
      document.getElementById('bsRentalTermsIntro').value = s.rentalTermsIntro || '';
      document.getElementById('bsRentalTermsBody').value = s.rentalTermsBody || '';
      document.getElementById('bsRentalRequirementsBody').value = s.rentalRequirementsBody || '';
      document.getElementById('bsRentalPoliciesBody').value = s.rentalPoliciesBody || '';
      document.getElementById('bsRentalPrivacyBody').value = s.rentalPrivacyBody || '';
      document.getElementById('bsActive').checked = s.active !== false;
      document.querySelectorAll('textarea[data-editor="modal"]').forEach(syncRichPreview);
      renderVehicleCatalogPreview();
      if(state.activeSection === 'business') renderBusinessPaneNav();
    }

    function businessPayload(){
      const current = businessSettings();
      const stationKey = String(state.activeBusinessStationKey || 'default');
      const stationSettings = { ...(stationSettingsMap() || {}) };
      const scopedValues = {
        title: document.getElementById('bsTitle').value.trim(),
        currency: document.getElementById('bsCurrency').value.trim() || 'EUR',
        vatRate: Number(document.getElementById('bsVatRate').value || 0),
        nightStartHour: Number(document.getElementById('bsNightStartHour').value || 0),
        nightEndHour: Number(document.getElementById('bsNightEndHour').value || 0),
        defaultDeposit: Number(document.getElementById('bsDefaultDeposit').value || 0),
        companyName: document.getElementById('bsCompanyName').value.trim(),
        companyCity: document.getElementById('bsCompanyCity').value.trim(),
        companyTagline: document.getElementById('bsCompanyTagline').value.trim(),
        companyAddress: document.getElementById('bsCompanyAddress').value.trim(),
        footerNote: document.getElementById('bsFooterNote').value.trim(),
        companyPhone: document.getElementById('bsCompanyPhone').value.trim(),
        companyEmail: document.getElementById('bsCompanyEmail').value.trim(),
        operatingHoursLabel: document.getElementById('bsOperatingHoursLabel').value.trim(),
        afterHoursNotice: document.getElementById('bsAfterHoursNotice').value.trim(),
        allowOverbooking: !!document.getElementById('bsAllowOverbooking').checked,
        enableTriggeredEmails: !!document.getElementById('bsEnableTriggeredEmails').checked,
        vehiclesPageDisplayMode: document.getElementById('bsVehiclesPageDisplayMode').value.trim() || 'categories',
        vehiclesPageModelsSource: document.getElementById('bsVehiclesPageModelsSource').value.trim() || 'fleet',
        bookingConfirmedTriggerId: document.getElementById('bsBookingConfirmedTriggerId').value.trim(),
        bookingCanceledTriggerId: document.getElementById('bsBookingCanceledTriggerId').value.trim(),
        insuranceTermsTitle: document.getElementById('bsInsuranceTermsTitle').value.trim(),
        insuranceTermsIntro: document.getElementById('bsInsuranceTermsIntro').value.trim(),
        insuranceTermsBody: document.getElementById('bsInsuranceTermsBody').value.trim(),
        insuranceDetailsTitle: document.getElementById('bsInsuranceDetailsTitle').value.trim(),
        insuranceDetailsIntro: document.getElementById('bsInsuranceDetailsIntro').value.trim(),
        insuranceDetailsBody: document.getElementById('bsInsuranceDetailsBody').value.trim(),
        rentalTermsTitle: document.getElementById('bsRentalTermsTitle').value.trim(),
        rentalTermsIntro: document.getElementById('bsRentalTermsIntro').value.trim(),
        rentalTermsBody: document.getElementById('bsRentalTermsBody').value.trim(),
        rentalRequirementsBody: document.getElementById('bsRentalRequirementsBody').value.trim(),
        rentalPoliciesBody: document.getElementById('bsRentalPoliciesBody').value.trim(),
        rentalPrivacyBody: document.getElementById('bsRentalPrivacyBody').value.trim(),
        stationProfiles: effectiveStationProfiles(),
        active: !!document.getElementById('bsActive').checked
      };
      stationSettings[stationKey] = { ...(stationSettings[stationKey] || {}), ...scopedValues };
      return {
        _id: current._id,
        ...scopedValues,
        stationSettings,
        stationProfiles: effectiveStationProfiles(),
      };
    }

    function filterList(list, text, extraFn){
      const q = String(text || '').trim().toLowerCase();
      return list.filter(item => {
        const match = !q || [item.key, item.label, item.title, item.displayTitle, item.description, item.ruleType, item.audienceGroup, item.category, item.categoryCode, item.seasonKey, item.transmission, item.fuelType, item.plate, item.model, item.address, item.city, item.locationType, item.stationKey, item.stationLabel, item.station].some(v => String(v || '').toLowerCase().includes(q));
        return match && (!extraFn || extraFn(item));
      });
    }

    function renderVehicleCategoriesTable(){
      const rows = filterList(vehicleCategoryRows(), state.filters.vehicleCategories);
      els.vehicleCategoriesCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.vehicleCategoriesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν κατηγορίες οχημάτων.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.vehicleCategoriesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Κατηγορία</th><th>Code</th><th>Price / day</th><th>Specs</th><th>Status</th><th>Active</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(normalizedVehicleCategoryTitle(item))}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(item.category || '—')}</td>
          <td>${money(item.price || 0, currency)}</td>
          <td>${esc([item.transmission, item.fuelType, item.seats ? `${item.seats} seats` : '', item.doors ? `${item.doors} doors` : '', item.luggage ? `${item.luggage} luggage` : ''].filter(Boolean).join(' · ') || '—')}</td>
          <td>${esc(item.status || 'active')}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 0)}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="vehicleCategory" data-id="${esc(item._id || item.category)}">Edit</button><button class="link" data-duplicate-kind="vehicleCategory" data-id="${esc(item._id || item.category)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.vehicleCategoriesWrap, vehicleCategoryRows());
      enhanceTable(els.vehicleCategoriesWrap, 'fleetCategories');
    }

    function renderFleetVehiclesTable(){
      const rows = filterList(fleetVehicleRows(), state.filters.fleetVehicles);
      els.fleetVehiclesCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.fleetVehiclesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν οχήματα στόλου.</div>'; return; }
      els.fleetVehiclesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Πινακίδα</th><th>Μοντέλο</th><th>Category</th><th>Status</th><th>Active</th><th>Notes</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.plate || '—')}</strong></td>
          <td>${esc(item.model || '—')}</td>
          <td>${esc(normalizedFleetCategoryLabel(item))}</td>
          <td>${esc(item.status || 'available')}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${esc(item.notes || '—')}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="fleetVehicle" data-id="${esc(item._id || item.plate)}">Edit</button><button class="link" data-duplicate-kind="fleetVehicle" data-id="${esc(item._id || item.plate)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.fleetVehiclesWrap, fleetVehicleRows());
      enhanceTable(els.fleetVehiclesWrap, 'fleetVehicles');
    }


    function renderStationsLocationsTabs(){
      const stationCount = effectiveStationProfiles().length;
      const locationCount = pickupLocationRows().length;
      const tabs = [
        { key:'stations', label:`Σταθμοί (${stationCount})`, short:'Σταθμοί', hint:'Διαχείριση station profiles με πίνακα και ενέργειες επεξεργασίας.' },
        { key:'locations', label:`Τοποθεσίες (${locationCount})`, short:'Τοποθεσίες', hint:'Pickup/dropoff locations με station φίλτρο, sorting και κατάσταση.' }
      ];
      if (!tabs.some((tab)=>tab.key === state.locationsWorkspaceTab)) state.locationsWorkspaceTab = 'stations';
      if (els.stationsLocationsTabs) {
        els.stationsLocationsTabs.innerHTML = `<div class="business-subtabs-left">${tabs.map((tab)=>`<button class="business-subtab ${state.locationsWorkspaceTab === tab.key ? 'active' : ''}" data-locations-workspace-tab="${tab.key}">${tab.label}</button>`).join('')}</div>`;
      }
      if (els.stationsLocationsNote) {
        const current = tabs.find((tab)=>tab.key === state.locationsWorkspaceTab) || tabs[0];
        els.stationsLocationsNote.innerHTML = `<span>${current.hint}</span><strong>${current.short}</strong>`;
      }
      if (els.stationsWorkspacePane) els.stationsWorkspacePane.style.display = state.locationsWorkspaceTab === 'stations' ? '' : 'none';
      if (els.locationsWorkspacePane) els.locationsWorkspacePane.style.display = state.locationsWorkspaceTab === 'locations' ? '' : 'none';
    }

    function renderStationWorkspace(){
      const profiles = effectiveStationProfiles();
      const scope = String(state.locationStationScope || 'all');
      if (els.stationProfilesCount) els.stationProfilesCount.textContent = `${profiles.length} stations`;
      if (!profiles.some((row)=>row.key === state.activeBusinessStationKey)) state.activeBusinessStationKey = profiles[0]?.key || 'default';
      if (els.locationStationScope) {
        const prev = scope;
        els.locationStationScope.innerHTML = `<option value="all">Όλοι οι σταθμοί</option>` + profiles.map((row) => `<option value="${esc(row.key)}">${esc(row.label)} (${esc(row.key)})</option>`).join('');
        if ([...els.locationStationScope.options].some((opt)=>opt.value === prev)) els.locationStationScope.value = prev;
        else els.locationStationScope.value = 'all';
        state.locationStationScope = els.locationStationScope.value;
      }
      renderStationsLocationsTabs();
    }

    function renderStationProfilesTable(){
      const rows = filterList(effectiveStationProfiles(), state.filters.stationProfiles)
        .sort((a, b) => Number(a.sortOrder || 9999) - Number(b.sortOrder || 9999) || String(a.label || '').localeCompare(String(b.label || ''), 'el'));
      if (els.stationProfilesCount) els.stationProfilesCount.textContent = `${rows.length} stations`;
      if (!els.stationProfilesWrap) return;
      if(!rows.length){ els.stationProfilesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν σταθμοί.</div>'; return; }
      els.stationProfilesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Σταθμός</th><th>City / Address</th><th>Description</th><th>Linked locations</th><th>Status</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => {
        const linked = pickupLocationRows().filter((row) => String(row.stationKey || row.station || 'default').trim().toLowerCase() === String(item.key || '').trim().toLowerCase()).length;
        return `
        <tr>
          <td><strong>${esc(item.label || 'Κεντρικός σταθμός')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.key || 'default')}</div></td>
          <td>${esc(item.city || '—')}<div style="color:var(--muted);font-size:12px">${esc(item.address || '—')}</div></td>
          <td>${esc(item.description || '—')}</td>
          <td>${linked > 0 ? `<span class="badge good">${Number(linked)} linked</span>` : '<span class="badge off">0 linked</span>'}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 9999)}</td>
          <td><div class="row-actions"><button class="link" data-station-action="edit" data-station-key="${esc(item.key || '')}">Edit</button><button class="link danger" data-station-action="delete" data-station-key="${esc(item.key || '')}">Delete</button></div></td>
        </tr>`;
      }).join('')}</tbody></table></div>`;
      enhanceTable(els.stationProfilesWrap, 'stationProfiles');
    }

    function renderPickupLocationsTable(){
      const scope = String(state.locationStationScope || 'all');
      const rows = filterList(pickupLocationRows(), state.filters.pickupLocations, (item) => {
        if (scope === 'all') return true;
        const stationKey = String(item.stationKey || item.stationCode || item.station || 'default').trim().toLowerCase() || 'default';
        return stationKey === scope;
      });
      if (els.pickupLocationsCount) els.pickupLocationsCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.pickupLocationsWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν τοποθεσίες.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.pickupLocationsWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Σταθμός</th><th>Τοποθεσία</th><th>Type</th><th>Address</th><th>Extra fee</th><th>Active</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.stationLabel || item.station || 'Κεντρικός σταθμός')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.stationKey || 'default')}</div></td>
          <td><strong>${esc(item.label || item.displayTitle || '—')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(item.locationType || 'both')}</td>
          <td>${esc(item.address || '—')}</td>
          <td>${money(item.extraFee || 0, currency)}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 0)}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="pickupLocation" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="pickupLocation" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.pickupLocationsWrap, pickupLocationRows());
      enhanceTable(els.pickupLocationsWrap, 'pickupLocations');
    }

    function renderDynamicPricingSection(){
      renderPricingSeasonsTable();
      renderCategoryRateRulesTable();
      renderPricingSimulator();
    }

    function renderPricingSeasonsTable(){
      const rows = filterList(pricingSeasonsRows(), state.filters.pricingSeasons);
      if (els.pricingSeasonsCount) els.pricingSeasonsCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.pricingSeasonsWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν pricing seasons.</div>'; return; }
      els.pricingSeasonsWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Season</th><th>Window</th><th>Priority</th><th>Repeat yearly</th><th>Active</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label || item.title || item.key || '—')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc((item.startDate || '—') + ' → ' + (item.endDate || '—'))}</td>
          <td>${Number(item.priority || 0)}</td>
          <td>${item.repeatYearly ? '<span class="badge good">Yearly</span>' : '<span class="badge off">One-off</span>'}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 0)}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="pricingSeason" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="pricingSeason" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.pricingSeasonsWrap, pricingSeasonsRows());
      enhanceTable(els.pricingSeasonsWrap, 'pricingSeasons');
    }

    function renderCategoryRateRulesTable(){
      const rows = filterList(categoryRateRuleRows(), state.filters.categoryRateRules, item => {
        const categoryOk = !state.filters.rateRuleCategory || String(item.categoryCode || '').toUpperCase() === String(state.filters.rateRuleCategory || '').toUpperCase();
        const seasonFilter = String(state.filters.rateRuleSeason || '').trim().toLowerCase();
        const itemSeason = String(item.seasonKey || '').trim().toLowerCase();
        const seasonOk = !seasonFilter || itemSeason === seasonFilter;
        return categoryOk && seasonOk;
      });
      if (els.categoryRateRulesCount) els.categoryRateRulesCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.categoryRateRulesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν κανόνες dynamic pricing.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.categoryRateRulesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Rule</th><th>Category</th><th>Season</th><th>Days</th><th>Price / day</th><th>Active</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label || item.title || item.key || '—')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(categoryLabelFromRule(item))}</td>
          <td>${esc(seasonLabelFromRule(item))}</td>
          <td>${esc(dayBracketLabel(item.minDays, item.maxDays))}</td>
          <td>${money(item.pricePerDay || 0, currency)}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 0)}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="categoryRateRule" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="categoryRateRule" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.categoryRateRulesWrap, categoryRateRuleRows());
      enhanceTable(els.categoryRateRulesWrap, 'categoryRateRules');
    }

    function renderPricingSimulator(){
      if(els.filterRateRuleCategory){
        const current = String(state.filters.rateRuleCategory || '');
        const options = ['<option value="">Όλες οι κατηγορίες</option>'].concat(vehicleCategoryRows().map(row => `<option value="${esc(row.category || '')}" ${String(row.category || '') === current ? 'selected' : ''}>${esc(row.displayTitle || row.title || row.category || '')}</option>`));
        els.filterRateRuleCategory.innerHTML = options.join('');
      }
      if(els.filterRateRuleSeason){
        const current = String(state.filters.rateRuleSeason || '');
        const options = ['<option value="">Όλες οι seasons</option>'].concat(pricingSeasonsRows().map(row => `<option value="${esc(row.key || '')}" ${String(row.key || '') === current ? 'selected' : ''}>${esc(row.label || row.title || row.key || '')}</option>`));
        els.filterRateRuleSeason.innerHTML = options.join('');
      }
      if(els.simCategoryCode){
        const current = String(els.simCategoryCode.value || '');
        const options = ['<option value="">Επίλεξε κατηγορία</option>'].concat(vehicleCategoryRows().map(row => `<option value="${esc(row.category || '')}" ${String(row.category || '') === current ? 'selected' : ''}>${esc(row.displayTitle || row.title || row.category || '')}</option>`));
        els.simCategoryCode.innerHTML = options.join('');
        if(!els.simCategoryCode.value && vehicleCategoryRows()[0]?.category) els.simCategoryCode.value = vehicleCategoryRows()[0].category;
      }
      if(els.simPickupDate && !els.simPickupDate.value){ els.simPickupDate.value = new Date().toISOString().slice(0,10); }
      if(els.simBasePrice && (!els.simBasePrice.value || Number(els.simBasePrice.value) === 0)){
        const vehicle = vehicleCategoryByCode(els.simCategoryCode?.value || '');
        if(vehicle?.price) els.simBasePrice.value = String(vehicle.price);
      }
      const result = resolveSimulatorPrice();
      const currency = businessSettings().currency || 'EUR';
      els.pricingSimulatorWrap.innerHTML = `<div class="viewer-stack" style="margin-top:10px"><div class="viewer-card"><h4>Resolved inputs</h4><div class="kv-grid"><div><strong>Category</strong><div>${esc(result.vehicle?.displayTitle || result.vehicle?.title || result.vehicle?.category || '—')}</div></div><div><strong>Days</strong><div>${Number(result.days || 0)}</div></div><div><strong>Season</strong><div>${esc(result.season?.label || 'All year / none')}</div></div><div><strong>Applied range</strong><div>${esc(result.rule ? dayBracketLabel(result.rule.minDays, result.rule.maxDays) : 'Fallback only')}</div></div></div></div><div class="viewer-card"><h4>Resolved pricing</h4><div class="kv-grid"><div><strong>Source</strong><div>${esc(result.rule ? 'Dynamic rate rule' : 'Vehicle fallback price')}</div></div><div><strong>Price / day</strong><div>${money(result.pricePerDay || 0, currency)}</div></div><div><strong>Total (base)</strong><div>${money(result.total || 0, currency)}</div></div><div><strong>Rule</strong><div>${esc(result.rule?.label || result.rule?.key || '—')}</div></div></div></div></div>`;
    }

    function renderInsuranceTable(){
      const rows = filterList(insuranceRows(), state.filters.insurance);
      els.insuranceCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.insuranceWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν insurance plans.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.insuranceWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Label</th><th>Key</th><th>Price / day</th><th>Public</th><th>Active</th><th>Sort</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label)}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(item.key)}</td>
          <td>${money(item.pricePerDay || 0, currency)}</td>
          <td>${item.publicVisible !== false ? '<span class="badge good">Public</span>' : '<span class="badge private">Private</span>'}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${Number(item.sortOrder || 0)}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="insurance" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="insurance" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.insuranceWrap, insuranceRows());
      enhanceTable(els.insuranceWrap, 'insurance');
    }

    function renderExtrasTable(){
      const rows = filterList(extraRows(), state.filters.extras);
      els.extrasCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.extrasWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν extra services.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.extrasWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Label</th><th>Key</th><th>Price</th><th>Billing mode</th><th>Public</th><th>Active</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label)}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(item.key)}</td>
          <td>${money(item.price || 0, currency)}</td>
          <td>${esc(item.billingMode || 'perDay')}</td>
          <td>${item.publicVisible !== false ? '<span class="badge good">Public</span>' : '<span class="badge private">Private</span>'}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="extra" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="extra" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.extrasWrap, extraRows());
      enhanceTable(els.extrasWrap, 'extras');
    }

    function renderFeesTable(){
      const rows = filterList(feeRows(), state.filters.fees, item => !state.filters.ruleType || String(item.ruleType || '') === state.filters.ruleType);
      els.feesCount.textContent = `${rows.length} items`;
      if(!rows.length){ els.feesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν fee rules.</div>'; return; }
      const currency = businessSettings().currency || 'EUR';
      els.feesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Label</th><th>Key</th><th>Rule type</th><th>Audience group</th><th>Amount</th><th>Mode</th><th>Public</th><th>Active</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label)}</strong></td>
          <td>${esc(item.key)}</td>
          <td>${esc(item.ruleType || 'manual')}</td>
          <td>${esc(item.audienceGroup || '—')}</td>
          <td>${money(item.amount || 0, currency)}</td>
          <td>${esc(item.billingMode || 'perBooking')}</td>
          <td>${item.publicVisible !== false ? '<span class="badge good">Public</span>' : '<span class="badge private">Private</span>'}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td><div class="row-actions"><button class="link" data-edit-kind="fee" data-id="${esc(item._id || item.key)}">Edit</button><button class="link" data-duplicate-kind="fee" data-id="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      bindTableActions(els.feesWrap, feeRows());
      enhanceTable(els.feesWrap, 'fees');
    }


    function roleFlagsSummary(role){
      const summary = ROLE_MODULES.map((moduleKey) => {
        const flags = ROLE_ACTIONS.filter((action) => !!role?.permissions?.[moduleKey]?.[action]).map((action) => action.charAt(0).toUpperCase()).join('/');
        return flags ? `${moduleKey}: ${flags}` : '';
      }).filter(Boolean);
      return summary.join(' • ');
    }

    function renderSecuritySection(){
      const current = accessSnapshot().currentStaff || {};
      const users = staffUsers();
      const selectedEmail = String(state.securityEmail || current.email || users[0]?.email || '').toLowerCase();
      state.securityEmail = selectedEmail;
      const selectedUser = users.find((item) => String(item.email || '').toLowerCase() === selectedEmail) || null;
      const rows = sessionRows().filter((item) => {
        const q = String(state.filters.sessions || '').trim().toLowerCase();
        const selectedMatch = !selectedEmail || String(item.email || '').toLowerCase() === selectedEmail;
        const searchMatch = !q || [item.email, item.fullName, item.roleKey, item.userAgent].some((v) => String(v || '').toLowerCase().includes(q));
        return selectedMatch && searchMatch;
      });
      els.sessionsCount.textContent = `${rows.length} sessions`;
      els.securityCards.innerHTML = [
        { label:'Selected user', value:selectedUser?.fullName || selectedUser?.email || '—' },
        { label:'Role', value:selectedUser?.roleKey || '—' },
        { label:'Active sessions', value:String(selectedUser?.activeSessionCount || 0) },
        { label:'Password status', value:selectedUser ? (selectedUser.hasCredential ? (selectedUser.mustChangePassword ? 'Must change' : 'Ready') : 'Not set') : '—' },
        { label:'Credential updated', value:selectedUser?.passwordSetAt ? new Date(selectedUser.passwordSetAt).toLocaleString('el-GR') : '—' }
      ].map((item) => `<div class="mini"><div class="label">${esc(item.label)}</div><div class="value">${esc(item.value)}</div></div>`).join('');
      els.securityUserSelect.innerHTML = users.length ? users.map((user) => `<option value="${esc(user.email)}" ${String(user.email).toLowerCase() === selectedEmail ? 'selected' : ''}>${esc(user.fullName || user.email)} · ${esc(user.roleKey || '')}</option>`).join('') : '<option value="">Δεν υπάρχουν staff users</option>';
      els.securityMustChange.checked = selectedUser ? !!selectedUser.mustChangePassword : true;
      if (!state.securityResult) {
        const bootstrap = accessSnapshot().bootstrapHelp || {};
        els.securityResult.value = bootstrap.notes || 'Επίλεξε χρήστη και όρισε ή κάνε reset στο password.';
      } else {
        els.securityResult.value = state.securityResult;
      }
      if(!rows.length){ els.sessionsWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν active sessions για τον επιλεγμένο χρήστη.</div>'; return; }
      els.sessionsWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>User</th><th>Role</th><th>Created</th><th>Last seen</th><th>Expires</th><th>Device</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.fullName || item.email)}</strong><div style="color:var(--muted);font-size:12px">${esc(item.email || '')}${item.currentSession ? ' · current' : ''}</div></td>
          <td>${esc(item.roleKey || '')}</td>
          <td>${esc(item.createdAt ? new Date(item.createdAt).toLocaleString('el-GR') : '—')}</td>
          <td>${esc(item.lastSeenAt ? new Date(item.lastSeenAt).toLocaleString('el-GR') : '—')}</td>
          <td>${esc(item.expiresAt ? new Date(item.expiresAt).toLocaleString('el-GR') : '—')}</td>
          <td style="max-width:280px">${esc(item.userAgent || '—')}</td>
          <td><div class="row-actions"><button class="link" data-session-revoke="${esc(item._id || '')}">Revoke</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      els.sessionsWrap.querySelectorAll('[data-session-revoke]').forEach((btn) => btn.addEventListener('click', () => {
        setBusy(true);
        post('revokeStaffSessions', { payload: { itemId: btn.dataset.sessionRevoke } });
      }));
      enhanceTable(els.sessionsWrap, 'sessions');
    }

    function renderRolesTable(){
      const q = String(state.filters.roles || '').toLowerCase();
      const rows = roleRows().filter((item) => !q || [item.key, item.label, item.description].some(v => String(v || '').toLowerCase().includes(q)));
      els.rolesCount.textContent = `${rows.length} roles`;
      if(!rows.length){ els.rolesWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν ρόλοι.</div>'; return; }
      els.rolesWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Role</th><th>Key</th><th>Status</th><th>Permissions</th><th>Special</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.label)}</strong><div style="color:var(--muted);font-size:12px">${esc(item.description || '')}</div></td>
          <td>${esc(item.key)}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td style="max-width:320px">${esc(roleFlagsSummary(item) || 'No permissions')}</td>
          <td>${esc((item.specialPermissionsList || []).length)}</td>
          <td><div class="row-actions"><button class="link" data-role-inspect="${esc(item._id || item.key)}">Inspect</button><button class="link" data-role-edit="${esc(item._id || item.key)}">Edit</button><button class="link" data-role-duplicate="${esc(item._id || item.key)}">Duplicate</button></div></td>
        </tr>`).join('')}</tbody></table></div>`;
      els.rolesWrap.querySelectorAll('[data-role-inspect]').forEach(btn => btn.addEventListener('click', () => {
        const item = roleRows().find(row => String(row._id || row.key) === String(btn.dataset.roleInspect));
        if(item) openInspector('role', item);
      }));
      els.rolesWrap.querySelectorAll('[data-role-edit]').forEach(btn => btn.addEventListener('click', () => {
        const item = roleRows().find(row => String(row._id || row.key) === String(btn.dataset.roleEdit));
        openEditor('role', item || null);
      }));
      els.rolesWrap.querySelectorAll('[data-role-duplicate]').forEach(btn => btn.addEventListener('click', () => {
        const item = roleRows().find(row => String(row._id || row.key) === String(btn.dataset.roleDuplicate));
        if(!item) return;
        const copy = clone(item); delete copy._id; copy.key = `${copy.key}_copy`; copy.label = `${copy.label} Copy`;
        openEditor('role', copy);
      }));
      enhanceTable(els.rolesWrap, 'roles');
    }

    function renderUsersTable(){
      const q = String(state.filters.users || '').toLowerCase();
      const rows = staffUsers().filter((item) => !q || [item.email, item.fullName, item.roleKey, item.notes].some(v => String(v || '').toLowerCase().includes(q)));
      els.usersCount.textContent = `${rows.length} users`;
      if(!rows.length){ els.usersWrap.innerHTML = '<div class="empty">Δεν βρέθηκαν staff users.</div>'; return; }
      const roleMap = new Map(roleRows().map(item => [String(item.key), item.label]));
      els.usersWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Status</th><th>Credential</th><th>Sessions</th><th>Actions</th></tr></thead><tbody>${rows.map(item => `
        <tr>
          <td><strong>${esc(item.fullName || '—')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.notes || '')}</div></td>
          <td>${esc(item.email)}</td>
          <td>${esc(roleMap.get(String(item.roleKey || '')) || item.roleKey || '—')}</td>
          <td>${item.active !== false ? '<span class="badge good">Active</span>' : '<span class="badge off">Inactive</span>'}</td>
          <td>${item.hasCredential ? `<span class="badge ${item.mustChangePassword ? 'private' : 'good'}">${item.mustChangePassword ? 'Temp / must change' : 'Ready'}</span>` : `<span class="badge off">No credential</span>`}${item.passwordSetAt ? `<div style="color:var(--muted);font-size:12px;margin-top:4px">${esc(new Date(item.passwordSetAt).toLocaleString('el-GR'))}</div>` : ''}</td>
          <td><span class="badge ${Number(item.activeSessionCount || 0) ? 'good' : 'off'}">${Number(item.activeSessionCount || 0)} active</span></td>
          <td><div class="row-actions"><button class="link" data-user-inspect="${esc(item._id || item.email)}">Inspect</button><button class="link" data-user-edit="${esc(item._id || item.email)}">Edit</button><button class="link" data-user-security="${esc(item.email || '')}">Security</button><button class="link" data-user-invite="${esc(item.email || '')}">${item.hasCredential ? 'Re-invite' : 'Invite'}</button>${item.active !== false ? `<button class="link" data-user-deactivate="${esc(item._id || item.email)}">Deactivate</button>` : `<button class="link" data-user-reactivate="${esc(item._id || item.email)}">Reactivate</button>`}</div></td>
        </tr>`).join('')}</tbody></table></div>`;
      els.usersWrap.querySelectorAll('[data-user-inspect]').forEach(btn => btn.addEventListener('click', () => {
        const item = staffUsers().find(row => String(row._id || row.email) === String(btn.dataset.userInspect));
        if(item) openInspector('user', item);
      }));
      els.usersWrap.querySelectorAll('[data-user-edit]').forEach(btn => btn.addEventListener('click', () => {
        const item = staffUsers().find(row => String(row._id || row.email) === String(btn.dataset.userEdit));
        openEditor('user', item || null);
      }));
      els.usersWrap.querySelectorAll('[data-user-security]').forEach(btn => btn.addEventListener('click', () => { state.securityEmail = String(btn.dataset.userSecurity || '').toLowerCase(); state.activeSection = 'security'; renderTabs(); renderSections(); renderSecuritySection(); requestResize(); }));
      els.usersWrap.querySelectorAll('[data-user-invite]').forEach(btn => btn.addEventListener('click', () => {
        const item = staffUsers().find(row => String(row.email || '').toLowerCase() === String(btn.dataset.userInvite || '').toLowerCase());
        if(!item){ showToast('Δεν βρέθηκε staff user για onboarding.', 'error'); return; }
        setBusy(true);
        post('inviteStaffUser', { payload: { email: item.email, fullName: item.fullName, roleKey: item.roleKey, notes: item.notes, active: item.active !== false } });
      }));
      els.usersWrap.querySelectorAll('[data-user-deactivate]').forEach(btn => btn.addEventListener('click', () => {
        setBusy(true); post('deactivateStaffUser', { payload: { itemId: btn.dataset.userDeactivate } });
      }));
      els.usersWrap.querySelectorAll('[data-user-reactivate]').forEach(btn => btn.addEventListener('click', () => {
        setBusy(true); post('reactivateStaffUser', { payload: { itemId: btn.dataset.userReactivate } });
      }));
      enhanceTable(els.usersWrap, 'users');
    }

    function renderAuditTable(){
      const q = String(state.filters.audit || '').trim().toLowerCase();
      const typeFilter = String(state.filters.auditType || '').trim();
      const rows = auditRows().filter((item) => {
        if(typeFilter && String(item.entityType || '') !== typeFilter) return false;
        if(!q) return true;
        const hay = [item.actorName, item.actorEmail, item.action, item.entityType, item.summary, item.entityId].map((v) => String(v || '').toLowerCase()).join(' ');
        return hay.includes(q);
      });
      els.auditCount.textContent = `${rows.length} entries`;
      if(!rows.length){ els.auditWrap.innerHTML = '<div class="empty">Δεν υπάρχει ακόμη audit history για αυτά τα φίλτρα.</div>'; return; }
      els.auditWrap.innerHTML = `<div class="table-wrap"><table><thead><tr><th>Date</th><th>Actor</th><th>Action</th><th>Entity</th><th>Summary</th><th>Actions</th></tr></thead><tbody>${rows.map(item => {
        const canIssueRecovery = String(item.entityType || '') === 'accessRecovery' && String(item.action || '') === 'requestRecovery' && String(item.entityId || '').includes('@');
        return `
        <tr>
          <td>${esc(new Date(item.createdAt).toLocaleString('el-GR'))}</td>
          <td><strong>${esc(item.actorName || item.actorEmail || '—')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.actorEmail || '')}</div></td>
          <td>${esc(item.action || '')}</td>
          <td>${esc(item.entityType || '')}</td>
          <td><strong>${esc(item.summary || '')}</strong><div style="color:var(--muted);font-size:12px">${esc(item.entityId || '')}</div></td>
          <td><div class="row-actions"><button class="link" data-audit-view="${esc(item._id || item.createdAt || item.entityId || '')}">View</button>${canIssueRecovery ? `<button class="link" data-audit-reset="${esc(item.entityId || '')}">Issue reset</button>` : ''}</div></td>
        </tr>`;
      }).join('')}</tbody></table></div>`;
      els.auditWrap.querySelectorAll('[data-audit-view]').forEach((btn) => btn.addEventListener('click', () => {
        const target = rows.find((row) => String(row._id || row.createdAt || row.entityId || '') === String(btn.dataset.auditView));
        if(target) openInspector('audit', target);
      }));
      els.auditWrap.querySelectorAll('[data-audit-reset]').forEach((btn) => btn.addEventListener('click', () => {
        const email = String(btn.dataset.auditReset || '').trim();
        if(!email) return;
        state.securityEmail = email.toLowerCase();
        setBusy(true);
        post('resetStaffPassword', { payload: { email } });
      }));
      enhanceTable(els.auditWrap, 'audit');
    }

    function bindTableActions(container, allRows){
      container.querySelectorAll('[data-edit-kind]').forEach(btn => btn.addEventListener('click', () => {
        const kind = btn.dataset.editKind;
        const id = btn.dataset.id;
        const item = allRows.find(row => String(row._id || row.key || row.plate) === String(id));
        openEditor(kind, item || null);
      }));
      container.querySelectorAll('[data-duplicate-kind]').forEach(btn => btn.addEventListener('click', () => {
        const kind = btn.dataset.duplicateKind;
        const id = btn.dataset.id;
        const item = allRows.find(row => String(row._id || row.key || row.plate) === String(id));
        duplicateItem(kind, item || null);
      }));
    }

    function openEditor(kind, item){
      state.editor = { open:true, kind, item: clone(item) || defaultsFor(kind) };
      const isNew = !state.editor.item._id;
      const titleMap = { vehicleCategory:'vehicle category', fleetVehicle:'fleet vehicle', pickupLocation:'pickup location', pricingSeason:'pricing season', categoryRateRule:'category rate rule', insurance:'insurance plan', extra:'extra service', fee:'fee rule', role:'role', user:'staff user' };
      const subMap = { vehicleCategory:'Live public κατηγορία οχημάτων που εμφανίζεται μπροστά στο site.', fleetVehicle:'Πραγματική μονάδα στόλου για plate, model, category και operational status.', pickupLocation:'Τοποθεσία παραλαβής / επιστροφής που θέλεις να χειρίζεσαι από το backroom.', pricingSeason:'Χρονική περίοδος τιμολόγησης για low/high/peak season λογική.', categoryRateRule:'Κανόνας τιμής ανά κατηγορία, season και day range.', insurance:'Ρυθμίσεις ασφαλιστικού πλάνου.', extra:'Ρυθμίσεις προαιρετικής παροχής.', fee:'Ρυθμίσεις automatic ή manual fee.', role:'Πίνακας permissions τύπου Wheels με View / Insert / Edit / Delete και Special rights.', user:'Ξεχωριστός staff λογαριασμός με deactivate αντί για delete.' };
      els.editorTitle.textContent = `${isNew ? 'Νέο' : 'Επεξεργασία'} ${titleMap[kind] || 'item'}`;
      els.editorSub.textContent = subMap[kind] || 'Συμπλήρωσε τα πεδία και αποθήκευσε.';
      els.deleteEditorBtn.style.display = (isNew || ['role','user'].includes(kind)) ? 'none' : '';
      els.deleteEditorBtn.dataset.kind = kind;
      els.editorGrid.innerHTML = editorFields(kind, state.editor.item);
      bindEditorTools(kind);
      els.editorOverlay.classList.add('show');
      requestResize();
    }
    function closeEditor(){ state.editor = { open:false, kind:'', item:null }; els.editorOverlay.classList.remove('show'); }
    function defaultsFor(kind){
      if(kind === 'vehicleCategory') return { _id:'', title:'', category:'', price:0, image:'', active:true, status:'active', sortOrder:9999, transmission:'', fuelType:'', seats:'', doors:'', luggage:'', airCondition:true, publicVisible:true, description:'' };
      if(kind === 'fleetVehicle') return { _id:'', plate:'', model:'', category:(vehicleCategoryRows()[0]?.category || ''), status:'available', active:true, notes:'' };
      if(kind === 'pickupLocation') {
        const scopeKey = String(state.locationStationScope || 'all');
        const activeStation = effectiveStationProfiles().find((row) => row.key === scopeKey) || effectiveStationProfiles()[0] || { key:'default', label:'Κεντρικός σταθμός' };
        return { _id:'', key:'', label:'', description:'', locationType:'both', stationKey: activeStation.key || 'default', stationLabel: activeStation.label || 'Κεντρικός σταθμός', address:'', mapUrl:'', extraFee:0, sortOrder:9999, active:true, publicVisible:true, pickupInfoLabel:'', pickupInfoPlaceholder:'', showOriginField:true, originFieldLabel:'', originFieldPlaceholder:'', dropoffInfoLabel:'', dropoffInfoPlaceholder:'' };
      }
      if(kind === 'pricingSeason') return { _id:'', key:'', label:'', description:'', startDate:'', endDate:'', priority:0, sortOrder:9999, repeatYearly:false, active:true, publicVisible:true };
      if(kind === 'categoryRateRule') return { _id:'', key:'', label:'', description:'', categoryCode:(vehicleCategoryRows()[0]?.category || ''), vehicleCategoryId:(vehicleCategoryRows()[0]?._id || ''), seasonKey:'', minDays:1, maxDays:3, pricePerDay:0, sortOrder:9999, active:true, publicVisible:true, rangePreset:'1_3' };
      if(kind === 'insurance') return { _id:'', key:'', label:'', description:'', pricePerDay:0, active:true, publicVisible:true, sortOrder:9999 };
      if(kind === 'extra') return { _id:'', key:'', label:'', description:'', price:0, billingMode:'perDay', active:true, publicVisible:true, sortOrder:9999 };
      if(kind === 'role') {
        const role = { _id:'', key:'', label:'', description:'', active:true, systemRole:false, sortOrder:9999, specialPermissions:'' };
        ROLE_MODULES.forEach((moduleKey) => ROLE_ACTIONS.forEach((action) => { role[`${moduleKey}${action.charAt(0).toUpperCase()}${action.slice(1)}`] = false; }));
        return role;
      }
      if(kind === 'user') return { _id:'', email:'', fullName:'', roleKey:(roleRows()[0]?.key || 'viewer'), active:true, memberId:'', notes:'' };
      return { _id:'', key:'', label:'', ruleType:'manual', audienceGroup:'', amount:0, billingMode:'perBooking', active:true, publicVisible:false, sortOrder:9999 };
    }
    function bindEditorTools(kind){
      if(kind === 'role'){
        const allBtn = document.getElementById('roleCheckAllBtn');
        const noneBtn = document.getElementById('roleClearAllBtn');
        if(allBtn) allBtn.addEventListener('click', () => { els.editorGrid.querySelectorAll('[data-perm-box]').forEach(box => { box.checked = true; }); });
        if(noneBtn) noneBtn.addEventListener('click', () => { els.editorGrid.querySelectorAll('[data-perm-box]').forEach(box => { box.checked = false; }); });
        return;
      }
      if(kind === 'categoryRateRule'){
        const presetSelect = els.editorGrid.querySelector('[data-field="rangePreset"]');
        const minInput = els.editorGrid.querySelector('[data-field="minDays"]');
        const maxInput = els.editorGrid.querySelector('[data-field="maxDays"]');
        const categorySelect = els.editorGrid.querySelector('[data-field="categoryCode"]');
        const categoryIdInput = els.editorGrid.querySelector('[data-field="vehicleCategoryId"]');
        const syncPreset = () => {
          const preset = DAY_BRACKET_PRESETS.find((row) => row.key === String(presetSelect?.value || 'custom'));
          if(!preset || preset.key === 'custom') return;
          if(minInput) minInput.value = String(preset.minDays);
          if(maxInput) maxInput.value = String(preset.maxDays);
        };
        const syncCategoryId = () => {
          const row = vehicleCategoryByCode(categorySelect?.value || '');
          if(categoryIdInput) categoryIdInput.value = row?._id || '';
        };
        const syncCustomPreset = () => {
          if(!presetSelect) return;
          const key = presetKeyForRange(minInput?.value, maxInput?.value);
          presetSelect.value = key || 'custom';
        };
        if(presetSelect) presetSelect.addEventListener('change', syncPreset);
        if(categorySelect) categorySelect.addEventListener('change', syncCategoryId);
        if(minInput) minInput.addEventListener('input', syncCustomPreset);
        if(maxInput) maxInput.addEventListener('input', syncCustomPreset);
        syncCategoryId();
        syncCustomPreset();
        return;
      }
      if(kind === 'pickupLocation'){
        const stationSelect = els.editorGrid.querySelector('[data-station-profile-select]');
        const stationLabelInput = els.editorGrid.querySelector('[data-field="stationLabel"]');
        const syncStationLabel = () => {
          const selectedKey = String(stationSelect?.value || '').trim().toLowerCase();
          const match = effectiveStationProfiles().find((row) => row.key === selectedKey);
          if (stationLabelInput) stationLabelInput.value = match?.label || '';
        };
        if (stationSelect) stationSelect.addEventListener('change', syncStationLabel);
        syncStationLabel();
      }
    }

    function editorFields(kind, item){
      const common = `
        <div class="field col-4"><label>Key</label><input data-field="key" value="${esc(item.key || '')}" placeholder="uniqueKey" /></div>
        <div class="field col-4"><label>Label</label><input data-field="label" value="${esc(item.label || '')}" /></div>
        <div class="field col-4"><label>Sort order</label><input data-field="sortOrder" type="number" step="1" value="${Number(item.sortOrder || 0)}" /></div>
        <div class="field col-12"><label>Description</label><textarea data-field="description">${esc(item.description || '')}</textarea></div>
        <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
        <div class="field col-3 switch"><input data-field="publicVisible" type="checkbox" ${item.publicVisible !== false ? 'checked' : ''} /><label>Public visible</label></div>`;
      if(kind === 'vehicleCategory'){
        return `
          <div class="field col-6"><label>Τίτλος εμφάνισης</label><input data-field="title" value="${esc(item.title || '')}" placeholder="Hyundai i10" /></div>
          <div class="field col-3"><label>Category code</label><input data-field="category" value="${esc(item.category || '')}" placeholder="A" /></div>
          <div class="field col-3"><label>Sort order</label><input data-field="sortOrder" type="number" step="1" value="${Number(item.sortOrder || 0)}" /></div>
          <div class="field col-4"><label>Price / day</label><input data-field="price" type="number" step="0.01" value="${Number(item.price || 0)}" /></div>
          <div class="field col-4"><label>Status</label><input data-field="status" value="${esc(item.status || 'active')}" placeholder="active" /></div>
          <div class="field col-4"><label>Image URL / Wix image</label><input data-field="image" value="${esc(item.image || '')}" placeholder="https://... ή wix:image://..." /></div>
          <div class="field col-3"><label>Transmission</label><input data-field="transmission" value="${esc(item.transmission || '')}" /></div>
          <div class="field col-3"><label>Fuel type</label><input data-field="fuelType" value="${esc(item.fuelType || '')}" /></div>
          <div class="field col-2"><label>Seats</label><input data-field="seats" value="${esc(item.seats || '')}" /></div>
          <div class="field col-2"><label>Doors</label><input data-field="doors" value="${esc(item.doors || '')}" /></div>
          <div class="field col-2"><label>Luggage</label><input data-field="luggage" value="${esc(item.luggage || '')}" /></div>
          <div class="field col-12"><label>Description</label><textarea data-field="description">${esc(item.description || '')}</textarea></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-3 switch"><input data-field="airCondition" type="checkbox" ${item.airCondition !== false ? 'checked' : ''} /><label>Air condition</label></div>
          <div class="field col-3 switch"><input data-field="publicVisible" type="checkbox" ${item.publicVisible !== false ? 'checked' : ''} /><label>Public visible</label></div>
          <div class="field col-12"><label>Preview title</label><input value="${esc(((item.category || '').trim() && (item.title || '').trim()) ? `${item.category} - ${item.title}` : (item.title || item.category || ''))}" readonly /></div>`;
      }
      if(kind === 'fleetVehicle'){
        const categoryOptions = vehicleCategoryRows().map(row => `<option value="${esc(row.category || '')}" ${String(item.category || '') === String(row.category || '') ? 'selected' : ''}>${esc(row.displayTitle || row.title || row.category || '')}</option>`).join('');
        return `
          <div class="field col-4"><label>Πινακίδα</label><input data-field="plate" value="${esc(item.plate || '')}" placeholder="ΝΗΧ1234" /></div>
          <div class="field col-5"><label>Μοντέλο</label><input data-field="model" value="${esc(item.model || '')}" placeholder="Hyundai i10" /></div>
          <div class="field col-3"><label>Category</label>${categoryOptions ? `<select data-field="category">${categoryOptions}</select>` : `<input data-field="category" value="${esc(item.category || '')}" placeholder="A" />`}</div>
          <div class="field col-4"><label>Status</label><select data-field="status"><option value="available" ${String(item.status || '') === 'available' ? 'selected' : ''}>available</option><option value="rented" ${String(item.status || '') === 'rented' ? 'selected' : ''}>rented</option><option value="service" ${String(item.status || '') === 'service' ? 'selected' : ''}>service</option><option value="inactive" ${String(item.status || '') === 'inactive' ? 'selected' : ''}>inactive</option></select></div>
          <div class="field col-8"><label>Notes</label><textarea data-field="notes">${esc(item.notes || '')}</textarea></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-12"><label>Preview label</label><input value="${esc((((item.plate || '').trim() ? ((item.plate || '').trim() + ((item.model || '').trim() ? ` - ${(item.model || '').trim()}` : '')) : ((item.model || '').trim() || ''))) + (((item.category || '').trim()) ? ` (${(item.category || '').trim()})` : ''))}" readonly /></div>`;
      }
      if(kind === 'role'){
        const specials = new Set((Array.isArray(item.specialPermissionsList) ? item.specialPermissionsList : String(item.specialPermissions || '').split('|')).filter(Boolean));
        const permRows = ROLE_MODULES.map((moduleKey) => `<tr><td><strong>${esc(moduleKey)}</strong></td>${ROLE_ACTIONS.map((action) => { const field = `${moduleKey}${action.charAt(0).toUpperCase()}${action.slice(1)}`; return `<td style="text-align:center"><input data-perm-box="1" data-field="${field}" type="checkbox" ${item[field] ? 'checked' : ''} /></td>`; }).join('')}</tr>`).join('');
        const specialRows = SPECIAL_PERMISSIONS.map((key) => `<label style="display:flex;gap:8px;align-items:flex-start;padding:6px 0"><input data-special-box="1" type="checkbox" value="${esc(key)}" ${specials.has(key) ? 'checked' : ''}/><span><strong>${esc(SPECIAL_PERMISSION_LABELS[key] || key)}</strong><br><span style="color:var(--muted);font-size:12px">${esc((specialDescriptions().get(key) || ''))}</span></span></label>`).join('');
        return common + `
          <div class="field col-3 switch"><input data-field="systemRole" type="checkbox" ${item.systemRole ? 'checked' : ''} /><label>System role</label></div>
          <div class="field col-9"><label>Permissions window</label><div class="row-actions" style="margin-bottom:10px"><button type="button" class="btn" id="roleCheckAllBtn">Check all</button><button type="button" class="btn" id="roleClearAllBtn">Un-check all</button></div><div class="table-wrap"><table><thead><tr><th>Module</th><th>View</th><th>Insert</th><th>Edit</th><th>Delete</th></tr></thead><tbody>${permRows}</tbody></table></div></div>
          <div class="field col-12"><label>Special permissions</label><div class="grid" style="grid-template-columns:repeat(3,minmax(0,1fr));gap:4px 16px">${specialRows}</div></div>`;
      }
      if(kind === 'user'){
        const roleOptions = roleRows().filter(role => role.active !== false).map(role => `<option value="${esc(role.key)}" ${String(item.roleKey || '') === String(role.key) ? 'selected' : ''}>${esc(role.label)}</option>`).join('');
        return `
          <div class="field col-6"><label>Full name</label><input data-field="fullName" value="${esc(item.fullName || '')}" /></div>
          <div class="field col-6"><label>Email</label><input data-field="email" value="${esc(item.email || '')}" placeholder="name@company.com" /></div>
          <div class="field col-4"><label>Role</label><select data-field="roleKey">${roleOptions}</select></div>
          <div class="field col-4 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-4"><label>Member link</label><input value="${esc(item.memberId || 'Pending / not linked yet')}" readonly /></div>
          <div class="field col-12"><label>Notes</label><textarea data-field="notes">${esc(item.notes || '')}</textarea></div>
          <div class="field col-12"><label>Guideline</label><textarea readonly>Κράτα ξεχωριστό account ανά άνθρωπο. Μην διαγράφεις λογαριασμούς που έχουν ιστορικό — κάνε deactivate για να σωθεί το audit trail.</textarea></div>`;
      }
      if(kind === 'pricingSeason'){
        return `
          <div class="field col-4"><label>Key</label><input data-field="key" value="${esc(item.key || '')}" placeholder="summer_peak" /></div>
          <div class="field col-5"><label>Label</label><input data-field="label" value="${esc(item.label || '')}" placeholder="August Peak" /></div>
          <div class="field col-3"><label>Sort order</label><input data-field="sortOrder" type="number" step="1" value="${Number(item.sortOrder || 0)}" /></div>
          <div class="field col-4"><label>Start date</label><input data-field="startDate" type="date" value="${esc(item.startDate || '')}" /></div>
          <div class="field col-4"><label>End date</label><input data-field="endDate" type="date" value="${esc(item.endDate || '')}" /></div>
          <div class="field col-2"><label>Priority</label><input data-field="priority" type="number" step="1" value="${Number(item.priority || 0)}" /></div>
          <div class="field col-2 switch"><input data-field="repeatYearly" type="checkbox" ${item.repeatYearly ? 'checked' : ''} /><label>Repeat yearly</label></div>
          <div class="field col-12"><label>Description</label><textarea data-field="description">${esc(item.description || '')}</textarea></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-3 switch"><input data-field="publicVisible" type="checkbox" ${item.publicVisible !== false ? 'checked' : ''} /><label>Public visible</label></div>
          <div class="field col-6"><label>Hint</label><input value="Π.χ. 2026-08-01 έως 2026-08-31 ή recurring season με Repeat yearly." readonly /></div>`;
      }
      if(kind === 'categoryRateRule'){
        const categoryOptions = vehicleCategoryRows().map(row => `<option value="${esc(row.category || '')}" ${String(item.categoryCode || item.category || '') === String(row.category || '') ? 'selected' : ''}>${esc(row.displayTitle || row.title || row.category || '')}</option>`).join('');
        const seasonOptions = ['<option value="">All year / no season</option>'].concat(pricingSeasonsRows().map(row => `<option value="${esc(row.key || '')}" ${String(item.seasonKey || '') === String(row.key || '') ? 'selected' : ''}>${esc(row.label || row.title || row.key || '')}</option>`)).join('');
        const presetOptions = DAY_BRACKET_PRESETS.map(row => `<option value="${esc(row.key)}" ${presetKeyForRange(item.minDays, item.maxDays) === row.key ? 'selected' : ''}>${esc(row.label)}</option>`).join('');
        return `
          <div class="field col-4"><label>Key</label><input data-field="key" value="${esc(item.key || '')}" placeholder="a_summer_1_3" /></div>
          <div class="field col-5"><label>Label</label><input data-field="label" value="${esc(item.label || '')}" placeholder="A · Summer · 1-3 ημέρες" /></div>
          <div class="field col-3"><label>Sort order</label><input data-field="sortOrder" type="number" step="1" value="${Number(item.sortOrder || 0)}" /></div>
          <div class="field col-4"><label>Vehicle category</label>${categoryOptions ? `<select data-field="categoryCode">${categoryOptions}</select>` : `<input data-field="categoryCode" value="${esc(item.categoryCode || '')}" placeholder="A" />`}</div>
          <div class="field col-4"><label>Season</label><select data-field="seasonKey">${seasonOptions}</select></div>
          <div class="field col-4"><label>Preset day range</label><select data-field="rangePreset">${presetOptions}</select></div>
          <div class="field col-3"><label>Min days</label><input data-field="minDays" type="number" step="1" min="1" value="${Number(item.minDays || 1)}" /></div>
          <div class="field col-3"><label>Max days (0 = infinity)</label><input data-field="maxDays" type="number" step="1" min="0" value="${Number(item.maxDays || 0)}" /></div>
          <div class="field col-3"><label>Price / day</label><input data-field="pricePerDay" type="number" step="0.01" value="${Number(item.pricePerDay || 0)}" /></div>
          <div class="field col-3"><label>Category ID snapshot</label><input data-field="vehicleCategoryId" value="${esc(item.vehicleCategoryId || vehicleCategoryByCode(item.categoryCode)?._id || '')}" readonly /></div>
          <div class="field col-12"><label>Description</label><textarea data-field="description">${esc(item.description || '')}</textarea></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-3 switch"><input data-field="publicVisible" type="checkbox" ${item.publicVisible !== false ? 'checked' : ''} /><label>Public visible</label></div>
          <div class="field col-6"><label>Hint</label><input value="Μπορείς να βάλεις custom ranges ή να ξεκινήσεις από preset και μετά να τα αλλάξεις χειροκίνητα." readonly /></div>`;
      }

      if(kind === 'pickupLocation'){
        const profiles = effectiveStationProfiles();
        const stationKey = String(item.stationKey || item.station || profiles[0]?.key || 'default').trim().toLowerCase();
        const selectedStation = profiles.find((row) => row.key === stationKey) || profiles[0] || { key:'default', label:'Κεντρικός σταθμός' };
        const stationOptions = profiles.map((row) => `<option value="${esc(row.key)}" ${row.key === selectedStation.key ? 'selected' : ''}>${esc(row.label)} (${esc(row.key)})</option>`).join('');
        return `
          <div class="field col-4"><label>Key</label><input data-field="key" value="${esc(item.key || '')}" placeholder="airport_thessaloniki" /></div>
          <div class="field col-4"><label>Label</label><input data-field="label" value="${esc(item.label || '')}" placeholder="Αεροδρόμιο Θεσσαλονίκης" /></div>
          <div class="field col-4"><label>Sort order</label><input data-field="sortOrder" type="number" step="1" value="${Number(item.sortOrder || 0)}" /></div>
          <div class="field col-4"><label>Station</label><select data-field="stationKey" data-station-profile-select="1">${stationOptions}</select></div>
          <div class="field col-4"><label>Station label</label><input data-field="stationLabel" value="${esc(selectedStation.label || item.stationLabel || item.station || '')}" readonly /></div>
          <div class="field col-4"><label>Type</label><select data-field="locationType"><option value="pickup" ${item.locationType === 'pickup' ? 'selected' : ''}>pickup only</option><option value="dropoff" ${item.locationType === 'dropoff' ? 'selected' : ''}>dropoff only</option><option value="both" ${(!item.locationType || item.locationType === 'both') ? 'selected' : ''}>both</option></select></div>
          <div class="field col-4"><label>Extra fee</label><input data-field="extraFee" type="number" step="0.01" value="${Number(item.extraFee || 0)}" /></div>
          <div class="field col-4"><label>Map URL</label><input data-field="mapUrl" value="${esc(item.mapUrl || '')}" placeholder="https://maps.google.com/..." /></div>
          <div class="field col-12"><label>Address</label><input data-field="address" value="${esc(item.address || '')}" placeholder="Address / σημείο συνάντησης" /></div>
          <div class="field col-12"><label>Description</label><textarea data-field="description">${esc(item.description || '')}</textarea></div>
          <div class="field col-6"><label>Label πεδίου παραλαβής (πτήση/πλοίο/ξενοδοχείο)</label><input data-field="pickupInfoLabel" value="${esc(item.pickupInfoLabel || '')}" placeholder="π.χ. Αριθμός Πτήσης" /></div>
          <div class="field col-6"><label>Placeholder παραλαβής</label><input data-field="pickupInfoPlaceholder" value="${esc(item.pickupInfoPlaceholder || '')}" placeholder="π.χ. A3 224" /></div>
          <div class="field col-4 switch"><input data-field="showOriginField" type="checkbox" ${item.showOriginField !== false ? 'checked' : ''} /><label>Εμφάνιση πεδίου πόλης προέλευσης</label></div>
          <div class="field col-4"><label>Label πεδίου πόλης</label><input data-field="originFieldLabel" value="${esc(item.originFieldLabel || '')}" placeholder="π.χ. Πόλη άφιξης" /></div>
          <div class="field col-4"><label>Placeholder πόλης</label><input data-field="originFieldPlaceholder" value="${esc(item.originFieldPlaceholder || '')}" placeholder="π.χ. Αθήνα" /></div>
          <div class="field col-6"><label>Label πεδίου επιστροφής</label><input data-field="dropoffInfoLabel" value="${esc(item.dropoffInfoLabel || '')}" placeholder="π.χ. Πτήση Αναχώρησης" /></div>
          <div class="field col-6"><label>Placeholder επιστροφής</label><input data-field="dropoffInfoPlaceholder" value="${esc(item.dropoffInfoPlaceholder || '')}" placeholder="π.χ. A3 225" /></div>
          <div class="field col-3 switch"><input data-field="active" type="checkbox" ${item.active !== false ? 'checked' : ''} /><label>Active</label></div>
          <div class="field col-3 switch"><input data-field="publicVisible" type="checkbox" ${item.publicVisible !== false ? 'checked' : ''} /><label>Public visible</label></div>
          <div class="field col-6"><label>Hint</label><input value="Σταθμός = group οργάνωσης (π.χ. airport / center). Location = συγκεκριμένο σημείο εξυπηρέτησης του σταθμού." readonly /></div>`;
      }

      if(kind === 'insurance'){
        return common + `
          <div class="field col-3"><label>Price / day</label><input data-field="pricePerDay" type="number" step="0.01" value="${Number(item.pricePerDay || 0)}" /></div>
          <div class="field col-3"><label>Billing mode</label><input value="perDay" readonly /></div>`;
      }
      if(kind === 'extra'){
        return common + `
          <div class="field col-3"><label>Price</label><input data-field="price" type="number" step="0.01" value="${Number(item.price || 0)}" /></div>
          <div class="field col-3"><label>Billing mode</label><select data-field="billingMode"><option value="perDay" ${item.billingMode === 'perDay' ? 'selected' : ''}>perDay</option><option value="perBooking" ${item.billingMode === 'perBooking' ? 'selected' : ''}>perBooking</option></select></div>`;
      }
      return common + `
        <div class="field col-3"><label>Rule type</label><select data-field="ruleType"><option value="manual" ${item.ruleType === 'manual' ? 'selected' : ''}>manual</option><option value="ageRange" ${item.ruleType === 'ageRange' ? 'selected' : ''}>ageRange</option><option value="nightPickup" ${item.ruleType === 'nightPickup' ? 'selected' : ''}>nightPickup</option><option value="nightDropoff" ${item.ruleType === 'nightDropoff' ? 'selected' : ''}>nightDropoff</option><option value="night" ${item.ruleType === 'night' ? 'selected' : ''}>night</option></select></div>
        <div class="field col-3"><label>Audience group</label><input data-field="audienceGroup" value="${esc(item.audienceGroup || '')}" placeholder="π.χ. 19-22" /></div>
        <div class="field col-3"><label>Amount</label><input data-field="amount" type="number" step="0.01" value="${Number(item.amount || 0)}" /></div>
        <div class="field col-3"><label>Billing mode</label><select data-field="billingMode"><option value="perBooking" ${item.billingMode === 'perBooking' ? 'selected' : ''}>perBooking</option><option value="perDay" ${item.billingMode === 'perDay' ? 'selected' : ''}>perDay</option></select></div>`;
    }
    function editorPayload(){
      const obj = { _id: state.editor.item?._id || '' };
      els.editorGrid.querySelectorAll('[data-field]').forEach(node => {
        const field = node.dataset.field;
        if(node.type === 'checkbox') obj[field] = !!node.checked;
        else if(node.type === 'number') obj[field] = Number(node.value || 0);
        else obj[field] = String(node.value || '').trim();
      });
      if(state.editor.kind === 'role'){
        obj.specialPermissions = Array.from(els.editorGrid.querySelectorAll('[data-special-box]:checked')).map(node => node.value).join('|');
      }
      if(state.editor.kind === 'categoryRateRule'){
        delete obj.rangePreset;
        obj.minDays = Math.max(1, Number(obj.minDays || 1));
        obj.maxDays = Math.max(0, Number(obj.maxDays || 0));
      }
      return obj;
    }

    function renderAll(){ pruneLegacyStationToolbar(); renderBackroomHeader(); renderTabs(); renderSections(); renderAccessGate(); renderKpis(); renderHealth(); setBusinessForm(); renderStationWorkspace(); renderStationProfilesTable(); renderVehicleCategoriesTable(); renderFleetVehiclesTable(); renderPickupLocationsTable(); renderDynamicPricingSection(); renderInsuranceTable(); renderExtrasTable(); renderFeesTable(); renderRolesTable(); renderUsersTable(); renderAuditTable(); requestResize(); }

    const TRUSTED_PARENT_SUFFIXES = ['wix.com','wixsite.com','parastorage.com','wixstatic.com'];
    function isTrustedParentOrigin(origin){ const raw=String(origin||'').trim().toLowerCase(); if(!raw) return true; try{ const host=new URL(raw).hostname.toLowerCase(); if(!host) return false; if(TRUSTED_PARENT_SUFFIXES.some(s=>host===s||host.endsWith('.'+s))) return true; try{ const ref=document.referrer; if(ref){ const rh=new URL(ref).hostname.toLowerCase(); if(rh&&host===rh) return true; } }catch(_){} return false; }catch(_){ return false; } }
    function handleMessage(event){
      if (event.origin && !isTrustedParentOrigin(event.origin)) return;
      const data = normalizeMessage(event.data);
      if(!data || !data.type) return;
      if(data.type === 'adminSnapshot'){
        state.snapshot = clone(data.snapshot) || { businessSettings:{}, vehicleCategories:[], fleetVehicles:[], pickupLocations:[], pricingSeasons:[], categoryRateRules:[], insurancePlans:[], extraServices:[], feeRules:[] };
        state.accessSnapshot = clone(data.accessSnapshot) || { accessState:'authRequired', currentStaff:null, roles:[], users:[], auditRows:[], sessionRows:[], bootstrapHelp:null, meta:{} };
        renderAll();
        writeSettingsSnapshot();
        setBusy(false);
        return;
      }
      if(data.type === 'toast'){ showToast(data.message || '', data.tone || 'info'); return; }
      if(data.type === 'seedPricingResult'){ const r = data.result || {}; const parts = ['business','insurance','extras','fees'].filter(k => r[k] && Number(r[k].total || 0) > 0).map(k => `${k}: +${Number(r[k].inserted || 0)} / ↻${Number(r[k].updated || 0)}`); showToast(parts.length ? `Seed defaults ολοκληρώθηκε · ${parts.join(' · ')}` : 'Seed defaults ολοκληρώθηκε.', 'success'); return; }
      if(data.type === 'resetPricingResult'){ const r = data.result || {}; const parts = ['business','insurance','extras','fees'].filter(k => r[k] && Number(r[k].total || 0) > 0).map(k => `${k}: +${Number(r[k].inserted || 0)} / ↻${Number(r[k].updated || 0)}`); showToast(parts.length ? `Reset defaults ολοκληρώθηκε · ${parts.join(' · ')}` : 'Reset defaults ολοκληρώθηκε.', 'success'); return; }
      if(data.type === 'staffPasswordResult'){ const result = data.result || {}; const when = result.changedAt ? ` at ${new Date(result.changedAt).toLocaleString('el-GR')}` : ''; state.securityResult = result.mode === 'reset' ? `Temporary password reset issued for ${result.email || 'selected user'}${when}. Share credentials out-of-band.` : `Password updated for ${result.email || 'selected user'}${when}.`; renderSecuritySection(); showToast(result.mode === 'reset' ? 'Temporary password reset completed.' : 'Password updated.', 'success'); return; }
      if(data.type === 'staffInviteResult'){ const result = data.result || {}; state.securityEmail = String(result.email || '').toLowerCase(); state.activeSection = 'security'; state.securityResult = result.onboardingMessage || `Temporary password for ${result.email}: ${result.tempPassword}`; renderTabs(); renderSections(); renderSecuritySection(); requestResize(); showToast(result.mode === 'invite' ? 'Staff onboarding έτοιμο.' : 'Εκδόθηκε νέο onboarding access.', 'success'); return; }
      if(data.type === 'sessionActionResult'){ const result = data.result || {}; state.securityResult = `Revoked sessions: ${Number(result.revoked || 0)}`; renderSecuritySection(); return; }
      if(data.type === 'busy'){ setBusy(!!data.flag); return; }
    }

    function openStationModal(mode = 'create', existing = null){
      state.stationModal = { open:true, mode, originalKey: String(existing?.key || '') };
      if (els.stationProfileTitle) els.stationProfileTitle.textContent = mode === 'edit' ? 'Επεξεργασία σταθμού' : 'Νέος σταθμός';
      if (els.stationProfileSub) els.stationProfileSub.textContent = mode === 'edit' ? 'Αλλαγή βασικών στοιχείων station profile.' : 'Δημιουργία νέου station profile για station-first ρυθμίσεις.';
      if (els.stationProfileKey) els.stationProfileKey.value = String(existing?.key || '');
      if (els.stationProfileLabel) els.stationProfileLabel.value = String(existing?.label || '');
      if (els.stationProfileSortOrder) els.stationProfileSortOrder.value = String(Number(existing?.sortOrder ?? 9999));
      if (els.stationProfileCity) els.stationProfileCity.value = String(existing?.city || '');
      if (els.stationProfileAddress) els.stationProfileAddress.value = String(existing?.address || '');
      if (els.stationProfileDescription) els.stationProfileDescription.value = String(existing?.description || '');
      if (els.stationProfileActive) els.stationProfileActive.checked = existing?.active !== false;
      if (els.deleteStationProfileModalBtn) els.deleteStationProfileModalBtn.style.display = mode === 'edit' ? '' : 'none';
      if (els.stationProfileOverlay) els.stationProfileOverlay.classList.add('show');
    }

    function closeStationModal(){
      state.stationModal = { open:false, mode:'create', originalKey:'' };
      if (els.stationProfileOverlay) els.stationProfileOverlay.classList.remove('show');
    }

    function stationProfileFromModal(){
      return normalizeStationProfileRow({
        key: els.stationProfileKey?.value || '',
        label: els.stationProfileLabel?.value || '',
        sortOrder: els.stationProfileSortOrder?.value || 9999,
        city: els.stationProfileCity?.value || '',
        address: els.stationProfileAddress?.value || '',
        description: els.stationProfileDescription?.value || '',
        active: !!els.stationProfileActive?.checked
      });
    }

    function upsertStationProfileLocal(next){
      const normalized = normalizeStationProfileRow(next || {});
      const rows = [...effectiveStationProfiles().filter((row) => row.key !== normalized.key), normalized];
      state.stationProfiles = rows.sort((a, b) => Number(a.sortOrder || 9999) - Number(b.sortOrder || 9999) || String(a.label || '').localeCompare(String(b.label || ''), 'el'));
      state.locationStationScope = normalized.key;
      renderStationWorkspace();
      renderStationProfilesTable();
      renderPickupLocationsTable();
      showToast('Ο σταθμός ενημερώθηκε. Πάτησε Save business settings για αποθήκευση.', 'info');
    }

    function deleteStationProfileLocal(key){
      const stationKey = String(key || '').trim().toLowerCase();
      if (!stationKey || stationKey === 'default') { showToast('Ο default σταθμός δεν διαγράφεται.', 'error'); return; }
      const hasLocations = pickupLocationRows().some((row) => String(row.stationKey || row.station || 'default').trim().toLowerCase() === stationKey);
      if (hasLocations) { showToast('Υπάρχουν locations στον σταθμό. Μετέφερέ τα πρώτα σε άλλο station.', 'error'); return; }
      state.stationProfiles = effectiveStationProfiles().filter((row) => row.key !== stationKey);
      removeStationSettingsKey(stationKey);
      state.locationStationScope = 'all';
      renderStationWorkspace();
      renderStationProfilesTable();
      renderPickupLocationsTable();
      showToast('Ο σταθμός αφαιρέθηκε. Πάτησε Save business settings για αποθήκευση.', 'info');
    }

    function bind(){
      if (els.refreshSnapshotBtn) els.refreshSnapshotBtn.addEventListener('click', () => { setBusy(true); post('refreshStaffAccess', {}); });
      if (els.seedAllDefaultsBtn) els.seedAllDefaultsBtn.addEventListener('click', () => { setBusy(true); post('seedPricingDefaults', { payload: { target:'all' } }); });
      if (els.resetBusinessDefaultsBtn) els.resetBusinessDefaultsBtn.addEventListener('click', () => { setBusy(true); post('resetPricingDefaults', { payload: { target:'business' } }); });
      if (els.exportSnapshotBtn) els.exportSnapshotBtn.addEventListener('click', exportSnapshot);
      if (els.copySnapshotBtn) els.copySnapshotBtn.addEventListener('click', copySnapshot);
      const copySectionDigestBtn = document.getElementById('copySectionDigestBtn'); if (copySectionDigestBtn) copySectionDigestBtn.addEventListener('click', async () => { const digest = buildCurrentSectionDigest(); try { if (navigator.clipboard?.writeText) { await navigator.clipboard.writeText(digest); showToast('Το section digest αντιγράφηκε.', 'success'); return; } } catch(_) {} const area = document.createElement('textarea'); area.value = digest; document.body.appendChild(area); area.select(); try { document.execCommand('copy'); showToast('Το section digest αντιγράφηκε.', 'success'); } catch(_) { showToast('Δεν έγινε copy section digest.', 'error'); } area.remove(); });
      const queueSettingsSectionBtn = document.getElementById('queueSettingsSectionBtn'); if (queueSettingsSectionBtn) queueSettingsSectionBtn.addEventListener('click', () => { queueCurrentSettingsSection(); });
      if (els.pinSettingsViewBtn) els.pinSettingsViewBtn.addEventListener('click', pinCurrentSettingsView);
      if (els.saveSettingsViewBtn) els.saveSettingsViewBtn.addEventListener('click', saveCurrentSettingsView);
      if (els.clearWorkspaceViewsBtn) els.clearWorkspaceViewsBtn.addEventListener('click', clearSavedWorkspaceViews);
      if (els.clearWorkspacePinsBtn) els.clearWorkspacePinsBtn.addEventListener('click', clearPinnedWorkspaceViews);
      els.saveBusinessBtn.addEventListener('click', () => {
        setBusy(true);
        post('saveBusinessSettings', { payload: businessPayload() });
      });
      ['bsVehiclesPageDisplayMode','bsVehiclesPageModelsSource'].forEach((id) => {
        const node = document.getElementById(id);
        if(node) node.addEventListener('change', () => { renderVehicleCatalogPreview(); requestResize(); });
      });
      enhanceBusinessTextEditors();
      els.refreshSnapshotBtn.addEventListener('click', () => { setBusy(true); post('pricingAdminReady', {}); });
      els.exportSnapshotBtn.addEventListener('click', () => exportSnapshot());
      document.querySelectorAll('[data-add-kind]').forEach(btn => btn.addEventListener('click', () => openEditor(btn.dataset.addKind, null)));
      document.querySelectorAll('[data-seed-target]').forEach(btn => btn.addEventListener('click', () => { setBusy(true); post('seedPricingDefaults', { payload: { target: btn.dataset.seedTarget || 'all' } }); }));
      document.querySelectorAll('[data-reset-target]').forEach(btn => btn.addEventListener('click', () => { setBusy(true); post('resetPricingDefaults', { payload: { target: btn.dataset.resetTarget || 'all' } }); }));
      if (els.searchVehicleCategories) els.searchVehicleCategories.addEventListener('input', (e) => { state.filters.vehicleCategories = e.target.value; renderVehicleCategoriesTable(); requestResize(); });
      if (els.searchFleetVehicles) els.searchFleetVehicles.addEventListener('input', (e) => { state.filters.fleetVehicles = e.target.value; renderFleetVehiclesTable(); requestResize(); });
      if (els.searchStationProfiles) els.searchStationProfiles.addEventListener('input', (e) => { state.filters.stationProfiles = e.target.value; renderStationProfilesTable(); requestResize(); });
      if (els.searchPickupLocations) els.searchPickupLocations.addEventListener('input', (e) => { state.filters.pickupLocations = e.target.value; renderPickupLocationsTable(); requestResize(); });
      if (els.locationStationScope) els.locationStationScope.addEventListener('change', (e) => { state.locationStationScope = String(e.target.value || 'all'); renderPickupLocationsTable(); requestResize(); });
      if (els.stationsLocationsTabs) els.stationsLocationsTabs.addEventListener('click', (e) => {
        const btn = e.target?.closest?.('[data-locations-workspace-tab]');
        if (!btn) return;
        state.locationsWorkspaceTab = String(btn.dataset.locationsWorkspaceTab || 'stations');
        renderStationsLocationsTabs();
        requestResize();
      });
      if (els.addStationProfileBtn) els.addStationProfileBtn.addEventListener('click', () => {
        openStationModal('create', null);
      });
      if (els.stationProfilesWrap) els.stationProfilesWrap.addEventListener('click', (e) => {
        const btn = e.target?.closest?.('[data-station-action]');
        if (!btn) return;
        const key = String(btn.dataset.stationKey || '');
        if (!key) return;
        if (btn.dataset.stationAction === 'edit') {
          const current = effectiveStationProfiles().find((row) => row.key === key);
          if (!current) { showToast('Δεν βρέθηκε station profile.', 'error'); return; }
          openStationModal('edit', current);
          return;
        }
        if (btn.dataset.stationAction === 'delete') {
          deleteStationProfileLocal(key);
          renderStationProfilesTable();
        }
      });
      if (els.closeStationProfileBtn) els.closeStationProfileBtn.addEventListener('click', closeStationModal);
      if (els.cancelStationProfileBtn) els.cancelStationProfileBtn.addEventListener('click', closeStationModal);
      if (els.stationProfileOverlay) els.stationProfileOverlay.addEventListener('click', (e) => { if (e.target === els.stationProfileOverlay) closeStationModal(); });
      if (els.saveStationProfileBtn) els.saveStationProfileBtn.addEventListener('click', () => {
        const next = stationProfileFromModal();
        if (!next.key || !next.label) { showToast('Συμπλήρωσε station key και station label.', 'error'); return; }
        const mode = state.stationModal?.mode || 'create';
        const originalKey = String(state.stationModal?.originalKey || '');
        if (mode === 'edit' && originalKey && originalKey !== next.key) {
          renameStationSettingsKey(originalKey, next.key);
          migratePickupLocationsStation(originalKey, next.key, next.label);
          state.stationProfiles = effectiveStationProfiles().filter((row) => row.key !== originalKey);
        }
        upsertStationProfileLocal(next);
        state.activeBusinessStationKey = next.key;
        closeStationModal();
      });
      if (els.deleteStationProfileModalBtn) els.deleteStationProfileModalBtn.addEventListener('click', () => {
        const key = String(state.stationModal?.originalKey || '');
        if (!key) return;
        deleteStationProfileLocal(key);
        closeStationModal();
      });
      if (els.searchPricingSeasons) els.searchPricingSeasons.addEventListener('input', (e) => { state.filters.pricingSeasons = e.target.value; renderPricingSeasonsTable(); requestResize(); });
      if (els.searchCategoryRateRules) els.searchCategoryRateRules.addEventListener('input', (e) => { state.filters.categoryRateRules = e.target.value; renderCategoryRateRulesTable(); requestResize(); });
      if (els.filterRateRuleCategory) els.filterRateRuleCategory.addEventListener('change', (e) => { state.filters.rateRuleCategory = e.target.value; renderCategoryRateRulesTable(); requestResize(); });
      if (els.filterRateRuleSeason) els.filterRateRuleSeason.addEventListener('change', (e) => { state.filters.rateRuleSeason = e.target.value; renderCategoryRateRulesTable(); requestResize(); });
      if (els.simCategoryCode) els.simCategoryCode.addEventListener('change', () => { const vehicle = vehicleCategoryByCode(els.simCategoryCode.value || ''); if (vehicle?.price) els.simBasePrice.value = String(vehicle.price); renderPricingSimulator(); requestResize(); });
      if (els.simPickupDate) els.simPickupDate.addEventListener('change', () => { renderPricingSimulator(); requestResize(); });
      if (els.simDays) els.simDays.addEventListener('input', () => { renderPricingSimulator(); requestResize(); });
      if (els.simBasePrice) els.simBasePrice.addEventListener('input', () => { renderPricingSimulator(); requestResize(); });
      els.searchInsurance.addEventListener('input', (e) => { state.filters.insurance = e.target.value; renderInsuranceTable(); requestResize(); });
      els.searchExtras.addEventListener('input', (e) => { state.filters.extras = e.target.value; renderExtrasTable(); requestResize(); });
      els.searchFees.addEventListener('input', (e) => { state.filters.fees = e.target.value; renderFeesTable(); requestResize(); });
      if (els.searchSessions) els.searchSessions.addEventListener('input', (e) => { state.filters.sessions = e.target.value; renderSecuritySection(); requestResize(); });
      if (els.openSecurityFromUsersBtn) els.openSecurityFromUsersBtn.addEventListener('click', () => { state.activeSection = 'security'; renderTabs(); renderSections(); renderSecuritySection(); requestResize(); });
      if (els.securityUserSelect) els.securityUserSelect.addEventListener('change', (e) => { state.securityEmail = String(e.target.value || '').toLowerCase(); state.securityResult = ''; renderSecuritySection(); requestResize(); });
      if (els.refreshSecurityBtn) els.refreshSecurityBtn.addEventListener('click', () => { setBusy(true); post('refreshStaffAccess', {}); });
      if (els.setPasswordBtn) els.setPasswordBtn.addEventListener('click', () => { const email = String(els.securityUserSelect.value || '').trim(); const pw = String(els.securityPassword.value || ''); const confirm = String(els.securityPasswordConfirm.value || ''); if(!email){ showToast('Επίλεξε staff user.', 'error'); return; } if(pw.length < 8){ showToast('Το password πρέπει να έχει τουλάχιστον 8 χαρακτήρες.', 'error'); return; } if(pw !== confirm){ showToast('Τα passwords δεν ταιριάζουν.', 'error'); return; } setBusy(true); post('setStaffPassword', { payload: { email, newPassword: pw, mustChangePassword: !!els.securityMustChange.checked } }); els.securityPassword.value=''; els.securityPasswordConfirm.value=''; });
      if (els.resetPasswordBtn) els.resetPasswordBtn.addEventListener('click', () => { const email = String(els.securityUserSelect.value || '').trim(); if(!email){ showToast('Επίλεξε staff user.', 'error'); return; } setBusy(true); post('resetStaffPassword', { payload: { email } }); });
      if (els.revokeUserSessionsBtn) els.revokeUserSessionsBtn.addEventListener('click', () => { const email = String(els.securityUserSelect.value || '').trim(); if(!email){ showToast('Επίλεξε staff user.', 'error'); return; } setBusy(true); post('revokeStaffSessions', { payload: { email } }); });
      els.searchRoles.addEventListener('input', (e) => { state.filters.roles = e.target.value; renderRolesTable(); requestResize(); });
      els.searchUsers.addEventListener('input', (e) => { state.filters.users = e.target.value; renderUsersTable(); requestResize(); });
      if (els.searchAudit) els.searchAudit.addEventListener('input', (e) => { state.filters.audit = e.target.value; renderAuditTable(); requestResize(); });
      if (els.filterAuditType) els.filterAuditType.addEventListener('change', (e) => { state.filters.auditType = e.target.value; renderAuditTable(); requestResize(); });
      els.filterRuleType.addEventListener('change', (e) => { state.filters.ruleType = e.target.value; renderFeesTable(); requestResize(); });
      els.refreshAuditBtn.addEventListener('click', () => { setBusy(true); post('refreshStaffAccess', {}); });
      els.closeEditorBtn.addEventListener('click', closeEditor);
      els.cancelEditorBtn.addEventListener('click', closeEditor);
      els.editorOverlay.addEventListener('click', (e) => { if(e.target === els.editorOverlay) closeEditor(); });
      els.saveEditorBtn.addEventListener('click', () => {
        const payload = editorPayload();
        if(state.editor.kind === 'user'){ if(!payload.email || !payload.roleKey){ showToast('Συμπλήρωσε email και role.', 'error'); return; } }
        else if(state.editor.kind === 'vehicleCategory'){ if(!payload.title || !payload.category){ showToast('Συμπλήρωσε τίτλο και category code.', 'error'); return; } }
        else if(state.editor.kind === 'fleetVehicle'){ if(!payload.plate || !payload.model || !payload.category){ showToast('Συμπλήρωσε πινακίδα, μοντέλο και category.', 'error'); return; } }
        else if(state.editor.kind === 'pickupLocation'){ if(!payload.key || !payload.label || !payload.address || !payload.stationKey || !payload.stationLabel){ showToast('Συμπλήρωσε station, key, label και address.', 'error'); return; } }
        else if(state.editor.kind === 'pricingSeason'){ if(!payload.key || !payload.label || !payload.startDate || !payload.endDate){ showToast('Συμπλήρωσε key, label και season dates.', 'error'); return; } }
        else if(state.editor.kind === 'categoryRateRule'){ if(!payload.key || !payload.label || !payload.categoryCode){ showToast('Συμπλήρωσε key, label και κατηγορία.', 'error'); return; } if(Number(payload.minDays || 0) < 1 || Number(payload.maxDays || 0) < 0){ showToast('Το day range δεν είναι έγκυρο.', 'error'); return; } if(Number(payload.maxDays || 0) > 0 && Number(payload.maxDays || 0) < Number(payload.minDays || 1)){ showToast('Το max days πρέπει να είναι >= από το min days ή 0 για infinity.', 'error'); return; } }
        else if(!payload.key || !payload.label){ showToast('Συμπλήρωσε τουλάχιστον key και label.', 'error'); return; }
        setBusy(true);
        if(state.editor.kind === 'vehicleCategory') post('upsertVehicleCategory', { payload });
        else if(state.editor.kind === 'fleetVehicle') post('upsertFleetVehicle', { payload });
        else if(state.editor.kind === 'pickupLocation') post('upsertPickupLocation', { payload });
        else if(state.editor.kind === 'pricingSeason') post('upsertPricingSeason', { payload });
        else if(state.editor.kind === 'categoryRateRule') post('upsertCategoryRateRule', { payload });
        else if(state.editor.kind === 'insurance') post('upsertInsurancePlan', { payload });
        else if(state.editor.kind === 'extra') post('upsertExtraService', { payload });
        else if(state.editor.kind === 'role') post('upsertStaffRole', { payload });
        else if(state.editor.kind === 'user') post('upsertStaffUser', { payload });
        else post('upsertFeeRule', { payload });
        closeEditor();
      });
      if (els.closeInspectorBtn) els.closeInspectorBtn.addEventListener('click', closeInspector);
      if (els.inspectorDoneBtn) els.inspectorDoneBtn.addEventListener('click', closeInspector);
      if (els.inspectorOverlay) els.inspectorOverlay.addEventListener('click', (e) => { if(e.target === els.inspectorOverlay) closeInspector(); });
      els.deleteEditorBtn.addEventListener('click', () => {
        const item = state.editor.item || {};
        if(!item._id) return;
        if(state.editor.kind === 'vehicleCategory'){
          setBusy(true);
          post('deleteVehicleCategory', { payload: { itemId: item._id, categoryCode: item.category } });
          closeEditor();
          return;
        }
        if(state.editor.kind === 'fleetVehicle'){
          setBusy(true);
          post('deleteFleetVehicle', { payload: { itemId: item._id, plate: item.plate } });
          closeEditor();
          return;
        }
        if(state.editor.kind === 'pickupLocation'){
          setBusy(true);
          post('deletePickupLocation', { payload: { itemId: item._id, itemKey: item.key } });
          closeEditor();
          return;
        }
        if(state.editor.kind === 'pricingSeason'){
          setBusy(true);
          post('deletePricingSeason', { payload: { itemId: item._id, itemKey: item.key } });
          closeEditor();
          return;
        }
        if(state.editor.kind === 'categoryRateRule'){
          setBusy(true);
          post('deleteCategoryRateRule', { payload: { itemId: item._id, itemKey: item.key } });
          closeEditor();
          return;
        }
        if(!['insurance','extra','fee'].includes(state.editor.kind)) return;
        const collectionName = state.editor.kind === 'insurance' ? 'InsurancePlans' : state.editor.kind === 'extra' ? 'ExtraServices' : 'FeeRules';
        setBusy(true);
        post('deletePricingItem', { payload: { collectionName, itemId: item._id, itemKey: item.key } });
        closeEditor();
      });
    }

    window.addEventListener('message', handleMessage);
    bind();
    renderTabs();
    renderSections();
    post('pricingAdminReady', {});
    requestResize();
    setTimeout(requestResize, 400);
    setTimeout(requestResize, 1200);
  </script>
</body>
</html>
